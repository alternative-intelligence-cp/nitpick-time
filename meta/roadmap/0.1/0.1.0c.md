# 0.1.0c — the access properties: `Vec`, `Bytes`, `CivilDate`, `CivilTime` — PLANNED

> **Scope:** the board's re-pin worklist **item 13** — `Vec<T>`'s `items`
> hidden and `count`/`cap` sealed under the prelude's `ListLen`, `Bytes`'
> `body` and `len` sealed and `len` under `ListLen` — **and the same mechanism
> applied to the two types whose whole contract is a validating constructor**:
> every field of `CivilDate` and `CivilTime` sealed, which gives
> `CALENDAR.md` C-8 back the guarantee C-8b said the language could not
> express. With them: the `LimitViolated` arm in every consumer that reaches a
> limited write, the arm generator's rule for it, five probes, one probe's
> verdict flipped, and `check_civil_literal` retired.
>
> **Written 2026-09-25 by the stream-2 planner** from a dry run on top of
> 0.1.0b's dry-run end state, which ended `GREEN -- 75 unit(s), 0 failures;
> 5 pending` at `c3bdae2` (with `check_civil_literal` not yet retired). Every
> number is a `c3bdae2` number from that run; **re-derive each one** with the
> command beside it.

**Gate for starting:**

1. `0.1.0b.md` is `DONE`, its verifier said PASS, and CI was green on its
   commit 2 (`0.1.0b.md`'s commit 3 names the run).
2. **`LimitViolated`'s exit code 109 confirmed by the orchestrator** for both
   streams (`0.1.0b.md` §2). `nitpick-regex` gives its `Vec`, `Bytes` and
   `SparseSet` the same properties and owes the same arm.
3. Q-6 is **not** a gate: the one contract-like construct here, a field's
   `limit<ListLen>`, is the board's decided design (item 13), not a P-1 choice.

---

## Why a subcycle of its own, and why here

**Decided at planning: item 13 is `0.1.0c`, after the adoption and before
0.1.1 — not folded into 0.1.0b.**

- **The adoption is mechanical and this is design.** 0.1.0b's acceptance is
  "compiles at the new pin and nothing else changed"; a verifier can check it
  by construction. This subcycle changes what a CONSUMER may write (four fields
  refuse a write, one refuses a read), adds an arm every `Vec`/`Bytes` consumer
  owes, and narrows a specification rule (C-8b). Mixed into one commit, a
  reviewer cannot tell a clause from a contract change, and a revert of one
  reverts both.
- **Before 0.1.1, because 0.1.1 is written against it.** With the seal,
  `date_to_days`'s argument can only have come from `civil_date` (or from
  `wild` storage, the author's opt-out); 0.1.1's plan leans on that and says so.
- *Declined — item 13 inside 0.1.0b:* the orchestrator's brief allowed either;
  the two reasons above decided it.
- *Declined — the `CivilDate` seal deferred to 0.1.1:* 0.1.1 is the algorithms;
  a public-surface change there would be the one line of that diff nobody
  reviews as a surface change.

**The `CivilDate`/`CivilTime` half is the planner's addition to item 13, and it
is flagged as such.** It is not on the board's list. It is here because C-8b's
premise — *"the language has no private field"* — is **false at `c3bdae2`**
(D-313, D-314), and `tests/probe/probe15_civil_literal_bypass.npk` was written to
announce exactly this day: *"If a future compiler grows per-field visibility …
this file stops compiling — and that is the notification that C-8b can be
narrowed and `check_civil_literal` can be retired."* It stops compiling here.

---

## 0. Read these first

Same variables as `0.1.0b.md` §0.

1. The compiler at the pin, READ ONLY:
   `git -C "$NPK_TREE" show c3bdae2:meta/specs/TYPE_REFERENCE.md` §9.1.1 (`sealed`,
   `hidden`) and §9.1.2 (a field's own `limit`), and
   `VERIFICATION_REFERENCE.md` §2.1 (*"A field's own rule"*); `DECISIONS.md`
   D-308, D-313, D-314.
2. `$WB/BOARD.md` — the worklist entry's item 13 and the entry *"THE CONTAINER
   QUESTION IS ANSWERED … KEEP OUR `Vec` AND GIVE IT THE THREE PROPERTIES"*.
3. `../../specs/CALENDAR.md` C-8 and C-8b; `../../DECISIONS.md` TM-148;
   `../../specs/SAFETY.md` S-4, S-17b, S-17c; `../../specs/BUILD.md` B-12.
4. `src/core/vec.npk` and `src/core/bytes.npk` headers, and
   `tests/probe/probe15_civil_literal_bypass.npk`'s header.

---

## 1. What was measured — the facts this plan rests on

**The four declarations, exactly as they compile at `c3bdae2`:**

```nitpick
pub struct:Vec<T> = { hidden wild T->:items; sealed limit<ListLen> int64:count; sealed limit<ListLen> int64:cap; };
pub struct:Bytes = { sealed buffer:body; sealed limit<ListLen> int64:len; };
pub struct:CivilDate = { sealed int32:year; sealed uint8:month; sealed uint8:day; };
pub struct:CivilTime = { sealed uint8:hour; sealed uint8:minute; sealed uint8:second; sealed uint32:nanos; };
```

- **THE ORDER IS `sealed limit<R> T:f`, AND THE COMPILER'S OWN REFERENCE SHOWS
  THE OTHER ONE.** `limit<ListLen> sealed int64:count` is `NITPICK-PARSE-001`,
  *"expected a type"*, at the column of `sealed` — measured. Yet
  `TYPE_REFERENCE.md` §9.1.2's example at `c3bdae2` is
  `limit<r_level> sealed int64:n;`. `VERIFICATION_REFERENCE.md` §2.1 writes
  `sealed limit<Len> int64:count;`, and that is the order that parses. **A
  finding for the compiler side, relayed by the orchestrator, not a defect this
  subcycle stops for.**
- **`ListLen` resolves with no import** (the prelude's `pub Rules<int64>`,
  D-308 §6; its `pub` is stored since DEF-93). Its rule
  `{ $ >= 0i64, $ <= 140737488355328i64 }` admits the vacant value 0, so
  `TYPE-077` is satisfied; `Vec<T>`'s fields are `int64`, not `T`, so the
  generic struct is no obstacle to `TYPE-059`.
- **No field this plan limits has its address taken anywhere in the tree**
  (`TYPE-063` would refuse it): every `Vec`/`Bytes` function takes the struct
  through a pointer and writes the field through it, which is a write point,
  not an address.

**What a CONSUMER can and cannot do afterwards — each a program run at
`c3bdae2`:**

```
v.count = 3i64;                      NITPICK-TYPE-079   (sealed: written only inside `vec`)
int64:x = v.items[0i64];             NITPICK-TYPE-080   (hidden: not even read outside `vec`)
b.len = 2i64;                        NITPICK-TYPE-079
CivilDate{ year: 32000i32, … }       NITPICK-TYPE-079 ×3, one per field named
a.month = 13u8;                      NITPICK-TYPE-079
CivilDate:v;  then v.month           NITPICK-ASSIGN-001 -- no default value to read (D-010)
v.count, v.cap, b.len, b.body.cap,   compile and run -- a sealed field is READ anywhere
d.month, d.clone(), d.cmp(e)
```

**So C-8's guarantee is the TYPE's again, for every module but `cal`**: a
consumer cannot build, overwrite or default-read an unreal `CivilDate`. The one
route left is `wild` storage reinterpreted by `=>!` — the language's opt-out for
every checked property, and stated as such, not hidden.

**The blast radius — measured, and small:**

- **`LimitViolated` is demanded by nine roots**, every one a consumer of
  `vec.npk` or `bytes.npk`: `tests/conformance/import.npk`,
  `tests/probe/defect/generic_owning_copy/case5_vec_at_destructive.npk`,
  `tests/unit/bytes_growth.npk`, `bytes_put_int.npk`, `bytes_view_lifetime.npk`,
  `vec_at_past_end.npk`, `vec_boundaries.npk`, `vec_pop_empty.npk`,
  `vec_set_past_end.npk`.
- **`probe15_civil_literal_bypass.npk` is refused `NITPICK-TYPE-079` ×3**, as
  its header predicted.
- **Nothing else moves.** No test writes `count`, `cap` or `len` from outside,
  none reads `.items` of the library's `Vec` (the probes that index `.items`
  declare their own `Vec`), and `civil_order.npk` and `civil_construct.npk`
  build through the constructors.

**The arm bills** (a program importing the one module, no `failsafe`, reading
`NITPICK-REACH-003`'s list): `vec.npk` 8 → **9**, `bytes.npk` 11 → **12**, the
umbrella `src/lib.npk` 12 → **13**; `cal.npk` stays **11** — sealing adds no
identity.

---

## 2. Steps, in order

Every step ends with the compile sweep of `0.1.0b.md` step 0 re-run; steps 7
and 8 end with the full harness.

### Step 1 — `Vec<T>` (draft PD-8)

Replace the declaration with §1's. Rewrite `vec.npk`'s field comment and its
BOUNDS section: **`items` is hidden now, so no code outside `vec` can index the
bare pointer at all** — what TM-108 and TM-129 kept true by convention and
`check_raw_index` by a tree check is a compile error for every other module
(`TYPE-080`). `check_raw_index` stays: it still watches `Bytes`' `.ptr[`, which
`sealed` does not hide (a read through a sealed field's pointer is a read), and
it still watches `vec.npk` itself. **Say that `count` and `cap` are checked
against `ListLen` after every write, in every build, trapping `LimitViolated`**,
and that the payoff the board recorded is the verified build's (the bounds rows
of the producer calls and `vec_reserve`'s `v.cap * 2i64` overflow row become
dischargeable), not a plain build's.

**Ends with:** `vec.npk` compiles; the sweep shows `LimitViolated` demanded by
the `vec` consumers of §1's list.

### Step 2 — `Bytes` (draft PD-8)

Replace the declaration with §1's. The field is `body`, not the board's `buf`
— that was `nitpick-regex`'s name. `body` is **sealed, not hidden**: tests and
the umbrella read `b.body.cap`, and hiding it would refuse them. `bytes_reserve`'s
`b.body = move(fresh);` is inside the module and stays legal. Update the header
paragraph on BOUNDS the same way as `vec.npk`'s.

**Ends with:** `bytes.npk` compiles; the sweep's `LimitViolated` set is §1's
nine roots, exactly.

### Step 3 — `CivilDate` and `CivilTime` sealed (draft PD-9)

Replace the two declarations with §1's. `CivilDateTime` is **not** sealed: its
members can only have come from the constructors, so its literal checks nothing
new (0.1.0's "no `civil_datetime`" reasoning, unchanged). In
`src/cal/cal.npk`'s header and `civil_date`'s comment, the sentences carrying
C-8b's limit (*"a CONSUMER can still write the struct literal"*) are re-written
to the measured state: refused, `TYPE-079`, since `c3bdae2`. In
`../../specs/CALENDAR.md`, **add Rule C-8c** after C-8b (C-8b's text stays, and
C-8c says it narrows it):

> **Rule C-8c (the PD-9 decision, cycle 0.1.0c) — the fields are sealed, so C-8
> holds of the TYPE again, for every module but `cal`.** At compiler `c3bdae2`
> a field may be `sealed` (D-313): read anywhere, written only by the module that
> declares it. Every field of `CivilDate` and `CivilTime` is sealed. Measured:
> a consumer's struct literal is `NITPICK-TYPE-079`, one per field named; a
> field assignment is `TYPE-079`; an uninitialised declaration cannot be read
> (`NITPICK-ASSIGN-001`, D-010). **What remains outside the guarantee is `wild`
> storage reinterpreted by `=>!`**, the language's opt-out for every checked
> property. C-8b's second bullet — write downstream functions total where they
> can be — stands, because it costs nothing and the opt-out exists.

And in the same document C-8's pointer to C-8b gains *"and C-8c"*.
`src/lib.npk`'s constructor comment says the same in one sentence.

**Until this step lands, the harness's `check_specs_current` reports `C-8c` as
cited and undeclared** — from this plan's own files and the cycle README, which
name the rule before it exists. That check reports and never fails, and this
step is what resolves it: after it, its headline reads `0 unresolved` again.
Check that it does.

**Ends with:** the sweep shows `probe15` refused `TYPE-079` ×3 and nothing
else new.

### Step 4 — the arms

Run `0.1.0b.md` step 6's script with `CODES`/`ORDER` extended by
`"LimitViolated": 109`. **Expect `edited 9 roots`** (§1's list) and the same
three `LEFT ALONE`.

### Step 5 — the generator learns `LimitViolated` (draft PD-11)

`harness/arms.py`: a fifth rule in `_arith_arms` — **`limit<` in a module's
code text arms `LimitViolated`** (`LIMIT_ARM`), with the comment that it is an
over-approximation in one direction only: a `Rules` declared and never used
would make it OVERSTATE, which `check_failsafe_arms` reports in both
directions, so it cannot drift silently. The umbrella row is its calibration:
without the rule it is short by exactly `LimitViolated`. `SAFETY.md` S-4b gains
the `limit` → `LimitViolated` row beside step 7 of 0.1.0b's `decreases` →
`DecreasesViolated`.

**Expect `check_failsafe_arms`:** 7 rows, `src/lib.npk` **13**
(`DecreasesViolated, DivByZero, DivOverflow, HeapBadRequest, HeapOom,
IntOverflow, LimitViolated, MachineFault, OutOfBounds, StackExhausted,
Unreachable, WildLeak, cal.ETimeValue`), the other six unchanged.

### Step 6 — `probe15` flips, and five probes pin the seal

**`probe15_civil_literal_bypass.npk`:** its first line becomes
`// expect-error: NITPICK-TYPE-079`, and its header is re-written the way
`tests/probe/defect/view_escape/`'s landed cases were: a **WAS** paragraph
(the bypass compiled, linked and ran at `aaffb87`, exit 0, `month == 99` — keep
the measured text) and a **NOW** paragraph (refused at `c3bdae2`, `TYPE-079`
×3; this file is the regression test of the seal). Its body stays exactly as it
is — the refusal of that body is what it asserts.

**Five new probes**, each exactly as below; each passed in the dry run as stated.
The `failsafe` is deliberately a SUPERSET of what REACH asks (constraint 3 of
S-6b: a superset compiles), so a later change in the compiler's phase order
cannot add a stray `REACH-002` to a refusal probe:

```nitpick
// the FAILSAFE block every probe below ends with; the two that import `cal`
// add `(cal.ETimeValue) { exit 80i32; },` as their first arm
func:failsafe = int32(Error:e) {
    pick (e) {
        (HeapBadRequest)    { exit 91i32; },
        (HeapOom)           { exit 92i32; },
        (IntOverflow)       { exit 93i32; },
        (OutOfBounds)       { exit 94i32; },
        (Unreachable)       { exit 95i32; },
        (WildLeak)          { exit 96i32; },
        (DivByZero)         { exit 97i32; },
        (DivOverflow)       { exit 98i32; },
        (StackExhausted)    { exit 106i32; },
        (MachineFault)      { exit 107i32; },
        (DecreasesViolated) { exit 108i32; },
        (LimitViolated)     { exit 109i32; },
        (*)                 { exit 99i32; }
    }
    exit 9i32;
};
```

| File | Marker | `main`'s body (after `mod:<name>;` and the `use` line) | Measured |
|---|---|---|---|
| `tests/probe/probe16_vec_count_write_refused.npk` | `// expect-error: NITPICK-TYPE-079` | `use "../../src/core/vec.npk".*;` — `Vec<int64>:v = raw vec_init::<int64>(4i64); v.count = 3i64; drop vec_free(@v); exit 0i32;` | `TYPE-079` ×1 |
| `tests/probe/probe16b_vec_items_read_refused.npk` | `// expect-error: NITPICK-TYPE-080` | `use "../../src/core/vec.npk".*;` — `Vec<int64>:v = raw vec_init::<int64>(4i64); drop vec_push(@v, 7i64); int64:x = v.items[0i64]; drop vec_free(@v); if (x != 7i64) { exit 3i32; } exit 0i32;` | `TYPE-080` ×1 |
| `tests/probe/probe16c_bytes_len_write_refused.npk` | `// expect-error: NITPICK-TYPE-079` | `use "../../src/core/bytes.npk".*;` — `Bytes:b = raw bytes_init(4i64); b.len = 2i64; exit 0i32;` | `TYPE-079` ×1 |
| `tests/probe/probe16d_civil_field_write_refused.npk` | `// expect-error: NITPICK-TYPE-079` | `use "../../src/cal/cal.npk".*;` — `CivilDate:a = civil_date(2026i64, 9i64, 25i64) ?! ETimeValue; a.month = 13u8; exit 0i32;` | `TYPE-079` ×1 |
| `tests/probe/probe16e_sealed_reads.npk` | `// expect-exit: 0` | all three `use` lines — reads `v.count`, `v.cap`, `b.len`, `b.body.cap`, `d.month` after one push each and a `civil_date`, `exit 1Nx` on any wrong value, `vec_free`, then `exit 0i32;` | runs, exit 0 |

Each file's header says, in a paragraph, which property it pins and why the
refusal is the assertion — the header style of `probe10b`/`probe10c`. `16e`
is the positive twin: without it, the four refusals would also pass a compiler
that refused every access. **The file names begin with `probe`** — a name that
begins with a digit is refused by D-248.

### Step 7 — `check_civil_literal` retires (draft PD-10)

TYPE-079 now enforces, for every module but `cal` — consumers **and** the rest
of `src/` — exactly the property `check_civil_literal` checked over `src/`.
Remove: the function and its section comment in `harness/checks.py` (around
lines 663–736), its entry in the registry list (around line 1195), its two
planted violations and two controls in `harness/selfcheck.py` (around lines
690–706), its row in `../../specs/TESTING.md` §2, and the live sentences naming
it: `../../specs/CALENDAR.md:171` (C-8b's first bullet — amended by C-8c, the
sentence stays as history with *"retired by PD-10's decision at 0.1.0c"* after
it), `src/lib.npk:164`, `probe15`'s header (step 6 rewrites it), and
`CLAUDE.md:72`'s count of tree checks — **re-derive that count from the run's
`[5/9]` line**, because it was already stale before this subcycle (it says
*"thirteen today"* and predates `check_civil_literal`). **History stays as
written**: `../0.1.0.md`, this cycle's README's 0.1.0 items,
`../../DECISIONS.md` TM-148. Search:
`git grep -n 'check_civil_literal' -- ':!meta/roadmap/done' ':!meta/roadmap/0.1/0.1.0b.md' ':!meta/roadmap/0.1/0.1.0c.md' ':!meta/roadmap/0.1/0.1.1.md'`
— **23 lines in 9 files at planning**; state the denominator (*N lines, M
edited, K history*).

> **Moved at 0.1.0b's close (2026-09-25), because that subcycle added prose
> above them:** `CLAUDE.md`'s *"thirteen today"* is at line 96, not 72;
> `src/lib.npk`'s sentence at 168, not 164; `check_civil_literal`'s section in
> `harness/checks.py` runs from line 668 (the function at 703) to 741, and its
> registry entry is at 1204; the plants in `selfcheck.py` are at about
> 690–712. The search above re-derives every one — work from its output, not
> from these numbers either.

**Ends with:** the self-check's tree-check line reads **16** planted violations
and 16 controls (it was 18) — the count is derived from the code, so read it,
do not type it.

### Step 8 — the documents, the denominators, the run, the commit

- `../../specs/SAFETY.md` S-4: the umbrella row **13**, with the step that made
  it 13; S-17b and S-17c: `items` is hidden, so the bare-pointer index is
  unreachable outside `vec` by the compiler's rule, and the accessor-pair rule
  is now the language's for `Vec` (it stays the library's for `Bytes`' `ptr`).
- `../../specs/BUILD.md` B-12: `Vec<T>`'s shape is `{ hidden wild T->:items;
  sealed limit<ListLen> int64:count; sealed limit<ListLen> int64:cap; }` and
  `Bytes`' is `{ sealed buffer:body; sealed limit<ListLen> int64:len; }`.
- `CLAUDE.md`'s "THREE things to know before touching `src/core/`" gain the
  seal in one line each.
- **The denominators** move, and `check_denominators` says where: at planning,
  `npk_total` 83 → **88** (including `../../specs/TESTING.md:20`'s tag, which
  sits in a table), `suite_roots` 46 → **51**, `probe_dir` 34 → **39**,
  `probe_error` 8 → **13**, `tests_error` 19 → **24**; `probe_exit` stays **26**
  (probe15 leaves it, probe16e joins it). Update from the check's `measured:`
  line only.
- **Expect `GREEN -- 75 unit(s), 0 failures; 5 pending`** at `c3bdae2`.
- `check_refs` clean before and after staging (five files added). One commit,
  `cycle 0.1.0c: the access properties — Vec, Bytes, CivilDate and CivilTime
  sealed, ListLen on the lengths`. Push; CI green on it, read from its log;
  the run id in the execution record (a second commit, as 0.1.0b's commit 3).

---

## 3. What will bite

- **The qualifier order** (§1). If `PARSE-001 expected a type` appears at a
  field, the `limit<…>` is in front of `sealed`.
- **`body`, not `buf`.** The board's item 13 was written from the sibling
  library's field names.
- **`hidden` refuses a struct literal naming the field outside the module** —
  `Vec{ items: … }` exists only in `vec_init`, inside `vec`, so nothing here
  trips it; a test that builds a `Vec` by literal would.
- **A limited field has no address** (`TYPE-063`). `@v.count` anywhere,
  including a `$$m` claim or a pointer-receiver call on the field, refuses.
- **The seal changes nothing at run time for correct code**, and the
  `LimitViolated` check can never fire from outside the module (nothing
  outside can write the field). Do not write a test that tries to make it fire
  from a consumer; it cannot, and that is the point.

---

## 4. Acceptance

- [ ] the four declarations exactly as §1, in the order that parses
- [ ] `LimitViolated` (109) named by exactly the nine roots that REACH asks,
      and nowhere else
- [ ] `check_failsafe_arms` generates the umbrella's **13** with `LIMIT_ARM`,
      both directions
- [ ] `probe15` asserts `TYPE-079` with its WAS/NOW header; `probe16`,
      `probe16b`, `probe16c`, `probe16d` refused with exactly their one code;
      `probe16e` runs, exit 0
- [ ] `CALENDAR.md` C-8c added; C-8 points at it; C-8b's text unchanged
- [ ] `check_civil_literal` retired, its search's denominator recorded; the
      self-check's planted count read from the run (16)
- [ ] `GREEN -- 75 unit(s), 0 failures; 5 pending` at `c3bdae2`; CI green
- [ ] PD-8…PD-11 recorded as the next four TM numbers, TM-148 marked
      `SUPERSEDED IN PART by` PD-9's number

---

## 5. Decisions drafted at planning

Recorded by the worker in `../../DECISIONS.md` as the next free TM numbers,
in this order, in the commit that makes the change.

**PD-8 — `Vec<T>` and `Bytes` carry the three properties.** `items` hidden;
`count` and `cap` sealed under the prelude's `ListLen`; `Bytes`' `body` sealed
and `len` sealed under `ListLen` (D-308, D-313, D-314; the board's worklist
item 13, decided on safety by the compiler seat with the author). Every
`Vec`/`Bytes` consumer owes `LimitViolated` (the umbrella 12 → 13, measured).
*Alternatives:* a `VecLen` rule of our own — declined: the prelude's `ListLen`
is the bound the compiler's own length facts use, and a second copy is a second
place to disagree; `$ > 0` — declined: `TYPE-077`, a rule must admit the vacant
value 0; `count`/`cap` hidden — declined: they are read across modules and by
tests; `items` merely sealed — declined: a read of the bare pointer outside
`vec` IS the unchecked index TM-108 closed by convention, and `hidden` makes it
a compile error.

**PD-9 — `CivilDate` and `CivilTime` seal every field; `CALENDAR.md` gains C-8c.**
Supersedes TM-148 in part: its measurement (a consumer's literal compiled at
`aaffb87`) stays true of that pin; its premise (*"the language has no private
field"*) is false at `c3bdae2`. *Alternatives:* `hidden` — declined: the fields
are read everywhere, and `civil_order.npk` reads every one; not sealing and
keeping C-8b — declined: it leaves unenforced a guarantee the language can now
enforce, which is the house-rule-over-compiler shape this ecosystem refuses;
a `limit` on `month`/`day` — declined: `TYPE-077` requires the rule to admit 0,
so `$ >= 1` cannot be written, and a rule that half-states C-4 is P-7's warning.

**PD-10 — `check_civil_literal` retires.** Its property is `TYPE-079`'s for
every module but `cal`. *Alternative:* keep it as a belt — declined: a tree
check duplicating a compiler rule is a second statement of the rule that can be
wrong on its own, and the probe that pins the rule (`probe15`, `probe16d`) is
the stronger instrument because it runs the compiler.

**PD-11 — `limit<` in a module's code text arms `LimitViolated`** in the S-6
generator, calibrated by the umbrella row. *Alternative:* no rule, and the
umbrella row excluded again — declined for PD-7's reasons.

---

## Execution record

*(Appended as the work is done. The REPORT block is its last entry.)*
