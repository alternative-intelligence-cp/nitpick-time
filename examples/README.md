# `examples/`

Runnable demonstrations, **built and run by the harness**, so a broken example
is a red run. The dogfood consumers from cycle 0.7 live here too — a
`date`-equivalent CLI and a small scheduler, which between them exercise
formatting breadth and DST edges.
