# 0.0.0 — the language probes — RUNNING (since 2026-09-03, s2)

> **Scope:** eleven small Nitpick programs that ask the compiler whether the
> shapes `meta/specs/` depends on are spellable, compilable and correct. No
> library code is written in this subcycle. What it produces is **a verdict per
> probe** and, where a verdict is negative, an amended specification and a
> recorded decision.
>
> **Why first.** `meta/specs/` was written against the compiler's
> documentation, and the compiler project's most expensive finding is that *a
> construct that parses is not a construct that works* — most of its cycle 0.4
> was repair, and every repair dated to the cycle that had parsed the
> construct. Two of this library's load-bearing facts (a derived `Ord`
> following declaration order, and signed division truncating toward zero) are
> facts about the *compiler* that `ntime` would otherwise discover by getting
> a wrong answer for the year −1.

---

## 1. Where you are

- Nothing is built. `src/` holds placeholder directories.
- The compiler is at `../../nitpick`, cycle 1.5 (verification), self-hosting
  declared under D-202. **Do not build it from here and do not read its `build/`**: the
  artifact these probes use is the pinned toolchain the board names
  (`../BOARD.md`, W-18) — `$NPKC` and `$NPKRT`, supplied by the orchestrator or
  set by hand from `../.internal/toolchain/<commit>/`.
- There is no `ntime` harness yet. **Probes are run by hand**, one command
  each, and that is deliberate: the harness is 0.0.2 and building it against an
  unverified set of language assumptions is the mistake this subcycle exists to
  avoid.
- The probes live in `tests/probe/` and **stay in the tree permanently**. They
  are a regression suite for the language facts this library depends on, and
  0.0.2 picks them up as ordinary `program`-stage entries.

---

## 2. The command

For each probe, from the `ntime` tree root, with `$NPKC` and `$NPKRT` from the pinned
toolchain (the board names the commit, W-18):

```
$NPKC tests/probe/probeNN_name.npk -o /tmp/p.ll \
  && llc -O0 -filetype=obj -relocation-model=static /tmp/p.ll -o /tmp/p.o \
  && ld.lld -static /tmp/p.o $NPKRT -o /tmp/p \
  && /tmp/p ; echo "exit $?"
```

A probe **expected to be refused** stops at the first command, and its verdict
is the diagnostic code and span, recorded verbatim.

**Every probe exits 0 on success and a distinct positive code per assertion it
fails**, in the compiler's house style, so a failure names itself. `exit 0`
additionally asserts, through D-151, that **no `wild` allocation is live** —
which is all D-151 watches. A **managed** body is outside it, so a probe over an
owning container asserts its memory as well (§6, and TM-106).

---

## 3. Decisions taken at planning

- **P-0 — a probe's filename may not begin with a digit**, so probes are named
  `probeNN_topic.npk` and the `mod:` line matches. A file's module name must
  equal its basename, and no identifier may begin with a decimal digit — D-147
  gives that opening to numeric literals alone, so `mod:01_argv;` is refused
  `NITPICK-LEX-003` with `NITPICK-RESOLVE-005` behind it. *Measured the hard
  way in `nitpick-posix`'s probe 02, 2026-09-03, after every plan in the
  ecosystem had written `01_name.npk`.*
- **P-1 — a probe is a program, never a library file.** It has its own `main`
  and `failsafe` and imports nothing from `src/`.
- **P-2 — a refused probe is a *result*, not a failure of the subcycle.** One
  (11's negative twin) is expected to be refused, and its value is the exact
  diagnostic. The subcycle fails only if a verdict is *undecided* — a compiler
  crash, a hang, or output nobody can read.
- **P-3 — the verdict is recorded before the next probe is written.** A batch
  written and then run together is a batch where the third one's answer would
  have changed the fourth one's design.
- **P-4 — a negative verdict amends `meta/specs/` in the same commit.** Not a
  note to self. The specification is the authority (TM-002), and an authority
  known to be wrong and not corrected is worse than none.
- **P-5 — the probes stay.** They become `tests/probe/`'s permanent suite, so a
  compiler change that breaks one of these facts is caught by `ntime`'s own
  harness rather than by a wrong date in cycle 0.6.
- **P-6 — probes that pin a *fact about the compiler* assert the expected
  answer**, rather than printing what they found. 01 and 07 are of that kind:
  the specification already states what the answer is, so the probe's job is to
  make a change to that answer a red run.

---

## 4. The probes

Each entry: **what it asks**, **why it matters**, and **what a negative verdict
costs**.

### 01 — a derived `Ord` follows declaration order

**Asks:** does `#[derive(Ord, Eq)]` on `{ int64:a; uint32:b; }` compare `a`
first and `b` second?

```nitpick
#[derive(Ord, Eq)]
struct:Pair = { int64:a; uint32:b; };
// assert: Pair{a:1, b:0}  <  Pair{a:1, b:9}
// assert: Pair{a:2, b:0}  >  Pair{a:1, b:9}     // a dominates
```

**Matters because** `Timestamp` is `{ int64:secs; uint32:nanos }` and TM-011
makes the field order *semantic* on exactly this basis. Every comparison of
two instants in the library rests on it.

**A negative verdict costs** the derived comparison: `Timestamp` grows a
hand-written `timestamp_cmp`, `TIME_MODEL.md` M-6 is rewritten, and every
other value type in the library is re-examined for the same assumption.

### 02 — `int128` arithmetic and a narrowing `=>!`

**Asks:** `int128` add and compare; `=>!` down to `int64` for a value that
fits; and the **trap** for one that does not.

**Matters because** `SPAN_MODEL.md` §5 names exactly three `int128` sites and
`VERIFICATION.md` P-5 calls the narrowing there "the single most valuable proof
in the library". If the narrowing silently truncates rather than trapping, the
proof is load-bearing rather than a belt, and the code must check explicitly
before every one.

**Also measures** whether `int128` division is needed anywhere — it is not, in
the current design, and the probe records that it was checked.

### 03 — a `timespec` in a `buffer`, through `sys`

**Asks:** `buffer_new(16)`, `#ptr_add<int64>` writes and reads,
`clock_gettime(CLOCK_REALTIME=0)` as `sys(228, 0, ptr)`, both fields read back
and sane; `#size_of` and the offsets asserted.

**Matters because** every function in `src/host/` is this shape
(`HOST.md` §2), and the 16-byte two-`int64` layout is the one fact about the
kernel this library assumes.

**A negative verdict costs** `src/host/`'s implementation strategy, and
`HOST.md` §2 is rewritten against whatever does work.

### 04 — a large `fixed` module-state table

**Asks:** a module-level `pub fixed` array of ~30 000 small structs. Does it
compile? Is it emitted as read-only data? Does the program's startup do any
work for it? Is `#size_of` of the row what the arithmetic assumed?

Read the emitted IR, not just the exit code: the question is whether the array
is a `@global constant` or something a startup routine fills in.

**Matters because** the compiled tzdb (TM-007) is exactly this, at roughly
27 000 transition rows, and `ZONE_MODEL.md` §3's whole cost model assumes it is
static data with no startup cost.

**A negative verdict is the most expensive one in the set.** If a large `fixed`
table is initialised at run time, TM-007's representation changes — the
fallback would be a `buffer` built from a byte blob at first use, which
reintroduces initialisation order and a mutable global, and `ZONE_MODEL.md` §3
would need rewriting before cycle 0.5. **Run this probe early**, and pair it
with 0.0.5's size spike.

### 05 — a payload enum in a `pick` and in a `Vec`

**Asks:** a tagged enum with payloads, destructured in a `pick`, stored in and
read back from a `Vec`, and `#[derive(Eq, Debug)]` on it.

**Matters because** `FmtPart`, `ValueFault`, `ZoneFault`, `ParseExpect`,
`Weekday` and `Month` are all this shape, and `Layout` stores `FmtPart` in a
`Vec`.

### 06 — a generic `Vec<T>` with `move` and a mutating receiver

**Asks:** `move T:v` into a generic container at both a scalar `T` and an
owning one; an inherent `impl:<T>:Vec<T>` with a `Vec<T>->:self` receiver that
mutates; and the free-function form beside it, so the fallback is measured in
the same probe.

**Matters because** `TM-005`'s `Vec<T>` is the library's one container.

### 07 — signed division and modulus at negative operands

**Asks, and asserts:** that `/` truncates toward zero and `%` takes the sign of
the dividend.

```nitpick
// assert: (-7i64) / 2i64  == -3i64      (truncating, not floor's -4)
// assert: (-7i64) % 2i64  == -1i64      (sign of the dividend)
// assert:   7i64  / (-2i64) == -3i64
// assert:   7i64  % (-2i64) ==  1i64
```

**Matters because** Hinnant's `days_from_civil` (TM-016) is written for C
semantics and carries the correction `(y >= 0 ? y : y - 399) / 400` for exactly
this reason. The compiler's `TYPE_REFERENCE.md` §28 says signed `/` is `sdiv`
and `%` is `srem`, which is C semantics, so **the expected verdict is that all
four assertions hold**.

**The probe exists anyway** because if this were ever otherwise the calendar
would be wrong for negative years *only*, and cycle 0.1's exhaustive sweep
would be the first thing to notice — a long way from the cause, and only if
somebody had thought to include negative years in the sweep.

### 08 — `readlink` through `sys`

**Asks:** `readlink("/etc/localtime", buf, 4096)` through `sys(89, …)`, with
the returned length as the authority and the buffer **not** NUL-terminated by
the kernel. Assert the length, assert that byte `[len]` is untouched, and
handle the not-a-symlink case (`EINVAL`).

**Matters because** `HOST.md` H-15 names four facts about this call that a
careless implementation gets wrong, and `host_system_zone`'s second discovery
step is entirely this.

**Needs a machine where `/etc/localtime` is a symlink**, which is the usual
case; where it is not, the probe asserts the `EINVAL` path instead and records
which arm it took.

### 09 — `environ()` and a `KEY=VALUE` split

**Asks:** `environ()` read, an entry located by prefix, the value taken as a
slice, and the borrow rules exercised — passed down (legal), returned
(expected refused), stored (expected refused).

**Matters because** `host_system_zone`'s first discovery step reads `$TZ`, and
because the environment block is the kernel's and never freed (the builtin's
own documentation says so), which is a lifetime a probe should confirm rather
than assume.

### 10 — `string_bytes` / `string_from_bytes` at the borrow edges

**Asks:** `string_bytes(s)` into a scanner, the slice's `.len` and `.ptr`, a
`string_from_bytes` view built in `wild` context, and the four edges the escape
analysis governs.

**Matters because** every parser in `src/fmt/` takes a `uint8[]` and every
formatter produces a `string` through `Bytes`, and D-186 made `string_slice`
an owned copy for exactly the use-after-free this probe maps.

### 11 — the `failsafe` arm contract, positive and negative

**Asks two programs:**

- one importing a module that declares a single `error:`, whose `failsafe`
  names exactly the arms REACH-002 requires — **must compile**;
- its twin, omitting one arm — **must be refused**, and the diagnostic recorded
  verbatim.

**Matters because** TM-017's whole budget rests on the claim that a missing arm
is a *compile error*, and `SAFETY.md` §2's table is a promise to consumers.
`check_failsafe_arms` (0.0.3) is built on the same mechanism, and this probe is
what proves the mechanism is real before a check is written against it.

**A negative verdict on the negative twin** — that is, if the omission
*compiles* — would mean the error budget is a convention rather than a
constraint, and `SAFETY.md` §2, `COMPAT.md` §6 and TM-013's major-version rule
all need rewriting.

---

## 5. The steps

Each step is one commit.

**Step 1 — the expensive two: 04, then 01.** These are the probes whose
negative verdicts change a *representation* rather than an API. 04 decides how
the zone table is spelled; 01 decides whether `Timestamp` can derive its
comparison. Run them, record verdicts, and if either is negative **stop and
re-plan** before writing another probe.

**Step 2 — the arithmetic facts: 07, 02.** These decide whether the calendar
algorithms can be used as published and whether the `int128` narrowings need a
check before them.

**Step 3 — the host shapes: 03, 08, 09.**

**Step 4 — the remainder: 05, 06, 10, 11.**

**Step 5 — the record and the amendments.** Every verdict written into §7;
every consequence written into `meta/specs/` and `meta/DECISIONS.md`;
`tests/probe/README.md` written, saying what the directory is for and that a
probe is never deleted.

---

## 6. What the executor should know

- **The reserved words bite immediately.** A probe about the clock wants a
  local called `buffer`; one about division wants `mod`; one about ranges wants
  `end` and `limit`. `specs/BUILD.md` §7 has the list and the substitutes.
- **`discard(x);` takes parentheses. `defer { … }` takes no trailing
  semicolon.** Declarations end `};`; control-flow blocks do not.
- **Adjacent string literals do not concatenate.**
- **A file's `mod:` name must equal its basename**, or the loader reports
  `NITPICK-RESOLVE-005` at line 1 and says nothing about the name.
- **`raw` is licensed only on a `never fails` callee** (D-163).
- **A `failsafe` must name every reachable error** (REACH-002), including the
  system ones the program's own machinery can raise. Copy the arm set from
  `../../nitpick/tests/backend/programs/proc_tool.npk` and prune.
- **Exit 0 means "no `wild` allocation is live"** (D-151) — that, and nothing
  more. Every probe exits 0 on success on purpose, and the assertion it buys is
  precisely the one `nitpick-sockets` states about its own control buffer: a
  path that *takes no `wild` bytes* **cannot trip D-151 on any exit path**.
  It is a statement about coverage, not a proof of cleanliness.
  **A managed body is outside it**, and this subcycle measured how far outside:
  probe 06's `Vec<string>` freed its block, orphaned its elements, **retained
  125 MiB over 2 000 000 elements and exited 0** (TM-106, `SAFETY.md` S-18b).
  So a probe over an owning container asserts its memory, and an exit code is
  not that assertion. Until the instrument exists the assertion is a `ulimit -v`
  cap — probe 06's own form, where the orphaning half gives `HeapOom` (exit 92)
  and the correct half exit 0. **The real gate arrives with the compiler's
  1.5.1b step 0**, `NPK_HEAP_STATS`: an allocator-level report of `allocated`,
  `peak_live` and `count` for *managed* memory, plus a `cost` harness stage.
  Run on this repository's own two container probes it reported **`peak_live`
  41 321 bytes against 400 101 320** — the pair that both exited 0. At the
  re-pin, "assert the memory" becomes a `peak_live` bound per test, and
  cycle 0.0.3 is where the stage that carries it is written.
- **`llc` and `ld.lld` must be the pinned 20.1.2.** `llvm-config --version`.
- **Read the emitted IR for probe 04.** The exit code cannot tell you whether a
  table is static data or filled at startup; `-o /tmp/p.ll` and reading it can.

---

## 7. Verdicts

*(One row per probe, filled as they run. `ACCEPTED` / `REFUSED <code> at
<span>` / `UNDECIDED <why>`, plus the measured facts where a probe measures
something.)*

| # | Probe | Verdict | Measured | Consequence |
|---|---|---|---|---|
| 01 | derived `Ord` follows declaration order | **ACCEPTED** — exit 0 | declaration order decides, proven against a reversed twin | none. TM-011, S-14 and M-6 stand as written |
| 02 | `int128` + narrowing `=>!` that FITS | **ACCEPTED** — exit 0 | add, compare and shift exact past `int64` in both signs; `=>!` round-trips both of `int64`'s bounds; a widened `int64` sign-extends rather than bit-copies; `ns_add_checked` answers correctly at both ends of the range and at each boundary from both sides. No `int128` **division** is needed anywhere in the design, and the probe records that it was checked | none for 02's own asks. **Its twins carry the negative verdict**, below |
| 02b | `=>!` at a value that does NOT fit | **ACCEPTED — and the verdict is NEGATIVE** — exit 0 | it **truncates, in silence, with no trap**. Four shapes pinned: high bits discarded; `I64_MAX + 1` landing on `I64_MIN`; **a positive `int128` narrowing to a negative `int64`**; and a negative one narrowing to a small positive | **TM-105**, with `SAFETY.md` S-15b and `SPAN_MODEL.md` N-20b. The range check before every narrowing is ordinary library code that must be written |
| 02c | the checked `=>` at the same narrowing | **REFUSED `NITPICK-TYPE-009` at 33:27** — the expected result (`expect-error`), `npkc` exit 1 in 0.03 s | verbatim: ``NITPICK-TYPE-009 tests/probe/probe02c_narrow_refused.npk:33:27: `int128` does not fit in `int64`, so this conversion can lose information: the target is narrower than the source. Write `=>!` to accept that.`` | with 02b: **there is no checked narrowing in this language.** A narrowing is refused where it is written, or unchecked when it runs |
| 02d | can `int64`'s MINIMUM be spelled as a literal? | **REFUSED `NITPICK-LEX-004` + `NITPICK-PARSE-002` at 51:18** — `npkc` exit 1 in 0.02 s. **Unplanned**; written because correcting `VERIFICATION.md` P-5 required the bound pair | verbatim: ``NITPICK-LEX-004 …:51:18: this literal is outside the 64-bit literal envelope (D-148); a type's outermost values are constructed arithmetically, not spelled``, then ``NITPICK-PARSE-002 …:51:18: expected an expression``. The **maximum** is spellable; only the minimum's magnitude is one too large | `VERIFICATION.md` P-5's sample rewritten. Two codes, so D-237's exact-set rule makes a harness entry expect both |
| 03 | `timespec` through `sys` | **ACCEPTED** — exit 0 | `#size_of<int64>` = 8 and `buffer_new(16)` gives `len` = `cap` = 16, so H-4's 2 × 8 arithmetic holds; **field order proven by non-overlapping ranges**, slot 0 in [1.6e9, 4.1e9] and slot 1 < 1e9; two readings advance and do not jump; `CLOCK_BOOTTIME` = 7 answers on a different timeline (uptime ≪ wall clock); **a bad clock id is an errno in the `Result`, not a trap**, and `?\|` defaults without trapping | `HOST.md` §2 and H-4/H-7/H-8 stand as written; `SAFETY.md` S-5's no-arm claim for `host_now_utc` is measured, not assumed |
| 04 | large `fixed` table | **ACCEPTED** — exit 0 | row = 16 B and array = 480 000 B = 30 000 × 16, re-checkable by inspection. Emission shape — `constant` not `global`, `.rodata`, 0 `global_ctors` — **observed once, see the note below**. **281 s and 30.9 GiB to compile** | language answer positive, so TM-007 and Z-7 stand. The **cost** is O-N4 and it stopped the subcycle |
| 04b | the same emission shape at 300 rows | **ACCEPTED** — exit 0, 0.16 s, 28 MiB | `constant [300 x …]`; 0 `llvm.global_ctors`, 0 `llvm.global_dtors`, 0 `appending` globals; `.rodata` with flags `A` and no `W`; linked into a LOAD segment mapped `R`; symbol size 4 800 B = 300 × 16; no `.init_array` in the executable | S-19 and Z-7 evidenced, verbatim and cheaply, in [`../../../tests/probe/probe04b_emission_shape.txt`](../../../tests/probe/probe04b_emission_shape.txt) |
| 05 | payload enum in `pick` and `Vec` | **ACCEPTED** — exit 0 | the shape works: construction, a `pick` binding the payload, five values in and out of a `Vec<Part>` across two reallocations, `Clone` keeping the payload, and the tag readable as `=>! int32` in declaration order. `#size_of<Part>` = **8** — not the 6 a tag-plus-`uint16` suggests, because the payload aligns. Two costs pinned: a `pick` arm's variant must be **qualified** (`(Part.Year4)`; bare is `NITPICK-RESOLVE-002`), and an **owning** payload cannot be bound by a lending `pick` (`NITPICK-TYPE-046`; use `pick (move(v))`) — which does not bite today, `FmtPart.Literal(uint16)` being the only payload variant in the spec set | `FORMAT_MODEL.md` F-4 and `SAFETY.md` S-3 stand. **But see 05b** |
| 05b | `#[derive(Eq)]` on a payload enum | **REFUSED `NITPICK-TYPE-034`** in `<derived-1>` — `npkc` exit 1. **THE THIRD STOP** | verbatim: ``NITPICK-TYPE-034 <derived-1>:2:73: `Part` has no built-in `==`: derive or implement `Eq` and compare with `a.eq(b)``` — the derived `Eq` is being told to derive `Eq`, in a synthetic file. **And `#[derive(Ord)]` on the same declaration COMPILES and its `cmp` ignores the payload: `Literal(7).cmp(Literal(9))` is `Equal`.** `Hash` hashes the tag alone; `Clone` is correct. The other five derives compile | **O-N10**, reproduced in `defect/derive_payload_enum/`. Not blocking — one type is exposed and no rule needs either derive on it — but the cycle that builds `src/fmt/` must know that `#[derive(Ord)] enum:FmtPart` compiles and is wrong |
| 06 | generic `Vec<T>` with `move` | **ACCEPTED** — exit 0 | B-12's shape compiles and works at a scalar `T` and an owning one: `move T:x` consumes, `ralloc` doubling preserves order and string bodies across two growths, `#size_of<string>` = **24** (not 16 — a `string` carries a third word). Call syntax pinned: an explicit generic argument is a **turbofish**, `vec_init::<int64>(2i64)`; `vec_init<int64>(…)` is `NITPICK-PARSE-002` | **TM-106 and `SAFETY.md` S-18b** — freeing the block does not drop the elements and `exit 0` does not notice: 125 MiB retained over 2 M elements at exit 0, `HeapOom` under a 64 MiB cap, 0 KiB when each element is moved out first. Three of 0.0.4's API entries silently carry that obligation |
| 07 | signed `/` and `%` at negatives | **ACCEPTED** — exit 0 | all four hold: `/` truncates toward zero and `%` takes the **dividend's** sign, in every sign combination. And the algorithm itself — Hinnant's `days_from_civil` transcribed as published — returns the reference value at 1970-01-01, 2000-03-01, years 1, 0 and −1, the era boundary at −400, and both ends of `CALENDAR.md`'s range; consecutive days step by exactly 1 across the sign change, where a wrong era would show as a 146 097-day jump | TM-016 stands: the published algorithm is used **as given**, and its `- 399` correction is required rather than redundant |
| 08 | `readlink` through `sys` | **ACCEPTED** — exit 0 | all four of H-15's facts: syscall **89**; a 4096-byte buffer; the kernel writes exactly `n` bytes and **NUL-terminates nothing** — the byte at `[n]` and at `[n+1]` are still the 0xAA sentinel and there is no NUL inside the result; and **truncation is silent** — the same link into an 8-byte buffer returns 8, with the bytes matching the real target's first eight and no error. `EINVAL` for a regular file and for an absent path, both as errors in the `Result` rather than traps. **Arm taken here: the symlink arm** — this machine's `/etc/localtime` is a symlink, and the tail after `zoneinfo/` was found, non-empty, NUL-free and correctly separated. The zone **name** is never compared against a literal | `HOST.md` H-13, H-14 and H-15 stand; H-15's "treat `n == bufsiz` as not-found" is now a measured rule rather than folklore |
| 09 | `environ()` and the borrow edges | **HELD** — not written | | the author's ruling on Q-5: O-N9 is BLOCKING, so the borrow-edge probes wait for the compiler's 1.5.1b rather than being written against a rule about to change |
| 10 | `string_bytes` borrow edges | **HELD** — not written | | the same ruling |
| 11 | `failsafe` arms, positive and negative | **ACCEPTED** — exit 0, and the negative twin **REFUSED `NITPICK-REACH-002`**, `npkc` exit 1 in 0.07 s | verbatim: ``NITPICK-REACH-002 tests/probe/probe11b_arm_omitted_refused.npk:60:5: `failsafe` does not name `probe11_arms_lib.EProbeZone`, which can reach it (D-179): add the arm — `(*)` counts for nothing here, so a new failure mode is always acknowledged before the program compiles again``. **A missing arm is a compile error**, so TM-017's budget is a constraint | **`SAFETY.md` §2, `COMPAT.md` §6 and TM-013 stand as written.** The positive half compiles and runs at exit 0 with nine arms |
| 11c | what an imported module's **arithmetic** costs | **ACCEPTED — and the verdict is NEGATIVE for `SAFETY.md` §2's table** — refused `NITPICK-REACH-002` ×4, `npkc` exit 1 | a module that declares **no error at all** but divides, indexes and adds costs an importing program `DivByZero`, `DivOverflow`, `IntOverflow` and `OutOfBounds` — measured against 11d's floor, whose `failsafe` this file copies character for character and which compiles | **TM-107**, with `SAFETY.md` **S-4b** and the three constraints added to **S-6**. `cal` divides and indexes, so every consumer pays this |
| 11d | the **unconditional floor** | **ACCEPTED** — exit 0, 0.10 s | `Unreachable`, `HeapOom`, `HeapBadRequest`, `WildLeak` and `(*)`, with nothing imported and nothing computed. The control that makes 11c a measurement | none; it is the baseline the other three are read against |
| 11e | is the arm owed by the import or the call? | **ACCEPTED** — refused `NITPICK-REACH-002`, `npkc` exit 1 | importing the raiser and calling only its **infallible** half is still refused. Module boundaries are the only granularity; avoiding the failing function buys nothing | **TM-107**, `SAFETY.md` **S-4c**. S-4's decomposition is doing more work than its table suggested |
| 11f | does a `pub error:` **declaration** cost an arm? | **ACCEPTED** — exit 0, 0.10 s | **no.** An identity declared and never raised arms nothing; the set is computed from `fail`/`?!`/`!!!` **sites** | **TM-107**, `SAFETY.md` **S-6** constraint 1: the generator counts sites, never declarations |
| — | **a program with `main` and NO `failsafe`** | **DEFECT — `npkc` exit 0**, `llc` exit 1. **THE FOURTH STOP** | a four-line program with neither import nor arithmetic is accepted at exit 0; `llc` refuses the IR with ``use of undefined value '@npk_failsafe'`` at a generated line, naming neither `failsafe`, D-013, nor the file. `reach_settle` returns early at `if (x.failsafe_decl == 0i32)`, so the **whole REACH-002 contract is discharged by deleting the handler** | **O-N11** *(provisional)*, reproduced with its control and its contrast in `defect/missing_failsafe/`. Blocks nothing here; constrains 0.0.3's harness, which must not read `npkc`'s exit code as "well-formed" |

**Probe 04's 30 000-row emission shape is a ONE-TIME OBSERVATION.** It was made
during the original run on 2026-09-03 and it is **not re-derivable without
paying 281 s and 30.9 GiB**, so nothing in this repository re-runs it and no
later session should treat it as a repeatable check. What *is* re-derivable, and
is now committed with its exit codes rather than narrated, is the same
declaration's emission shape at 300 rows —
`tests/probe/probe04b_emission_shape.npk` and its `.txt` transcript, 0.16 s. The
reason that substitution is sound is mechanical rather than statistical: the
emission form is chosen by the same lowering path whatever the element count,
and O-N4 is a cost defect and not a semantics one. The two claims that remain
probe 04's alone — 30 000 rows compile, and `#size_of` × 30 000 = 480 000 —
are checkable by inspection of a file already in the tree.

**Two language facts fell out of the stop and are recorded here because a later
probe would otherwise rediscover them.** Neither is a probe's verdict; both were
read off diagnostics while measuring O-N4.

- **`failsafe`'s `pick` over `Error` must carry `(*)`, always.** Omitting it is
  `NITPICK-PICK-003`: *"this selector's type has more values than a `pick` can
  list, so it needs a `(*)` arm to be exhaustive."* The wildcard does **not**
  discharge REACH-002 — the diagnostic for a missing named arm says so in as
  many words: *"`(*)` counts for nothing here, so a new failure mode is always
  acknowledged before the program compiles again."* So a conforming `failsafe`
  names every reachable identity **and** ends with `(*)`. This is probe 11's
  territory and probe 11 must still be written; what is settled early is only
  that `SAFETY.md` §2's promise to consumers is a real constraint and not a
  convention.
- **`npkc` accepts a root file whose `mod:` name differs from its basename**,
  and if a sibling file *does* carry that basename it silently compiles that one
  too, merges both into one module, and emits IR with two `define i32 @main` —
  **exiting 0** while producing LLVM the assembler then refuses. Met by accident
  while staging a probe under the wrong filename. It costs `ntime` nothing,
  because the house rule is already `mod:` = basename, so it is not O-N4's
  blocker and it is not worked around; it is raised alongside it.

---

## 8. Acceptance

- [ ] eleven probes exist under `tests/probe/`, each with a header comment
      naming what it asks and which specification rule depends on the answer
- [ ] every verdict recorded in §7, with the exact diagnostic where refused
- [ ] every negative verdict's consequence landed in `meta/specs/` and recorded
      in `meta/DECISIONS.md`, **in the same commit as the verdict**
- [ ] `tests/probe/README.md` written
- [ ] no probe deleted, ever — they are 0.0.2's first `program`-stage entries

---

## Execution record

### Step 1 — the expensive two: 04, then 01 (2026-09-03, s2)

Run in §5's order, and for §5's reason: **04 first**, because its negative
verdict was the one that would have changed a *representation*.

**Probe 04 — the large `fixed` table. Verdict ACCEPTED; the subcycle stopped
anyway.** Every *language* question §4 asked came back positive, so TM-007 and
Z-7 stand exactly as written: the 30 000-row table is emitted as a
`@global constant`, it lands in `.rodata`, there is no `llvm.global_ctors`
entry and the program does no startup work for it, `#size_of` of the row is
16 bytes, and the array is 480 000 bytes — precisely 30 000 × 16, so the
arithmetic `ZONE_MODEL.md` §3 rests on is right. The IR was read rather than
inferred from the exit code, as §6 required.

What is not acceptable is the **cost**: 281.35 seconds and 30.9 GiB of peak
resident memory to compile that one declaration. `ZONE_MODEL.md` §3 sizes the
real table at 26 838 transition rows, so this is not a synthetic worst case —
it is the shipping shape, measured. A 16 GiB machine cannot build `ntime` at
all; CI cannot; and because the table lives in the library, every *consumer*
pays it too.

**Chasing that number is where the subcycle went.** The measurement was
extended into a curve on three axes, all against the pinned toolchain, and the
growth is quadratic on each:

| axis | 1× | 2× | 4× | 8× |
|---|---|---|---|---|
| elements in one `fixed` array | 1 000: 0.41 s / 54 MiB | 2 000: 1.19 s / 140 MiB | 4 000: 4.19 s / 473 MiB | 8 000: 15.83 s / 1.73 GiB |
| statements in one function body | 1 000: 0.87 s / 134 MiB | 2 000: 2.27 s / 375 MiB | 4 000: 7.03 s / 1.27 GiB | — |
| bytes in one string literal | 60 k: 5.24 s / 25 MiB | 120 k: 22.72 s / 27 MiB | 240 k: 78.11 s / 32 MiB | 480 k: 308.12 s / 41 MiB |

A ratio near 4 per doubling in both columns is the signature. Two controls
placed the cost in the **declaration** and not in any use of it: a `main` that
never reads the table costs the same (4.36 s vs 4.19 s at 4 000 rows), and
4 000 *separate* module-level `fixed int64` bindings cost 0.61 s and 58 MiB —
so it is the size of **one** declaration that hurts, not the number of
constants. The string-literal axis is quadratic in time with memory flat, which
makes it a second pathology rather than the same one seen through the lexer; it
matters here because Z-7's fourth table is a name pool, which is exactly a large
string constant.

**The rows above are the generic recipe's**, not any committed file's — a
struct `R` with fields `a` and `b`. That distinction was implicit when the table
was written and it is now load-bearing; see the correction below.

The whole reproduction, with the commands and the generator recipes, is
[`../../../tests/probe/defect/README.md`](../../../tests/probe/defect/README.md);
the committed case `big_fixed_array_cost.npk` is the 4 000-row point, chosen so
a reader sees the defect in about six seconds rather than five minutes. It is
recorded as **O-N4** in
[`../../OPEN_QUESTIONS.md`](../../OPEN_QUESTIONS.md).

**A measurement discipline the first pass got wrong, recorded because it nearly
produced a false conclusion.** The first attempt at these curves logged several
"fast" configurations that were in fact compile *failures* stopping early — and
they made the growth look linear. Every row above is paired with `npkc` exit 0.
A timing without an exit code is not a measurement.

**Probe 01 — a derived `Ord` follows declaration order. Verdict ACCEPTED**,
exit 0, and proven against a reversed twin so that the probe distinguishes
"declaration order" from "the order that happened to be right". TM-011,
`SAFETY.md` S-14 and `TIME_MODEL.md` M-6 stand as written, and `Timestamp` keeps
its derived comparison. §5's gate — *if either is negative, stop and re-plan* —
did not fire on the language; it fired on 04's cost.

### The stop (W-11)

Probes 02, 03, 05–11 were **not** written or run. The stream stopped at O-N4
rather than proceed, and the reason is worth stating plainly because the
alternative was cheap and available: shrinking the table, splitting it across
modules, or shipping it as a byte blob decoded at first use would each buy the
number back. Each is a workaround for a compiler bug, buried in library code,
that would outlive the bug — and TM-007 would then be a decision nobody could
read the reason for. `ntime` does none of them.

The same reasoning constrains a question already open: **O-Z1 (ship every zone,
or a subset?) must not be answered "a subset" on the strength of O-N4.** That
would be the identical workaround wearing a decision's clothes.

### Two language facts, and a second defect, met on the way

Neither of the first two is a probe's verdict; both were read off diagnostics
while measuring O-N4, and both are recorded in §7 so that a later probe does not
rediscover them. In short: a `failsafe`'s `pick` over `Error` must carry `(*)`
**and** name every reachable identity — the wildcard does not discharge
REACH-002, and the compiler says so in as many words. Probe 11 still has to be
written; what is settled early is only that `SAFETY.md` §2's promise is a real
constraint rather than a convention.

The second defect — `npkc` silently merging a sibling file when a root file's
`mod:` name differs from its basename, emitting two `define i32 @main` at exit
0 — is what made a one-second compile look like a three-hundred-second one, and
is why it was found at all. It costs `ntime` nothing, because the house rule is
already `mod:` = basename, so it blocks nothing and nothing here is shaped
around it. It is raised alongside O-N4.

### What it cost

Dominated by compilation, not by thought: the 30 000-row case alone is 281 s,
the 480 000-byte literal 308 s, and the two curves together about 12 minutes of
`npkc` wall time, at a peak of 30.9 GiB. That figure is itself the finding.

### This entry

This dispatch is the record only. Its predecessor in this subcycle was the same
session; its findings were received and accepted, and nothing here re-runs a
probe, changes a verdict or touches a measurement. What it fixes is that all of
the above was uncommitted and the execution record was empty — W-20 wants the
report in two places, and only one of them existed. Whether the nine unaffected
probes proceed against the current pin is the author's decision and is open.

```
REPORT nitpick-time 0.0.0
status: STOPPED
stream: s2
model: claude-opus-5
toolchain: 950bb1d
commits:
  - HEAD cycle 0.0.0: probes 01 and 04 accepted; stopped on O-N4
harness: not run: no harness until 0.0.2
  "$NPKC" tests/probe/probe01_derive_ord.npk -o /tmp/p.ll && llc -O0 -filetype=obj -relocation-model=static /tmp/p.ll -o /tmp/p.o && ld.lld -static /tmp/p.o "$NPKRT" -o /tmp/p && /tmp/p -> exit 0
  "$NPKC" tests/probe/probe04_big_fixed_table.npk -o /tmp/p.ll && llc -O0 -filetype=obj -relocation-model=static /tmp/p.ll -o /tmp/p.o && ld.lld -static /tmp/p.o "$NPKRT" -o /tmp/p && /tmp/p -> exit 0 (281.35 s, 30.9 GiB peak RSS at the npkc step; do not re-run casually)
  "$NPKC" tests/probe/defect/big_fixed_array_cost.npk -o /tmp/p.ll -> exit 0 (6.11 s, 593 796 KiB; the committed 4 000-row point of O-N4's curve. CORRECTED AT VERIFICATION: this line first read "4.19 s, 473 MiB", which is the generic recipe's 4 000-row point and not this file's)
check: clean
record: meta/roadmap/0.0/0.0.0.md — RUNNING (since 2026-09-03, s2)
next: n/a
findings-for-playbook:
  - A timing not paired with an exit code is not a measurement: a source file that fails to compile stops early and looks fast, which inverted the first pass at O-N4's curve.
  - A `failsafe`'s `pick` over `Error` needs `(*)` AND every reachable named arm; `(*)` does not discharge REACH-002, and omitting it is NITPICK-PICK-003.
  - `npkc` compiles a sibling file whose basename matches a root file's mismatched `mod:` name, merges both and emits two `define i32 @main` at exit 0 — so `mod:` = basename fails silently, not loudly.
open-questions-raised: O-N4
for-the-author:
  - O-N4: raise the quadratic-compile defect against the compiler (W-11). Nothing in the language changes; it is `npkc`'s implementation on three axes. Never work it around here — and do not let it settle O-Z1 as "ship a subset".
  - Probes 02, 03, 05-11 are unaffected by O-N4, which is a resource cost and not a semantics change. Recommendation: work the nine against the current pin rather than idle the stream; O-N4 changes none of their answers and only probe 04's cost waits on a re-pin. The decision is the author's.
  - The second, narrow `npkc` defect (mod:/basename sibling merge, two `main`s at exit 0) should be raised alongside O-N4, blocking nothing; it costs `ntime` nothing.
compiler-defect: tests/probe/defect/README.md
  reproduction case: tests/probe/defect/big_fixed_array_cost.npk
tokens-and-time: no harness yet, so nothing reports it; the subcycle's cost was dominated by `npkc` wall time — about 12 minutes across the three curves, with single compiles of 281 s (30 000 rows, 30.9 GiB) and 308 s (480 000-byte literal)
```

### The verifier's FAIL, triaged (2026-09-03, s2)

The verifier confirmed the tree, both reference checks, probe 01 — reading the
probe and working out for itself that the `Flip` twin makes the result
unreachable by any width-, alignment-, signedness- or name-based ordering, so
declaration order really is proven and TM-011 stands — and the quadratic shape.
It failed **two record claims**, and both failures were correct.

**(a) The magnitude was attributed to the wrong file.** The record gave
`tests/probe/defect/big_fixed_array_cost.npk` as costing 4.19 s and 473 MiB. It
does not. Three runs by the verifier and a fourth here, all exit 0:

| run | wall | peak RSS |
|---|---:|---:|
| verifier 1 | 6.19 s | 593 992 KiB |
| verifier 2 | 5.30 s | 593 620 KiB |
| verifier 3 | 5.34 s | 593 592 KiB |
| this dispatch | 6.11 s | 593 796 KiB |

Memory reproduces to within 0.1 %, so this is not load noise; the wall-time
spread is. **4.19 s / 473 MiB is the generic recipe's 4 000-row point** — the
`R`/`T`/`a`/`b` spelling the curve was generated with — and the committed file
carries `ntime`'s real identifiers. The two were conflated when the record was
written. Corrected in the defect `README.md`, in the file's own header comment,
in the paragraph above and in the previous entry's REPORT block, which now says
what it first said and why it was wrong. **The generic curve's rows are
unchanged**; what changed is which file each number belongs to, and the curve
now says so at the top of its own table.

**(b) Probe 04's IR-level claims were prose with nothing behind them.** The
record asserted `constant [30000 x …]` rather than `global`, `.rodata`
placement and zero `llvm.global_ctors`. `git ls-files` showed no `.ll`, no
`readelf` output and no log: probe 04's own header comment promised that the
record "records what they showed", and it recorded a summary of something
nobody could re-read. The row count and the 16-byte arithmetic **are**
confirmed by inspecting a committed file; the emission-shape claims were not.

Fixed by separating two questions that had been travelling together. *Do
30 000 rows compile, and does the arithmetic hold at that count* needs 30 000
rows. *What is the declaration lowered to* does not — the emission form is
chosen by the same path at any element count — so it is evidenced at **300
rows** by `tests/probe/probe04b_emission_shape.npk`, which compiles in 0.16 s
and 28 MiB, and the evidence is committed **verbatim, with every command's exit
code**, as `tests/probe/probe04b_emission_shape.txt`. What it shows:

- `@"npk.probe04b_emission_shape.TRANSITIONS" = constant [300 x %"…ZoneTransition"]` —
  `constant`, and a `grep` for the `global` spelling returns 0;
- 0 `llvm.global_ctors`, 0 `llvm.global_dtors`, 0 `appending` globals anywhere
  in the module, so nothing fills it at startup;
- in the object, `[ 5] .rodata PROGBITS … Flg A` — allocated, **not** `W`, and
  there is no `.init_array` or `.ctors` section in the list at all;
- in the **linked program**, which is what actually ships, the symbol is at
  `0x200790` inside a LOAD segment whose flags are `R` alone, so a write to the
  table is a fault the kernel raises rather than a corruption;
- symbol size `4800` = 300 × 16 **exactly**, so the linker's own accounting
  agrees with `#size_of` about the row that Z-7's estimate multiplies;
- exactly one `define i32 @main`, which incidentally guards against the second
  defect met in this subcycle;
- and it runs, exit 0 — which under D-151 also says nothing leaked.

That is a stronger statement than the original prose, because the original
stopped at the IR and this one follows the table into the segment the loader
maps. Probe 04's 30 000-row observation is now marked in §7 as made once, on
2026-09-03, and **not re-derivable without paying 281 s and 30.9 GiB**.

**Probe 04 was not re-run**, deliberately: another session is running four
harnesses on this machine, so everything compiled here stayed under about
600 MiB RSS and every compile carried `/usr/bin/time -f "%e s %M KiB"` and had
its exit code recorded beside its number.

### A new finding, recorded and NOT verified — identifier length

The valuable part of the verifier's pass, and the reason the magnitude error
was worth chasing rather than merely correcting. At a **fixed** element count of
4 000 rows, with `diff` confirming the row data identical and only the *names*
varying, the peak memory moves by half:

| the same 4 000 rows, spelled | peak RSS |
|---|---:|
| generic `R` / `T` / `a` / `b` | 398 036 KiB (388.7 MiB) |
| `ZoneTransition` / `TRANSITIONS` / `at_utc` / `type_index` | 546 628 – 554 800 KiB |
| the committed file | ≈ 593 800 KiB (579.8 MiB) |

The committed file differs from the middle row's reconstruction **only by a
nine-character-longer module name and two comment lines**, and that is worth
about 8 %. Two comment lines should not cost 40 MiB if identifier length were
the whole variable, and the generic figure here does not reconcile with axis 1's
own generic 4 000-row cell either — so the honest reading is *something about
total source bytes reaching the declaration*, which would make it axis 3 again
rather than a new axis.

It is written into
[`../../../tests/probe/defect/README.md`](../../../tests/probe/defect/README.md)
as a fourth observation, **attributed to the verifier and labelled as one
unreproduced measurement**, and this dispatch spent nothing trying to confirm
it. It has already been relayed to the compiler session under the same label,
because it fits a suspect that session already held — a `string_concat` per
element — and would explain the string axis being quadratic in time with memory
flat.

### Steps 2 and 3, minus 09 — the arithmetic facts and the host shapes (2026-09-03, s2)

**Committed first, before a single new probe was written.** Six files — 02, its
two twins, 03, 07 and 08 — had been built and run green and were sitting
uncommitted in the working tree for the second time in this subcycle. A session
loss would have taken all six, so this dispatch's first act was to re-run every
one against the pinned toolchain and land them with their verdicts, rather than
add a seventh to the pile. §7's table is filled from **this dispatch's own
runs**, not from the predecessor's report:

| probe | `npkc` | wall / peak RSS | `llc` | `ld.lld` | run |
|---|---|---:|---|---|---|
| `probe02_int128.npk` | exit 0 | 0.11 s / 24 896 KiB | exit 0 | exit 0 | **exit 0** |
| `probe02b_narrow_unchecked.npk` | exit 0 | 0.11 s / 24 128 KiB | exit 0 | exit 0 | **exit 0** |
| `probe02c_narrow_refused.npk` | **exit 1** | 0.03 s / 3 968 KiB | — | — | — |
| `probe02d_wide_literal_refused.npk` | **exit 1** | 0.02 s / 2 368 KiB | — | — | — |
| `probe03_timespec_sys.npk` | exit 0 | 0.25 s / 35 648 KiB | exit 0 | exit 0 | **exit 0** |
| `probe07_negative_div.npk` | exit 0 | 0.27 s / 37 568 KiB | exit 0 | exit 0 | **exit 0** |
| `probe08_readlink.npk` | exit 0 | 0.27 s / 37 184 KiB | exit 0 | exit 0 | **exit 0** |

Every one under 40 MiB, because another session holds four harnesses on this
machine. `probe04_big_fixed_table.npk` was **not** re-run, for the same reason
and by instruction.

**02c's exit 1 is its pass.** It carries `// expect-error: NITPICK-TYPE-009`
and `// expect-error-at: 33:27`, and the diagnostic reproduced verbatim, span
included. A probe expected to be refused stops at the first command (§2), and
the refusal *is* the verdict.

**The order was §5's, with one substitution.** Step 2 (07, 02) and step 3 (03,
08) were run as planned; **09 was held back**, not skipped, because it and 10
are the borrow-edge probes whose shape may change with the disposition of the
view-escape defect this subcycle found — reproduced and raised in the next
commit, with the questions it carries. Step 4's 05, 06 and 11 follow; 10 waits
with 09.

**One editorial correction to a committed file.** `probe02_int128.npk`'s header
named its twin `probe02b_narrow_trap.npk` — the name the file was going to have
before the measurement came back the other way. There is no trap, so there is
no `_trap` file, and a header naming a file that does not exist is the kind of
thing a later reader spends ten minutes on. It now names both twins and says
what each measured.

### Probe 02's consequences, landed in the same commit as its verdict (2026-09-03, s2)

Subcycle rule **P-4** — *a negative verdict amends `meta/specs/` in the same
commit* — is why this is one commit and not two. 02b's verdict is negative, and
a verdict recorded without its amendment is the note-to-self P-4 exists to
forbid.

**TM-105** in `meta/DECISIONS.md`, with **`SAFETY.md` S-15b** and
**`SPAN_MODEL.md` N-20b** written against it: *there is no checked narrowing in
this language, so every narrowing carries its own runtime range check, on the
same path, failing `ETimeValue`/`Overflow`.* The plan had read a guarantee into
`=>!` that it never offered — S-15's "narrows with `=>!` at a point where the
value is known to fit" is compatible with both worlds, and did not say which one
it was written for. It is the obligation now, not an observation.

**Nothing is being worked around and this is not a compiler defect.** `=>!` is
named for being unchecked and behaves as specified; D-210's trap covers plain
arithmetic, which is the case it was designed for. What was wrong was this
library's plan.

**`VERIFICATION.md` P-5's sample was wrong three ways, and only one of them was
the one this dispatch was sent to fix.**

1. *As instructed:* the widenings now read `=>` rather than `=>!`, so the
   sample carries exactly one unchecked cast — the dangerous one. A sample
   where every cast is `=>!` teaches a reader that they are all alike.
2. *Found while making that edit:* the sample's lower bound,
   `-9223372036854775808i128`, **does not compile**. `NITPICK-LEX-004` — the
   literal envelope is 64-bit and `int64`'s minimum has a magnitude one too
   large — followed by `NITPICK-PARSE-002`, because the refused token leaves no
   expression. The **maximum** is fine, which is what makes it dangerous: the
   bound pair a reader writes by symmetry from a working upper bound is exactly
   what stops compiling. `probe02d_wide_literal_refused.npk` pins it, and the
   sample now constructs the minimum arithmetically.
3. The `prove` was standing where the runtime check belongs. It is retained,
   with the check in front of it and a sentence saying why both exist: the
   proof says the narrowing cannot lose for any input the precondition admits,
   and the check says what happens to a caller who violated the precondition.

**One thing was found and deliberately NOT settled: O-X6.** `SPAN_MODEL.md`
N-20 says the `int128` sites "are exactly three … named above" and §5's table
marks **one**. `TESTING.md`'s `check_int128_sites`, which 0.2 puts on the
harness and V-1 calls one of the two checks that matter, is specified against
"the three sites §5 names" and cannot be written until they are named. Choosing
which three means designing `period_add` and `timestamp_since`, which no cycle
has done — so it is recorded with a recommendation (make the **table** the
authority and drop the count) rather than guessed. **N-20b binds to every
narrowing site in the table regardless of the count**, so nothing waits on it.

`probe02_int128.npk`'s header claimed the three were "`period_add`'s year/month
step, its day step and its nanosecond step", which the table does not support
either; it now says what §5 actually marks.

### The view-escape defect, reproduced and committed — O-N9 (2026-09-03, s2)

**It was living in a scratch directory.** The defect had been found, argued and
relayed, and the files that demonstrated it were outside the tree — which is
the "a prose summary is not evidence" failure this subcycle was already failed
for once. It is committed now, in
[`../../../tests/probe/defect/view_escape/`](../../../tests/probe/defect/view_escape/README.md),
with [`TRANSCRIPT.txt`](../../../tests/probe/defect/view_escape/TRANSCRIPT.txt)
carrying every command and every exit code verbatim.

**The disposition question is Q-5 and is the author's. The defect is a fact
either way**, which is why it did not wait.

**Six cases, and the contrast is the report.** Cases 1 and 2 are controls — the
rule working:

| # | returns out of the owning frame | `npkc` | runs |
|---|---|---|---|
| 1 | `@x`, a local `int64` | **refused** `NITPICK-BORROW-001` | — |
| 2 | `Keeper{ p: @x }` | **refused** `NITPICK-BORROW-001` | — |
| 3 | `string_bytes(s)`, `s` a local `string` | exit 0 | exit 0 |
| 4 | `Holder{ view: string_bytes(s) }` | exit 0 | exit 0 |
| 5 | the same, and then reads it | exit 0 | **exit 170** |
| 6 | nothing — the view is a parameter | exit 0 | exit 0 |

**Cases 2 and 4 are the same program.** One field, `int64->` or `uint8[]`, both
holding an address into the frame that is returning. That pair is what makes
this under-enforcement rather than a design question — and the compiler's own
`TYPE_REFERENCE.md` §9.2.1 settles the rest: *"a slice is a second-class borrow
(D-004): it passes down the call stack and never up."*

**Case 5 is new work, and it is the part worth having.** The predecessor's
version asserted that the escaped view still read `'a'…'h'` and would have
passed at exit 0; re-run here it exits **30** — the bytes are already gone. So
the case was rewritten to *report* rather than assert: it exits with the byte
it read, and the answer is **170**. That is `0xAA`, and `0xAA` is the runtime's
own free-poison — `runtime/npkrt.ll` stores `i8 -86` across every freed
payload, with D-183's comment beside it saying why (*"0xAA in every freed byte
makes the very first stale read produce loud deterministic garbage"*). Four
consecutive runs, 170 every time.

That upgrades the claim from *"reads freed memory"* to *"reads the allocator's
free-poison, deterministically, through a bounds check that passed"* — because
a slice is `{ ptr, len }` and only the pointer died. **The length always
survives**, which is exactly why the failure is quiet.

**What it would look like here:** a `src/fmt/` helper returning "the tail after
the separator" is one `pass` from case 3. Right length, `0xAA` bytes, which
parse as no digit and no month name — so it surfaces as `ETimeParse` **on valid
input**, at a call site that looks correct.

**Nothing that depends on Q-5 was landed.** No rule was added to `SAFETY.md`,
and `check_no_view_returns` is *proposed* for 0.0.3 rather than put on its
list; both are what Q-5 decides. Probes 09 and 10 stay held for the same
reason. What did land is the reproduction, O-N9 and Q-5 in
`meta/OPEN_QUESTIONS.md`, and **O-N8** — the `mod:`/basename sibling-merge
defect, which had been described at the foot of the O-N4 document with no id at
all and is now registered and linked.

**A numbering note, recorded so it is not re-litigated.** This repository's
`O-N` entries stop at O-N4 and resume at O-N8. `O-N` ids belong to the
**workbench registry**, not to a repository, because a compiler gap is raised
once for the whole ecosystem; O-N1 … O-N4 coincide by luck and from here the
registry's number is used as given. `../PLAYBOOK.md` §7 is the rule.

**One claim was caught and corrected before it was committed.** A first draft
of the reproduction's README said `check_no_view_returns` "is on cycle 0.0.3's
list". It is not on any list. Corrected to "proposed" in both places it
appeared.

### Step 4, part-worked — probes 05 and 06, and the third stop (2026-09-03, s2)

**Probe 06 — ACCEPTED, and it found a leak the plan had reasoned itself out
of.** `BUILD.md` B-12's `Vec<T>` compiles and works at a scalar `T` and an
owning one; `move T:x` consumes; `ralloc` doubling preserves order and string
bodies across two growths. Two facts that are not guessable were pinned on the
way: an explicit generic argument is a **turbofish** (`vec_init::<int64>(2i64)`;
the plain `<int64>` form is `NITPICK-PARSE-002`), and `#size_of<string>` is
**24**, not the 16 a slice's `{ ptr, len }` suggests — the first draft of that
assertion said 16 and the probe caught it, which is the probe doing its job on
its own author.

The finding is **TM-106** / `SAFETY.md` **S-18b**: freeing a `Vec`'s block does
not drop its elements, and **`exit 0` does not notice**, because D-151 watches
`wild` allocations and a `string`'s body is managed.

| 2 000 000 × {init, push one 35-byte string, free} | peak RSS | exit | under `ulimit -v 65536` |
|---|---:|---|---|
| block freed, elements orphaned | 125 184 KiB | 0 | **exit 92 — `HeapOom`** |
| elements moved out first | 0 KiB | 0 | exit 0 |

The address-space cap is there because a peak-RSS number is arguable and an
exit code is not. `0.0.4.md` §2's API table has **three** entries that discard
elements — `vec_free`, `vec_clear`, `vec_truncate` — whose postconditions speak
only of `count`, `cap` and `items`; a note now sits above it. And P-23's "every
scope that made one calls `vec_free`, **or `exit 0` traps under D-151**" is
true of the block alone.

**Probe 05 — ACCEPTED for everything it asked except one derive, and that one
is the subcycle's THIRD STOP.** The payload-enum shape works: construction, a
`pick` binding the payload, five values through a `Vec<Part>` across two
reallocations, the tag readable as `=>! int32` in declaration order, and
`Clone` keeping the payload. `#size_of<Part>` is **8** — not the 6 a
tag-plus-`uint16` suggests, because the payload aligns, which is worth knowing
before anybody multiplies a row size by a count. Two costs pinned: a `pick`
arm's variant must be **qualified** (`(Part.Year4)`; the bare form is
`NITPICK-RESOLVE-002`), and an **owning** payload cannot be bound by a lending
`pick` (`NITPICK-TYPE-046` — `pick (move(v))` instead), which does not bite
today because `FmtPart.Literal(uint16)` is the only payload-carrying variant in
the entire specification set.

### The third stop — O-N10 (W-11)

`#[derive(Eq)]` on a payload enum **emits derived code that does not compile**:
`NITPICK-TYPE-034` inside `<derived-1>`, saying `Part` has no built-in `==` —
the derived `Eq` being told to derive `Eq`, in a synthetic file the user cannot
open. **And `#[derive(Ord)]` on the identical declaration compiles, and its
`cmp` compares tags only: `Literal(7).cmp(Literal(9))` answers `Equal`.**

The quiet half is the serious one. A refusal is a bad afternoon; two different
values reporting `Equal` is a wrong answer nobody goes looking for, and the two
halves are not even consistent with each other — the trait that would say
*these differ* will not compile, and the trait that ranks them says they are the
same. `Hash` hashes the tag alone. `Clone` is **correct**, and that is in the
reproduction on purpose: without it the report would read "the derives lose
payloads", which is false, and a reader would stop trusting the one that works.

Isolated to the payload and to `Eq` alone — a payload-less enum derives `Eq`
fine, a plain struct with the same field types derives `Eq` fine, and the other
six derives on the payload enum all compile. **No file anywhere in the
compiler's tree derives any trait on an enum with a payload**, so this is
untested territory rather than a regression, and the asymmetry follows: the
payload-less path is exercised, the payload path was written and never run.

Reproduced with its transcript in
[`../../../tests/probe/defect/derive_payload_enum/`](../../../tests/probe/defect/derive_payload_enum/README.md)
and registered as **O-N10** (a provisional id — `O-N` numbers are the workbench
registry's).

**It is not blocking**, on the same reading that made O-N9 not blocking:
`ntime` is exposed at exactly one type and **no rule requires either derive on
it** — `TESTING.md`'s round trips compare formatted strings and parsed values,
never two `Layout`s. And nothing was worked around: probe 05 derives the five
that are correct, 05b pins the refusal, and **no hand-written `eq` was added**
to stand in for the broken derive, because there is no library code that needs
one yet and writing one now would bury the defect exactly where it must not be
buried.

### Probe 11 was NOT worked, by instruction

This dispatch was told to stop at a third stop and report rather than work
through it, and probe 05 produced one. **11 is unwritten**; 09 and 10 remain
held on Q-5. The `tests/probe/support/` directory this dispatch had scaffolded
for 11's imported module was **removed** rather than left describing a file
that does not exist.

Two facts for whoever writes 11, established while surveying the syntax and
worth more than the twenty minutes they cost:

- **Declaring `pub error:Foo;` does not force an arm.** The reachable set is
  computed from `fail`, `?!` and `!!!` **sites**, not from declarations
  (`src/frontend/analysis/reach.npk`) — but the driver walks **every module in
  the graph**, so a `fail` inside an imported module does reach the importer's
  `failsafe`. `SAFETY.md` S-4's per-module table is therefore right in
  substance and right for a subtly different reason than it states.
- **There is an unconditional floor.** Every program's `failsafe` must name
  `Unreachable`, `HeapOom`, `HeapBadRequest` and `WildLeak`, plus
  `DivByZero`/`DivOverflow` if the program contains any `/` or `%`,
  `IntOverflow`, `OutOfBounds` if it indexes, and others. `cal` divides and
  indexes constantly, so **importing it will oblige a consumer's `failsafe` to
  name arms it would not otherwise owe** — a real cost of importing this
  library that `SAFETY.md` §2's table does not currently mention. Probe 11 is
  where that gets measured rather than asserted.

```
REPORT nitpick-time 0.0.0
status: STOPPED
stream: s2
model: claude-opus-5
toolchain: 950bb1d
commits:
  - ef14210 cycle 0.0.0: probes 02, 03, 07, 08 and their twins; TM-105
  - 0667ecb cycle 0.0.0: O-N9 reproduced and committed, with the contrast
  - eb8d6b4 cycle 0.0.0: probes 05 and 06; TM-106; the third stop, O-N10
  - HEAD cycle 0.0.0: the execution record and the report
harness: not run: no harness until 0.0.2
  each line below is "$NPKC f.npk -o p.ll && llc -O0 -filetype=obj -relocation-model=static p.ll -o p.o && ld.lld -static p.o $NPKRT -o p && ./p", run from the tree root
  tests/probe/probe01_derive_ord.npk -> all four steps exit 0
  tests/probe/probe02_int128.npk -> all four steps exit 0 (npkc 0.11 s, 24 896 KiB)
  tests/probe/probe02b_narrow_unchecked.npk -> all four steps exit 0 (npkc 0.11 s, 24 128 KiB)
  tests/probe/probe02c_narrow_refused.npk -> npkc exit 1, NITPICK-TYPE-009 at 33:27 (expected; expect-error)
  tests/probe/probe02d_wide_literal_refused.npk -> npkc exit 1, NITPICK-LEX-004 + NITPICK-PARSE-002 at 51:18 (expected)
  tests/probe/probe03_timespec_sys.npk -> all four steps exit 0 (npkc 0.25 s, 35 648 KiB)
  tests/probe/probe04b_emission_shape.npk -> all four steps exit 0
  tests/probe/probe05_payload_enum.npk -> all four steps exit 0 (npkc 0.26 s, 36 032 KiB)
  tests/probe/probe05b_derive_eq_refused.npk -> npkc exit 1, NITPICK-TYPE-034 in <derived-1> (expected)
  tests/probe/probe06_generic_vec.npk -> all four steps exit 0 (npkc 0.26 s, 35 648 KiB)
  tests/probe/probe07_negative_div.npk -> all four steps exit 0 (npkc 0.27 s, 37 568 KiB)
  tests/probe/probe08_readlink.npk -> all four steps exit 0 (npkc 0.27 s, 37 184 KiB)
  tests/probe/probe04_big_fixed_table.npk -> NOT RE-RUN by instruction (281 s, 30.9 GiB)
  tests/probe/defect/view_escape/TRANSCRIPT.txt -> six cases, every command and exit code, verbatim
  tests/probe/defect/derive_payload_enum/TRANSCRIPT.txt -> three cases plus the eight-row isolation, verbatim
check: clean
record: meta/roadmap/0.0/0.0.0.md — RUNNING (since 2026-09-03, s2)
next: n/a
findings-for-playbook:
  - `int64`'s MINIMUM cannot be spelled as a literal in any width: NITPICK-LEX-004, the envelope is 64-bit and the magnitude is one too large. The MAXIMUM is fine, so a bound pair written by symmetry from a working upper bound is exactly what stops compiling. Build it arithmetically.
  - An explicit generic argument at a call site is a TURBOFISH — `f::<int64>(x)`. The plain `f<int64>(x)` is NITPICK-PARSE-002 with no hint.
  - A `pick` arm's variant must be QUALIFIED — `(Part.Year4)`, never `(Year4)`, which is NITPICK-RESOLVE-002. A `failsafe`'s bare `(HeapOom)` is not a counter-example: those are the prelude's.
  - A lending `pick` may NOT bind a payload that owns heap storage — NITPICK-TYPE-046. Use `pick (move(v))`, or bind nothing and read through the selector.
  - Freeing a generic container's block does NOT drop its elements, and `exit 0` does not notice: D-151 watches `wild` allocations and a `string` body is managed. 125 MiB retained over 2 M elements at exit 0; HeapOom under a 64 MiB cap. "The leak test exits 0" is a claim about `wild` only.
  - `#[derive(Eq)]` on an enum with a PAYLOAD does not compile (NITPICK-TYPE-034 inside `<derived-1>`), and `#[derive(Ord)]` on the same declaration compiles with a tag-only `cmp` — `Literal(7).cmp(Literal(9))` is `Equal`. No file in the compiler's tree derives anything on a payload enum.
  - Measure `#size_of`, never derive it from field widths: a `string` is 24 bytes (not a slice's 16) and `enum { Literal(uint16); … }` is 8 (not 6) because the payload aligns. Two drafts here asserted the arithmetic answer and both were wrong.
  - The runtime poisons freed bytes with 0xAA on every free (npkrt.ll, D-183), so a stale read is loud and deterministic — which makes a dangling view reproducible evidence rather than a maybe.
open-questions-raised: O-X6, O-N8, O-N9, O-N10
for-the-author:
  - O-N10 is the third stop and its QUIET half is the one to raise loudest: `#[derive(Ord)]` on a payload enum compiles and reports two different values as `Equal`. Recommendation: raise both halves against the compiler as one defect, and ask for a test in its own tree that derives on a payload enum — the gap is coverage, since no file there does. Not blocking `ntime`: one type is exposed and no rule needs either derive on it.
  - The O-N10 id is PROVISIONAL. `O-N` numbers are the workbench registry's; renumber it there if 10 is taken.
  - O-X6: `SPAN_MODEL.md` N-20 says the `int128` sites are "exactly three … named above" and §5's table names one, so 0.2's `check_int128_sites` cannot be written against it. Recommendation: make the TABLE the authority and drop the count, at the cycle that designs `period_add`. Nothing waits on it — N-20b binds to every narrowing site regardless.
  - `SAFETY.md` §2's per-import arm table understates what a consumer writes: every program owes an unconditional floor (Unreachable, HeapOom, HeapBadRequest, WildLeak) plus DivByZero/DivOverflow for any `/` or `%`, IntOverflow and OutOfBounds. `cal` divides and indexes constantly, so importing it obliges arms a consumer would not otherwise owe. Recommendation: probe 11 measures it, and §2 gains a sentence.
  - Probe 11 was not worked and 09/10 stay held. Recommendation for the next dispatch: 11 first — it is independent of both open dispositions — then 09 and 10 once Q-5 is answered.
compiler-defect: tests/probe/defect/derive_payload_enum/README.md
  reproduction cases: tests/probe/defect/derive_payload_enum/case1_eq_refused.npk, case2_ord_ignores_payload.npk, case3_hash_and_clone.npk
  transcript: tests/probe/defect/derive_payload_enum/TRANSCRIPT.txt
  also landed this dispatch: tests/probe/defect/view_escape/README.md (O-N9)
tokens-and-time: no harness yet, so nothing reports it; `npkc` wall time across all thirteen probe compiles was under 3 s and no compile exceeded 37 MiB RSS, deliberately — another session holds four harnesses on this machine. The two 2 000 000-iteration leak measurements took 0.7 s and 0.9 s. `probe04_big_fixed_table.npk` was not re-run.
```

