# 0.0.6 — close — PLANNED

> **Scope:** reconcile what the cycle learned against what the specifications
> say, write 0.1's opening subcycle execution-grade, and archive.

## Steps

1. **The probe reconciliation.** Every verdict in `0.0.0.md` §7 re-read against
   `meta/specs/`. A verdict that forced a change should already have landed
   (0.0.0's P-4); this step is the check that it did, and it is done by reading
   the specification, not by remembering.
2. **The size reconciliation.** `0.0.5.md` §4's measured numbers against
   `ZONE_MODEL.md` §3's estimate. If they differ materially, §3 is amended and
   the amendment is a decision — TM-007's cost argument cites that table.
   **DONE AT 0.0.5 (TM-135):** they differ by 37%, §3 now carries the measured
   table beside the estimate and a new Z-7b for the 4.4% margin, and O-X2 and
   O-Z1 are struck through with the decision number in `meta/OPEN_QUESTIONS.md`
   rather than deleted. What is left for this step is to *re-read* that, not to
   redo it.
3. **The findings.** Everything the cycle found that was not a probe verdict or
   a measurement — a reserved word that cost a build, an ABI number the plan had
   wrong, a harness shape that did not work — written into this file as a
   numbered list. The compiler's execution records are load-bearing precisely
   because one writer kept them.
4. **The spike is gone.** `meta/scratch/tzdb_spike/` deleted or demonstrably
   imported by nothing (0.0.5's P-28). **Note before deleting:** its
   `TRANSCRIPT.txt` is the evidence TM-135, `ZONE_MODEL.md` §3 and
   `meta/research/tzdb-size.md` all cite, and a number whose program is not
   committed is a claim rather than evidence (`../../../PLAYBOOK.md` §6). So
   the choice to make with a reason in the record is *delete the code and keep
   the transcript*, or delete both and accept that the 475 006 becomes
   unre-derivable until 0.5. Its emitted `.npk` was never committed — it goes
   to `.internal/` — so nothing large is at stake either way.
5. **The full run.** `harness/run.py` green, self-check first, every stage
   reporting, the verdict list committed.
6. **`0.1/0.1.0.md` written execution-grade**, before the cycle closes, so that
   0.1 is openable by a session that was not here.
7. **Archive.** `git mv meta/roadmap/0.0 meta/roadmap/done/0.0`, and
   `ROADMAP.md`'s row updated to point at the archive.

## Acceptance

- [ ] every probe verdict reconciled against the specifications
- [ ] the tzdb size reconciled, and `ZONE_MODEL.md` §3 amended if it moved
- [ ] the cycle's findings written as a numbered list
- [ ] the spike removed
- [ ] a full green run, verdicts committed
- [ ] `0.1/0.1.0.md` written
- [ ] cycle archived, `ROADMAP.md` updated

## What cycle 0.0 taught

*(Written at the close. This section is the cycle's most durable output — the
compiler's equivalents are why its later cycles cost less than its earlier
ones.)*
