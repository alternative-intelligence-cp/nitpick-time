# `docs/`

User-facing documentation, written at cycle 1.0. Until then the specification
in `../meta/specs/` is the documentation. The `failsafe` arm contract
(`COMPAT.md` §6) is published here and is generated rather than written.
