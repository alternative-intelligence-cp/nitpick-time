# 0.1.1 — the algorithms — DONE (2026-09-25)

> **Scope:** `date_to_days` and `days_to_date` as Howard Hinnant's
> `days_from_civil` and `civil_from_days` (`CALENDAR.md` C-10, TM-016), cited
> in the module header; every division in them by a nonzero literal, and a tree
> check that says so (C-11); every intermediate in `int64` (C-12); and **the
> range constants recomputed by a test** — which, run once at planning, finds
> that one of them is wrong (§1). **No loop, no sweep**: the exhaustive round
> trip is 0.1.2's, and it is the cycle's gate.
>
> **Written 2026-09-25 by the stream-2 planner, against compiler `c3bdae2` and
> against the tree as `0.1.0b.md` and `0.1.0c.md` leave it.** Both functions
> below were compiled and run in a scratch copy of that tree at `c3bdae2`: all
> twenty-six vectors of §4 pass in both directions, the range test goes red at
> today's constant and green at the corrected one, and a full forward round trip
> over the corrected range (7 304 484 days) takes 1.23 s at `-O0` and 0.19 s
> under `opt -O2` — so 0.1.2's gate costs seconds, as TM-026 said.

**Gate for starting:**

1. `0.1.0b.md` and `0.1.0c.md` are `DONE`, verified, and green on CI.
2. **`../../OPEN_QUESTIONS.md` Q-6 answered — or the orchestrator's `NOTES:`
   says to proceed on its recommended default.** This subcycle is written for
   the recommendation (contracts stay comments; §6); §6 also gives the exact
   alternative if the author chooses live contracts, so neither answer needs a
   new plan.
3. The currency row for Hinnant's page in `../../research/CURRENCY.md` is dated
   (it is: checked 2026-09-25).

---

## 0. Read these first

Same variables as `0.1.0b.md` §0.

1. `../../specs/CALENDAR.md` §2, §4 (C-10 … C-12) and §5 (C-16); C-8, C-8b and
   C-8c (0.1.0c).
2. `../../DECISIONS.md` TM-014, TM-016, TM-026, TM-105, TM-122, TM-130.
3. `../0.1.0.md` §6 — *"the negative-year correction … IS still owed by
   `days_from_civil`'s `era`"* — and its execution record's finding 3.
4. Hinnant, *chrono-Compatible Low-Level Date Algorithms* — the page and its
   date are `../../research/CURRENCY.md`'s first row. **Read the two functions
   there, not a copy of them**; §2 below is a transcription and a transcription
   is what gets checked against the source, not the other way round.
5. `tests/probe/probe07_negative_div.npk` — `/` truncates toward zero and `%`
   takes the dividend's sign, which is what Hinnant's `era` correction is for.

---

## 1. The finding this subcycle fixes first — one range constant is off by a day

**`CALENDAR.md` §2's minimum day number, its total, and the minimum
`Timestamp.secs` are each one day wrong.** Measured three independent ways at
planning:

```
-9999-01-01 to 0001-01-01 is exactly 10 000 years = 25 x 146 097 = 3 652 425 days
day(0001-01-01) = -719 162                   (Python datetime, 3.12.3)
day(-9999-01-01) = -719 162 - 3 652 425 = -4 371 587        NOT -4 371 588
Hinnant at c3bdae2:  days_from_civil(-9999, 1, 1)  = -4 371 587
                     days_from_civil(-10000, 12, 31) = -4 371 588   <- today's NTIME_DAY_MIN
the range test (§4b) at today's constant: exit 10;  at -4 371 587: exit 0
```

| Quantity | Written today | Correct |
|---|---|---|
| `NTIME_DAY_MIN` — the day number of −9999-01-01 | −4 371 588 | **−4 371 587** |
| total days in the range | 7 304 485 | **7 304 484** |
| `NTIME_SECS_MIN` = `NTIME_DAY_MIN × 86 400` | −377 705 203 200 | **−377 705 116 800** |
| `NTIME_DAY_MAX` (9999-12-31) | 2 932 896 | 2 932 896 — right |
| `NTIME_SECS_MAX` | 253 402 300 799 | 253 402 300 799 — right |

**Impact, stated plainly.** `NTIME_DAY_MIN` and `NTIME_SECS_MIN` are **public**
— re-exported by `src/lib.npk` since 0.0.4 — and each names a day the library
cannot represent: −4 371 588 is **−10000-12-31**. With the old constant,
`days_to_date(NTIME_DAY_MIN)` would pass its own range check and then be
refused by `civil_date` (year −10000), two answers for one bound; and 0.1.2's
gate would fail on its first element. **No consumer exists yet** (`date` is
gated on cycle 0.7), so the damage is to documents, but TM-014 says these
numbers *"are computed, not estimated"* — they were computed, from a wrong
starting day, and every relation `src/core/limits.npk` writes out to check them
by hand was checked for internal consistency and therefore passed.

**Extent — `git grep -n '4371588\|4 371 588\|7304485\|7 304 485\|377705203200\|377 705 203 200' -- ':!meta/roadmap/0.1/0.1.1.md'`
— 36 lines in 16 files after this plan was committed** (this file quotes the
numbers and is excluded; two of the 36 are the cycle README's flag on this very
finding, which step 2 resolves), of which these are live and change in step 2: `src/core/limits.npk` (the two constants and the comment's three
relations), `tests/unit/limits_named.npk`, `../../specs/CALENDAR.md` (§2's table,
C-16), `../../specs/TIME_MODEL.md` (the minimum `Timestamp`),
`../../specs/VERIFICATION.md` P-6 (`Rules:DayNumber`), `../../specs/TESTING.md`
(the sweep table), `../../specs/BUILD.md` (the `sweep-count` example),
`nitpick.toml` (a comment), `harness/stages.py` (a comment), `src/cal/README.md`,
`../ROADMAP.md` (0.1's gate), this cycle's `README.md` (0.1.2's items and the
gate) and `../0.2/README.md`. `../../DECISIONS.md` TM-014, TM-026 and TM-122 carry
the numbers in settled text: **not rewritten** — PD-12 replaces part of each,
and each gets the marker line. `../done/0.0/` is history. State the denominator
(*N lines, M edited, K history*).

---

## 2. The two functions, decided at planning

In `src/cal/cal.npk`, after `days_in_month` and before the constructors, with
`use "../core/limits.npk".NTIME_DAY_MIN;` and `….NTIME_DAY_MAX;` added to the
file's `use` block. **This is the text that compiled and passed at `c3bdae2`:**

```nitpick
pub func:date_to_days = int64(CivilDate:cd) never fails {
    int64:m = cd.month => int64;
    int64:d = cd.day => int64;
    int64:y = cd.year => int64;
    if (m <= 2i64) { y = y - 1i64; }
    int64:era = (is (y >= 0i64) : y : (y - 399i64)) / 400i64;
    int64:yoe = y - (era * 400i64);
    int64:mp = is (m > 2i64) : (m - 3i64) : (m + 9i64);
    int64:doy = (((153i64 * mp) + 2i64) / 5i64) + d - 1i64;
    int64:doe = (yoe * 365i64) + (yoe / 4i64) - (yoe / 100i64) + doy;
    pass (era * 146097i64) + doe - 719468i64;
};

pub func:days_to_date = CivilDate(int64:n) {
    if (n < NTIME_DAY_MIN) { fail ETimeValue; }   // ValueFault.YearRange
    if (n > NTIME_DAY_MAX) { fail ETimeValue; }   // ValueFault.YearRange
    int64:z = n + 719468i64;
    int64:era = (is (z >= 0i64) : z : (z - 146096i64)) / 146097i64;
    int64:doe = z - (era * 146097i64);
    int64:yoe = (doe - (doe / 1460i64) + (doe / 36524i64) - (doe / 146096i64)) / 365i64;
    int64:y = yoe + (era * 400i64);
    int64:doy = doe - ((365i64 * yoe) + (yoe / 4i64) - (yoe / 100i64));
    int64:mp = ((5i64 * doy) + 2i64) / 153i64;
    int64:d = doy - (((153i64 * mp) + 2i64) / 5i64) + 1i64;
    int64:m = is (mp < 10i64) : (mp + 3i64) : (mp - 9i64);
    if (m <= 2i64) { y = y + 1i64; }
    pass (relay civil_date(y, m, d));
};
```

**The decisions in that text, each with what it declined** (PD-13 records them):

- **`date_to_days` is `never fails` and TOTAL over every field value.** With
  `year` an `int32` and `month`/`day` `uint8`, widened by the CHECKED `=>`
  (C-12), `|era| ≤ 5.4 × 10⁶` and `|era × 146 097| ≤ 7.9 × 10¹¹`: nothing can
  overflow `int64`, and every divisor is a nonzero literal, so no path traps.
  After C-8c its argument can only have come from `civil_date` — the totality is
  C-8b's second bullet, kept because it is free and `wild` is still an opt-out.
  *Declined:* a fallible `date_to_days` re-checking validity — the seal already
  guarantees it, and a second copy of the check is a second place to disagree.
- **`days_to_date` checks its range FIRST, before `n + 719468`** (S-12: checked
  before the trap). At `int64` MAX that addition would trap `IntOverflow`; the
  refusal test (§4c) asserts the check comes first.
- **`days_to_date` builds through `relay civil_date(y, m, d)`**, not a struct
  literal. So `civil_date` stays the only builder in `src/` (C-8 inside `cal`),
  the `int64 → int32/uint8` narrowing is validate-then-narrow by construction
  (TM-105), and **an algorithm error becomes a refusal the 0.1.2 sweep sees**
  rather than a wrong date it might not. Cost: five compares and one
  `days_in_month` per call, inside the 1.23 s above. *Declined:* the literal —
  one line shorter and it moves the narrowing back into this function; a
  private `Ymd` struct returned to a wrapper — a second type for one caller.
- **The negative-year corrections are Hinnant's, as given** (C-10): `era`'s
  `y - 399` and `z - 146096`. `../0.1.0.md` §6 recorded that the leap rule did
  not need one and `era` does; the vectors −1, −4, −100, −400 and −401 (§4a)
  are that correction under test.
- **`is (c) : a : b`** is the ternary (CONTROL_REFERENCE §1.4), measured at
  `c3bdae2`; Hinnant's `(m <= 2)` added to an integer becomes an `if`.
- **The names**: the parameter is `cd` so it does not read as `CivilDateTime`'s
  field `date` (`date` itself is a legal name — 0.1.0 measured it); `mp`,
  `doy`, `doe`, `yoe` and `era` are Hinnant's own, so the transcription can be
  checked against the page line by line.
- **No loop, so no measure and no `DecreasesViolated`**: `cal`'s arm bill stays
  **11** — measured with this text. The module header says so, because the next
  function added here that loops will add the arm.
- **The header cites the source**: the page, its URL, the page's own date
  (2021-09-01) and the date read (2026-09-25), *"per `meta/research/CURRENCY.md`"*
  — and names the one deviation: Hinnant's `unsigned` intermediates are `int64`
  here (C-12), which makes the arithmetic total on inputs his `unsigned` would
  wrap.

---

## 3. C-11: every divisor a nonzero literal, and the check that says so

**After this subcycle `src/cal/cal.npk` divides by:** `%` 4, 100, 400
(`is_leap_year`) and 7 (`weekday_number_sunday_first`); `/` 400, 5, 4, 100
(`date_to_days`) and 146 097, 1 460, 36 524, 146 096, 365, 153, 5, 4, 100
(`days_to_date`). **C-11's own list — *"(4, 5, 100, 400, 146097)"* — names
`days_from_civil`'s divisors only**; PD-14 amends C-11 to state the rule and
point at the check rather than carry a list that the next function falsifies.

**`check_literal_divisors`**, a new LIVE tree check in `harness/checks.py`,
registered in `LIVE` beside `check_constants_named`:

- **Scope:** every `.npk` under `src/cal/` (C-11 is the calendar's rule;
  `SAFETY.md` S-16's library-wide rule admits a *proven* divisor, which a text
  check cannot see).
- **Reads code, never prose:** `code_lines` (comments and string bodies
  blanked), as `check_civil_literal` did.
- **An operator** is a `/` or a `%` that is not part of a wrapping operator:
  a `%` immediately preceded by `+`, `-` or `*` is `+%`/`-%`/`*%` (D-312) and
  is not a division. A compound `/=` or `%=` is a division.
- **Its right operand**, after optional whitespace and optional `(`, must match
  `[1-9][0-9]*(i|u)(8|16|32|64|128)\b` — a nonzero integer literal with its
  width suffix. Anything else is a finding naming the file, the line and the
  operand's text: *"divides by `m`, which is not a nonzero literal (C-11): the
  divide-by-zero trap is no longer unreachable by construction"*; a literal
  zero gets its own sentence.
- **Its headline states its denominator**:
  `N division(s) over M file(s) in src/cal/, every divisor a nonzero literal`,
  and an empty scope is a failure (V-1b), as `check_civil_literal` did it.
- **Commissioned** (TM-126) in `harness/selfcheck.py`'s tree-check plants, in
  the tuple form the others use: a violation
  `("src/cal/cal.npk", "mod:cal;\nfunc:f = int64(int64:y, int64:m) never fails { pass y / m; };\n")`
  against a control with `/ 400i64` in its place, expecting `"is not a nonzero
  literal"`; a second plant `% 0i64` against `% 4i64`, expecting `"literal
  zero"`; and a third whose control is the WRAPPING form `y +% 1i64` beside a
  real `/ 4i64` — the proof that `+%` is not read as a division. The self-check's
  planted count goes up by three; read it from the run.
- `../../specs/TESTING.md` §2 gains its row; `CLAUDE.md`'s check count is
  re-derived.

---

## 4. The tests — three files, every value computed before it was written

Each ends with this `failsafe` — exactly the bill of a program importing `cal`
and `limits` at `c3bdae2`, 11 identities, measured. **If REACH asks for anything
else, stop and record it**: the bill was measured, and a difference is a finding
(TM-107), not an arm to add quietly.

```nitpick
func:failsafe = int32(Error:e) {
    pick (e) {
        (cal.ETimeValue)    { exit 80i32; },
        (HeapBadRequest)    { exit 91i32; },
        (HeapOom)           { exit 92i32; },
        (IntOverflow)       { exit 93i32; },
        (OutOfBounds)       { exit 94i32; },
        (Unreachable)       { exit 95i32; },
        (WildLeak)          { exit 96i32; },
        (DivByZero)         { exit 97i32; },
        (DivOverflow)       { exit 98i32; },
        (StackExhausted)    { exit 106i32; },
        (MachineFault)      { exit 107i32; },
        (*)                 { exit 99i32; }
    }
    exit 9i32;
};
```

**None of the three loops**: every case is written out. A test loop would need
a measure and owe `DecreasesViolated`; a `till`/`loop` counted loop owes
`BadStep` (measured — REACH arms it even for a literal step) — and neither is
needed for twenty-six cases. **If a later test wants a bounded range, the
cheapest form is `for (int64:i in lo..hi)`: inclusive at both ends (measured,
`0..10` visits eleven values), no clause, no extra arm.** 0.1.2's sweep should
read this.

### 4a. `tests/unit/day_number_vectors.npk` — `// expect-exit: 0`

Each row both ways: `date_to_days(civil_date(y, m, d) ?! ETimeValue)` must equal
`n`, and `days_to_date(n) ?! ETimeValue` must have `year`, `month`, `day` equal
to `y`, `m`, `d`. Row *k* exits `8 + 2k` on a forward mismatch and `9 + 2k` on a
backward one — 10 … 61, clear of every arm code.

| k | date | `n` | why this row |
|---|---|---|---|
| 1 | 1970-01-01 | 0 | the epoch |
| 2 | 1969-12-31 | −1 | the first negative day number |
| 3 | 2000-02-29 | 11 016 | a 400-year leap day |
| 4 | 2000-03-01 | 11 017 | Hinnant's shifted year begins in March |
| 5 | 1900-02-28 | −25 509 | a century that is not leap … |
| 6 | 1900-03-01 | −25 508 | … so no 29th between these two |
| 7 | 1600-02-29 | −135 081 | the first 400-multiple before the epoch |
| 8 | 2400-02-29 | 157 113 | and after it |
| 9 | 0001-01-01 | −719 162 | Python's `date.min` — the cross-oracle's floor (C-18) |
| 10 | 0000-12-31 | −719 163 | year 0 exists (C-2) … |
| 11 | 0000-03-01 | −719 468 | … and is where Hinnant's day 0 sits |
| 12 | 0000-02-29 | −719 469 | year 0 is a leap year |
| 13 | 0000-01-01 | −719 528 | |
| 14 | −0001-12-31 | −719 529 | the first negative year |
| 15 | −0001-03-01 | −719 834 | |
| 16 | −0001-02-28 | −719 835 | −1 is not leap: 28 is the last day |
| 17 | −0004-02-29 | −720 930 | a negative leap year |
| 18 | −0100-02-28 | −755 994 | a negative century, not leap … |
| 19 | −0100-03-01 | −755 993 | … so these are adjacent |
| 20 | −0400-02-29 | −865 566 | a negative 400-multiple: leap |
| 21 | −0401-12-31 | −865 626 | the day before an `era` boundary under truncating `/` |
| 22 | −9999-01-01 | −4 371 587 | `NTIME_DAY_MIN`, corrected (§1) |
| 23 | −9999-12-31 | −4 371 223 | |
| 24 | 9999-01-01 | 2 932 532 | |
| 25 | 9999-12-31 | 2 932 896 | `NTIME_DAY_MAX` |
| 26 | 2026-09-25 | 20 721 | the day this plan was written |

**Re-derive the table before writing a line of the test** — by two methods
that share nothing with Hinnant's formula, and they must agree:

```python
import datetime
E = datetime.date(1970, 1, 1).toordinal()
def m1(y, m, d): return datetime.date(y, m, d).toordinal() - E      # years >= 1
def m2(y, m, d):                    # shift into [1, 9999] by whole 400-year cycles
    k = 0
    while y + 400 * k < 1: k += 1
    while y + 400 * k > 9999: k -= 1
    return m1(y + 400 * k, m, d) - k * 146097
```

`m1` for every row with a year ≥ 1, `m2` for every row, and `m1 == m2` wherever
both apply. Record the command and its output in the execution record.

### 4b. `tests/unit/range_constants.npk` — `// expect-exit: 0` — WRITTEN FIRST

Recomputes the four constants from the algorithm and the named bounds, rather
than trusting `src/core/limits.npk`:

```
date_to_days(civil_date(NTIME_YEAR_MIN, 1, 1))   == NTIME_DAY_MIN            exit 10
date_to_days(civil_date(NTIME_YEAR_MAX, 12, 31)) == NTIME_DAY_MAX            exit 11
NTIME_DAY_MAX - NTIME_DAY_MIN + 1                == 7304484                  exit 12
NTIME_SECS_MIN == NTIME_DAY_MIN * NTIME_SECS_PER_DAY                         exit 13
NTIME_SECS_MAX == NTIME_DAY_MAX * NTIME_SECS_PER_DAY + NTIME_SECS_PER_DAY - 1 exit 14
days_to_date(NTIME_DAY_MIN)  is -9999-01-01;  days_to_date(NTIME_DAY_MAX) is 9999-12-31   15, 16
days_to_date(NTIME_DAY_MIN - 1) and (NTIME_DAY_MAX + 1) refused (is_error)   17, 18
civil_date(NTIME_YEAR_MIN - 1, 12, 31) and (NTIME_YEAR_MAX + 1, 1, 1) refused 19, 20
```

**Its control is the red it must produce first.** Write it, add the two
functions, and run it against the UNCHANGED constants: it must exit **10**
(measured at planning). Record that run. Then step 2 corrects the constants and
it exits 0. A test of a constant that has only ever been seen green proves
nothing about the constant — the board's worklist item 10, and DEF-67's lesson.

### 4c. `tests/unit/days_to_date_refused.npk` — `// expect-exit: 0`

`days_to_date` REFUSES, through its `Result`, never through a trap:
`NTIME_DAY_MIN - 1`, `NTIME_DAY_MAX + 1`, `9223372036854775807i64` (`int64`
MAX — the case that proves the range check precedes `n + 719468`, which would
otherwise exit **93**), and `int64` MIN built arithmetically,
`(0i64 - 9223372036854775807i64) - 1i64` (the literal cannot be spelled —
`probe02d`). Each `Result<CivilDate>:r = days_to_date(n); if (!r.is_error) { exit N; }`,
`civil_construct.npk`'s idiom, exits 10 … 13.

---

## 5. Steps, in order

1. **The test first.** §4b's file, then the two functions of §2 and their `use`
   lines, then run the harness `--only range_constants` **to iterate** (it
   concludes nothing, TM-125) and see exit **10**. Record the line.
2. **The constants (PD-12).** `src/core/limits.npk`: `NTIME_DAY_MIN = -4371587i64`,
   `NTIME_SECS_MIN = -377705116800i64`, and the comment's three hand-checked
   relations recomputed (*"2932896 + 4371587 + 1 = 7304484"*, *"-4371587 * 86400 =
   -377705116800"*), with one sentence saying the relations had been checked for
   consistency and the starting day had not, and that §4b is now the check.
   `tests/unit/limits_named.npk`: `7304484`. Then every live site of §1's
   search. `--only range_constants` exits 0.
3. **§4a and §4c.** Re-derive §4a's table first (the Python above).
4. **`check_literal_divisors`** and its three plants (§3).
5. **The module header**: the source citation (§2's last bullet); the list of
   public functions; *"no loop in this module — the first that loops adds
   `DecreasesViolated` to every consumer"*; and the removal of *"NO ALGORITHM
   HERE"* and *"`date_to_days` and `days_to_date` … are cycle 0.1.1's"*, which
   are now false.
6. **`src/lib.npk`** re-exports `date_to_days` and `days_to_date`, one line
   each, after `days_in_month` — `lib_reexports` 48 → **50**; its tag moves.
7. **The contracts** — §6, per Q-6.
8. **The documents**: `CALENDAR.md` C-10 cites the page and the currency row;
   C-11 per PD-14; §2's table and C-16 per PD-12. `SAFETY.md` S-4's `cal` row
   stays 11 — confirm it from `check_failsafe_arms`, do not assume it.
9. **The denominators**, from `check_denominators`' `measured:` line (three
   files added under `tests/unit/`: `npk_total`, `npk_tests`, `suite_roots`,
   `tests_exit` move), then the full run: **expect `GREEN -- 78 unit(s), 0
   failures; 5 pending`** (75 + 3). `check_refs` before and after staging. One
   commit: `cycle 0.1.1: Hinnant's algorithms, and NTIME_DAY_MIN corrected by
   the test that recomputes it`. Push; CI green on it, read from its log; the
   run id in the record.

---

## 6. The contracts, and Q-6

**As written — Q-6's recommended answer (A′):** the obligations stay comments,
in the exact syntax P-1 prescribes, with `answer` for `result` (TM-130) — the
form every existing comment contract in `src/` uses:

```
date_to_days:   // `ensures answer >= NTIME_DAY_MIN && answer <= NTIME_DAY_MAX`
                //  (true of every CivilDate `civil_date` can build -- C-8c)
days_to_date:   // `ensures answer.month >= 1 && answer.month <= 12`
                //  -- held by construction: it is `civil_date`'s result
                // `prove(date_to_days(answer) == n)`      -- VERIFICATION.md §6's row
```

and `VERIFICATION.md` §6's two rows for these functions gain *"written as
comments at 0.1.1 (Q-6)"*. **Nothing arms; `cal`'s bill stays 11.**

**If the author answers live contracts (A):** `date_to_days` carries a LIVE
clause instead of its comment —

```nitpick
pub func:date_to_days = int64(CivilDate:cd)
    ensures result >= NTIME_DAY_MIN && result <= NTIME_DAY_MAX
    never fails { … };
```

— which compiles at `c3bdae2` (measured) and **arms `EnsuresViolated` in every
consumer: `cal` 11 → 12, the umbrella 13 → 14.** That needs a cross-stream exit
code from the orchestrator first (the next free, 110, is the natural
proposal), §4's `failsafe` block gains the arm, `SAFETY.md` S-4 and the
generator learn it (an `ensures` in a module's code arms `EnsuresViolated`, a
rule in `_arith_arms` beside PD-7's and PD-11's), and the `prove` rows **stay
comments** under both answers — a plain build lowers `prove` to nothing, and
this harness has no verified build until cycle 0.8. `days_to_date` gets no live
clause under either answer: its range is a `fail`, not a `requires` (S-12), and
its result's validity is `civil_date`'s construction.

---

## 7. What will bite

- **Hinnant's `unsigned` becomes `int64`** — his `doy` is `[0, 365]` only for
  valid input; with `int64` a `d = 0` gives −1 instead of wrapping, which is
  why the function stays total. Do not "fix" it to `uint`.
- **`/` truncates toward zero** (probe07): `(y - 399) / 400` is the correction,
  not an off-by-one.
- **Validate, then narrow** — `days_to_date`'s narrowing is inside
  `civil_date`; do not add a `=>!` here.
- **`result` and `old` are reserved** (TM-130): comments use `answer`; a LIVE
  clause (§6's alternative) uses `result`, because there it is the keyword.
- **A constant's test must be seen red first** (§4b).
- **`fixed` initialisers are folded, so `NTIME_SECS_MIN`'s literal must equal
  the product exactly**, and `NTIME_DAY_MIN` is not a type's extreme, so
  `-4371587i64` spells as a plain literal (`limits.npk`'s header already says
  why that is fine).

---

## 8. Acceptance

- [x] `date_to_days` and `days_to_date` exactly as §2 — 27 code lines,
      identical with comments stripped — cited in the module header with the
      page, its URL, its date (2021-09-01) and the date read (2026-09-25)
- [x] every division in `src/cal/` by a nonzero literal — a POSITIVE one, as
      C-11 now says (TM-163) — and `check_literal_divisors` live,
      commissioned with **four** plants and their controls, not three: this
      section's third control could not discriminate, and its operand rule
      admitted `y / (400i64 + m)` and `y / 256i64 =>! uint8` (the record,
      step 4)
- [x] intermediates `int64`, widened by the checked `=>` (C-12, amended by
      TM-162)
- [x] the range constants recomputed by `range_constants.npk`, **seen red at
      exit 10 against the old constant** — `-O0 exited 10` and `opt -O2
      exited 10` under `--only` — green after PD-12's correction (TM-161)
- [x] the twenty-six vectors of §4a, re-derived **three** ways (a third forces
      a non-zero 400-year shift, because the second equals the first wherever
      its shift is zero), both directions; and all three new tests shown to
      fail under one-line mutations (the record's table)
- [x] `days_to_date` refuses `int64` MAX and MIN through its `Result` — no
      trap; and with its range lines deleted the same test exits 93, so it
      sees the order
- [x] PD-12's correction at every live site — 36 lines = 25 edited + 2 the
      flag, resolved + 9 history — and TM-014, TM-026 and TM-122 marked
      `SUPERSEDED IN PART by TM-161`; plus two range numbers §1 did not list,
      239 988 and "seven orders of magnitude"
- [x] `cal` still owes **11** — read from `check_failsafe_arms`; the umbrella
      13
- [x] `GREEN -- 78 unit(s), 0 failures; 5 pending`; `check_refs` clean (196
      tracked, after staging); CI green — run `36164292563`, read from its
      own log
- [x] PD-12…PD-15 recorded as TM-161 … TM-164

---

## 9. Decisions drafted at planning

Recorded by the worker in `../../DECISIONS.md` as the next free TM numbers, in
this order, in this subcycle's commit.

**PD-12 — the range constants, corrected.** `NTIME_DAY_MIN` is −4 371 587
(−9999-01-01), the range holds 7 304 484 days, and `NTIME_SECS_MIN` is
−377 705 116 800. Supersedes TM-014 in part (its bounds) and TM-026 and TM-122
in part (their count). The old value was −10000-12-31's day number; it passed
every relation written beside it because each relation was derived from it.
*Alternative:* keep −4 371 588 and widen the range by a day — declined: C-4's
range is years −9999 … +9999, and the constant names its first day.

**PD-13 — `date_to_days` is total and `never fails`; `days_to_date` checks its
range first and builds through `civil_date`.** §2's reasons and alternatives.

**PD-14 — C-11 states its rule without a list, and `check_literal_divisors`
enforces it over `src/cal/`.** The list named `days_from_civil`'s divisors and
the second algorithm made it incomplete on arrival. *Alternative:* extend the
list — declined: the next function falsifies it again, and the check is the
list that cannot go stale.

**PD-15 — 0.1.1's contracts follow Q-6.** Under the recommendation, comments;
under the alternative, `date_to_days`'s one live `ensures` and its arm. Recorded
with whichever answer applied, and the arm cost measured either way.

---

## Execution record

*(Appended as the work is done. The REPORT block is its last entry.)*

### 2026-09-25 — s2, `claude-opus-5-5`, at compiler `c3bdae2`

**Every number below was produced in this session by the command beside it;
none is copied from the plan's dry run.** `$W` is `$REPO/.internal/w011`
(gitignored scratch). `$W/run4.sh <file> <dir>` is `npkc` → `llc -O0
-filetype=obj -relocation-model=static` → `ld.lld -static … npkrt.o` → the
program under `env -i NTIME_HARNESS=1`, each status printed beside the
artefact it should have produced. A harness run is
`NPKC=$WB/.internal/toolchain/c3bdae2/npkc NPKRT=$WB/.internal/toolchain/c3bdae2/npkrt.o python3 harness/run.py`.

#### The start

The board's stream table shows `nitpick-time` `CLAIMED s2`; the tree was clean
at `ccb5978`; `sha256sum` of `npkc` and `npkrt.o` equals
`.internal/toolchain/c3bdae2/SHA256SUMS` (`5fd636b9…`, `162b8975…`). **Q-6 is
still the author's**, and the dispatch's `NOTES:` said to work this plan as
written, which is for A′ — §6's comment form (TM-164). **Hinnant's page was
read directly**, one fetch on 2026-09-25: its date line is 2021-09-01, and
both functions, compared with §2 line by line, differ only in the `unsigned`
intermediates — the deviation C-12 names. The committed functions and §2's
text are identical, 27 code lines each with comments stripped (a `difflib`
comparison of the two).

#### Step 0 — the baseline

```
harness/run.py   (the unchanged tree, ccb5978)       exit 0
  GREEN -- 75 unit(s), 0 failures; 5 pending
check_refs.py $REPO                                  exit 0
  nitpick-time clean (69 md files via git ls-files; leak scan 193 text of 193 tracked)
```

#### Step 1 — the test first, and its red

`tests/unit/range_constants.npk`, then §2's two functions and their two `use`
lines, and nothing else:

```
$W/run4.sh tests/unit/range_constants.npk            npkc 0 (.ll), llc 0 (.o), ld 0 (exe), run exit=10
harness/run.py --only range_constants                exit 1
  FAIL  tests/unit/range_constants.npk
        -O0 exited 10; the header expects 0
        opt -O2 exited 10; the header expects 0
  RED -- 24 unit(s) of 25 passed; 2 failure(s) in 58.8 s: check_denominators, tests/unit/range_constants.npk; 5 pending
```

Exit 10 is the first assertion — `date_to_days` of `civil_date(-9999, 1, 1)`
is not `NTIME_DAY_MIN` — as §4b predicted. `check_denominators` was red for
the new file (`npk_total` 88 → 89), as designed, and a filtered run concludes
nothing either way. The same run already read `src/cal/cal.npk owes 11 arm(s)
over 2 module(s)` with the functions in.

#### Step 2 — the constants (TM-161)

`NTIME_DAY_MIN = -4371587i64`, `NTIME_SECS_MIN = -377705116800i64`, the three
relations in `limits.npk`'s header recomputed with the sentence §5 asks for,
and `limits_named.npk` at `7304484`:

```
$W/run4.sh tests/unit/range_constants.npk            run exit=0   (and limits_named, civil_construct,
                                                                   leap_rule, civil_order, import: 0)
harness/run.py --only range_constants                exit 1
  ok    tests/unit/range_constants.npk                 exit 0, both legs, 0.5 s
  RED -- 25 unit(s) of 25 passed; 1 failure(s) in 58.5 s: check_denominators; 5 pending
```

**The extent, with its denominator** — §1's own command, `git grep -n
'4371588\|4 371 588\|7304485\|7 304 485\|377705203200\|377 705 203 200' --
':!meta/roadmap/0.1/0.1.1.md'`, at `ccb5978`: **36 lines in 16 files** — §1's
count exactly — **= 25 edited + 2 + 9**. The 25: `harness/stages.py` 1, this
cycle's README 3 (0.1.2's two items, the gate), `0.2/README.md` 1,
`ROADMAP.md` 2, `BUILD.md` 1, `CALENDAR.md` 5, `TESTING.md` 1,
`TIME_MODEL.md` 2, `VERIFICATION.md` 1, `nitpick.toml` 1, `src/cal/README.md`
1, `src/core/limits.npk` 4, `tests/unit/limits_named.npk` 2. The 2: the cycle
README's flag on this finding, resolved in place by a sentence after it. The 9:
settled text left as written — TM-014 (three lines), TM-026 and TM-122, each
decision now marked `SUPERSEDED IN PART by TM-161` — and four lines of
`meta/roadmap/done/0.0/`. The same command with `--untracked` afterwards finds
36 again, and reading all 36 finds every one a dated note, a marker or settled
text naming the old value as the old value. A second phrasing
(`4[ ,_]?371[ ,_]?58|7[ ,_]?304[ ,_]?48|377[ ,_]?705[ ,_]?(203|116)|4\.37|7\.30|7\.3 ?m`,
case-insensitive) adds only `7.3 million`/`7.3 M` approximations, still true.

**The fourth witness was in the tree all along.** The second phrasing, run
for the NEW value, found `tests/probe/probe07_negative_div.npk:112`:
`days_from_civil(0i64 - 9999i64, 1i64, 1i64) != (0i64 - 4371587i64)`, under the
comment *"The ends of CALENDAR.md's supported range"* — committed at cycle
0.0.0 (`ef14210`, `git log -S'4371587'`), and asserted green on every run
since 0.0.2. For three weeks the tree held the right first day in an executed
probe and the wrong one in the specification and the constant, and nothing
compared the two. TM-161 records it.

**Two more range numbers, found by the same reading and corrected here**
(TM-161): `TESTING.md` V-2's and this README's month-length count, **239 976**
— 19 998 years × 12, where `[−9999, +9999]` is 19 999 years, so **239 988**
(`python3 -c "print((9999-(-9999)+1)*12)"` → 239988) — and `TIME_MODEL.md`
M-8's *"twenty-seven orders of magnitude to spare"*, which is seven.

#### Step 3 — the vectors and the refusals

**§4a's table re-derived first, three ways** (`$W/vectors.py` and a
companion): `m1` — Python 3.12.3's `date.toordinal()` less the epoch's — for
the 13 rows with a year from 1 to 9999; `m2` — `m1` after a shift of whole
400-year cycles into `[1, 9999]` — for all 26; the two agree wherever both
apply, `rows: 26 mismatches: 0`, exit 0. **But `m2` IS `m1` wherever its shift
is zero**, which is every row whose year is already in `[1, 9999]` — so their
agreement there is by construction. A third, `m3`, forces a NON-ZERO shift for
every row (k = +1, +2, +25 or −1): `rows: 26 mismatches: 0`, exit 0. Every value
in §4a is therefore reached by two different ordinals, and equals the plan's.
The same script gives the constants: `NTIME_DAY_MIN = -4371587`,
`NTIME_DAY_MAX = 2932896`, `days = 7304484`, `SECS_MIN = -377705116800`,
`SECS_MAX = 253402300799`.

**`day_number_vectors.npk` is a `never fails` helper called twenty-six times**,
not a loop (no measure, no `DecreasesViolated`), and **both new tests read a
`Result` the way the compiler insists**: `is_error` first, then `.value`.
Measured before relying on it (`$W/spell/`): the checked form compiles and
runs, exit 0; the same read with the check deleted is `npkc` exit 1, no `.ll`,
`NITPICK-TAINT-001 … this Result has not been checked for an error, so its
value carries the unknown taint`. So a REFUSAL exits with its own row's or
assertion's code instead of the anonymous `ETimeValue` arm — which is what made
the mutation codes below readable. (`probe08_readlink.npk` already reads a
`.value` this way.)

```
$W/run4.sh tests/unit/day_number_vectors.npk          npkc 0, llc 0, ld 0, run exit=0
$W/run4.sh tests/unit/days_to_date_refused.npk        npkc 0, llc 0, ld 0, run exit=0
```

**The three tests were shown able to fail** — `$W/mut/<name>/`, a copy of
`src/` and the three tests with ONE change each, every test through
`run4.sh`:

| Mutation | `range_constants` | `day_number_vectors` | `days_to_date_refused` |
|---|---|---|---|
| none (the control) | 0 | 0 | 0 |
| `limits.npk` as it was (`git show HEAD:src/core/limits.npk`) | **10** | 0 | 0 |
| row 22's expected day number one low | 0 | **52** — row 22, forward | 0 |
| `date_to_days`' `era` correction deleted (`y / 400i64`) | 0 | **32** — row 12, forward | 0 |
| `days_to_date`'s `era` correction deleted (`z / 146097i64`) | **15** | **33** — row 12, backward | 0 |
| the March shift wrong (`mp < 9i64`) | **16** | **13** — row 2, backward | 0 |
| `days_to_date`'s two range lines deleted | 0 | 0 | **93** — `IntOverflow` at `int64` MAX |

Every mutation is caught, by the test the table names. Two readings are worth
keeping. `range_constants` cannot see a deleted `date_to_days` correction,
because −10000 is a multiple of 400 and truncation and floor agree there — so
the vectors are not redundant with it. And a deleted `days_to_date`
correction does not produce a wrong DATE: it produces day 0 of a month, which
`civil_date` refuses — §2's *"an algorithm error becomes a refusal the 0.1.2
sweep sees"*, measured rather than argued.

#### Step 4 — `check_literal_divisors` (TM-163)

A live tree check in `harness/checks.py`, registered in `checks.LIVE` after
`check_constants_named`. Against the tree: `17 division(s) over 1 file(s) in
src/cal/, every divisor a positive integer literal` — §3's seventeen, the
`%` 4, 100, 400 and 7 and the `/` of both algorithms.

**It reads code with comments AND strings blanked, and the plan's premise for
that was false.** §3 says *"`code_lines` (comments and string bodies
blanked), as `check_civil_literal` did"*. `code_lines` has never blanked a
string — read at `2589069`, `dcc8c72` and `3b319d3`, it is `strip_comments`
alone — although the retired check's docstring said so. Taken literally the
check would fail `cal.npk` on its five `use "../core/limits.npk"` lines.
`strip_strings` already existed, in `harness/arms.py` for the S-6 generator;
it moves to `checks.py` beside `strip_comments` and `arms.py` imports it, so
one definition of a string serves both. `code_lines` is unchanged, and its
docstring now says what it does.

**Three departures from §3's text, each measured before it was made:**

1. **The operand rule.** §3's *"after optional whitespace and optional `(`,
   `[1-9][0-9]*(i|u)(8|16|32|64|128)\b`"* accepts `y / (400i64 + m)` — the
   literal after the `(` matches and `\b` holds — and `y / 256i64 =>! uint8`,
   whose divisor is 0, since a cast binds tighter than `/` (the compiler's
   `OP_REFERENCE.md` §0). The check takes the literal only when it stands
   alone: no `(`, and nothing after it that binds tighter than `/` (`=>!`,
   `=>`, `|>`, `<|`, `.`, `?.`, `(`, `[`). `y / (400i64)` is refused too, the
   safe direction; no such spelling is in the tree.
2. **The third control.** §3's, `y +% 1i64` beside `/ 4i64`, cannot tell the
   right reading of `+%` from the wrong one: a check that took `+%` for a
   remainder would be dividing by the nonzero `1i64` and stay silent as well.
   Measured on the two fixtures with a deliberately naive reader: silent on
   `(y +% 1i64) / 4i64`, firing on `(y +% m) / 4i64`. The row's control is the
   second, and its plant is the same text without the `+`.
3. **A fourth row**, `y / 256i64 =>! uint8` against `y / 256i64`, because the
   tighter-binding rule is a branch and a branch never seen red is not
   commissioned (TM-126). The self-check reads **20** planted and 20 controls,
   not the 19 that three rows would have made.

**Every one of the eight fixtures is `selfcheck._div_tree(expr)`**: the
function under test beside a `use "../core/limits.npk"` path and a comment,
each holding a `/` and a `%`, so every silent control also proves the check
reads neither. Measured on the first row's control with the blanking disabled
by hand: strings not blanked → 2 findings (`core`, `limits.npk`); comments not
blanked → 5. And `selfcheck.part_b`, run on its own before the harness:
`PLANTED rows: 17  TREE_PLANTS: 20`, 0 problems — every row red on its plant
with its needle, silent on its control.

#### Steps 5–7 — the header, the umbrella, the contracts (TM-164)

`src/cal/cal.npk`'s header cites the page, its URL, its date and the date
read, per `CURRENCY.md`; lists the eight public functions; says the module has
no loop and what the first one would cost; and keeps the two sentences it
removes as a dated quotation. `src/lib.npk` re-exports `date_to_days` and
`days_to_date` after `days_in_month`: **48 → 50** (`grep -c '^pub use '` 50,
and `check_denominators`' `measured:` line `lib_reexports=50`). The contracts
are comments in §6's form, and `VERIFICATION.md` §6's two rows say so.

**The arm cost, measured both ways** (`$W/q6/`, a program importing the
module with `main` and no `failsafe`):

```
arm_cal_base_r2.npk   (as committed)             npkc 1, no .ll   NITPICK-REACH-003 -- 11 identities
arm_lib_base_r2.npk   (as committed)             npkc 1, no .ll   -- 13 identities
arm_cal_tree_r2.npk   (date_to_days with §6's live `ensures`)     -- 12 identities, the extra EnsuresViolated
arm_lib_tree_r2.npk   (the same)                                  -- 14 identities, the extra EnsuresViolated
```

— §6's prediction exactly. The first attempt at the live-`ensures` pair wrote
each `use` path one directory short, and both were `NITPICK-RESOLVE-005`,
`npkc` exit 1 — the same status a REACH refusal has. The diagnostic said so;
the status could not have. Re-run with the path corrected, as above.

#### Step 8 — the documents

`CALENDAR.md` §2's table and C-16 (TM-161, dated notes), C-10's citation, C-11
(TM-163) and C-12 (TM-162); `TIME_MODEL.md` M-8 and §8's table;
`VERIFICATION.md` §2, P-6's `DayNumber` and §6's two rows; `TESTING.md` V-2's
table, §2's new row, V-1a's arithmetic (**18 rows = 14 live + 4 pending**) and
V-14c's table; `SAFETY.md` S-16 and S-4b (TM-163); `BUILD.md` B-5's
`sweep-count` example and B-7b's parse split; `DECISIONS.md` TM-161 … TM-164
and the three markers; `OPEN_QUESTIONS.md` Q-6's note; `CLAUDE.md` (a status
paragraph, the check count); `harness/README.md`, `run.py`'s header, the CI
workflow's header; `ROADMAP.md`, this cycle's README and `0.2/README.md`;
`src/cal/README.md`, `limits.npk`, `lib.npk`, `leap_rule.npk`, `import.npk`.

**Found by reading, beyond the plan's list, and corrected here:**

- **C-12's figure and last clause** (TM-162): *"`era * 146097` at year −9999
  is about −4.4 × 10⁶"* is the day number, not the product (−3 652 425, `era`
  −25), and *"gets the range check's error"* promised `date_to_days` a range
  check it does not have.
- **S-16's closed list** (TM-163): *"divide by literals (4, 100, 400, 146097,
  86400, 1000000000) and nothing else"* — `civil_from_days` adds six more and
  `weekday_number_sunday_first` a seventh. The six stay named, because
  `check_constants_named`'s owner map cites S-16 for 86400 and 1000000000.
- **S-4b's prediction of `cal`** (TM-163): *"divides by 4, 100, 400, 146097,
  86400 and 1000000000 (S-16), indexes the month and zone tables"* — `cal`
  divides by neither of the last two and indexes no zone table.
- **`VERIFICATION.md` §2**: a "nonzero literal" discharges the zero and not,
  on its own, `MIN / −1`; C-11's "positive" closes both.
- **`src/cal/README.md`** said a calendar-only consumer *"owes exactly one
  `failsafe` arm"* — the identity half of an eleven-arm bill, stale since
  0.1.0 measured nine.
- **Two counts in `harness/`**: `harness/README.md`'s *"Nine planted
  violations"* (true at 0.0.3; 16 before this subcycle, 20 after) and
  `run.py`'s *"by eight checks"* (true until this subcycle; nine now).
- **`leap_rule.npk`** and **`is_leap_year`'s comment** said 0.1.1 *"still
  owes"* the `era` correction; it is delivered.

#### Step 9 — the denominators and the full run

The first full run was red exactly where designed:

```
harness/run.py        exit 1
  RED -- 78 unit(s) of 78 passed; 1 failure(s) in 82.9 s: check_denominators; 5 pending
  report: measured: defect_total=24  lib_reach=5  lib_reexports=50  npk_elsewhere=0  npk_src=10
          npk_tests=81  npk_total=91  probe_dir=39  probe_error=13  probe_exit=26  probe_nomarker=0
          suite_roots=54  support_total=3  tests_error=24  tests_exit=54  tests_nomarker=3
```

Five tagged sites, each moved from that line and not retyped: `npk_total` 88 →
**91** (`nitpick.toml`, `harness/run.py`, `BUILD.md`, `TESTING.md` §1) and
`suite_roots` 51 → **54** (`run.py`); `lib_reexports` 50 had been set at step
6 and passed. **Untagged numbers beside them, moved with them** (TM-142's
stated limit): "29 … by nothing else" stays 29 (91 − 5 − 54 − 3), and
`BUILD.md` B-7b's split is now **91 = 67 + 22 + 2**, the run's own
`67 parse cleanly, 22 parse and are refused by a later phase, 2 must not parse
and do not`. Then:

```
harness/run.py        exit 0
  ok    the tree checks                                20 planted violation(s) caught, 20 clean control(s) silent
[4/9] expect- header sweep -- 91 .npk in the tree = 10 src/ + 81 tests/ + 0 elsewhere
      defect corpus: 24 file(s) = 0 exempt (verdict re-derived above) + 24 asserted (13 run, 11 refusal, 0 unowned)
[5/9] tree checks -- 11 live, 4 pending
  ok    check_literal_divisors   17 division(s) over 1 file(s) in src/cal/, every divisor a positive integer literal
        report: src/lib.npk owes 13 arm(s) over 5 module(s): …
        report: src/cal/cal.npk owes 11 arm(s) over 2 module(s): …
      unit: 14 of 14 file(s) -- 14 run, 0 refusal
GREEN -- 78 unit(s), 0 failures; 5 pending
```

**`cal` still owes 11, read from `check_failsafe_arms`, and the umbrella 13.**
`78 = 75 + 3`, the three new unit tests; `78 = 24 + 54` by stage.

#### What it cost

Three full harness invocations at `c3bdae2` — the baseline (GREEN, 75), one
red on `check_denominators` alone as designed, and the final (GREEN, 78) —
plus two `--only range_constants` runs, one red at exit 10 and one green.
Scratch: 7 mutation trees × 3 tests (after an earlier round of 4 × 2, before
the tests read `.value`), 4 arm-bill programs and the 2 with the short path,
2 `.value` spellings, the vectors script and its `m3` companion. **Wall clock
deliberately not quoted** (`0.1.0.md`'s reason); the unit count, **75 → 78**,
is the number to carry. `journalctl -u earlyoom --since today`: SIGTERMs at
12:51, 12:54 and 12:56 to `traefik`, `coredns` and three processes named
`clam` (38–61 GiB RSS) — none of this subcycle's; every step here ended with
the status it was expected to have, and none died by signal. Tokens: not
reported by this harness.

#### For whoever works here next

- **0.1.2 — the sweep, the cycle's gate — has no execution-grade file.** The
  cycle README's checklist for it now carries the corrected counts,
  **7 304 484** each way and **239 988** (year, month) pairs, and §4 here
  records that `for (int64:i in lo..hi)` is inclusive and needs no measure
  and no arm, while a `till`/`loop` arms `BadStep` (DEF-95's spurious demand,
  droppable at its notice).
- **The mutation matrix above is cheap and it is the argument for the three
  tests**; a sweep at 0.1.2 deserves the same — a mutant that the round trip
  catches and the vectors do not is the proof the gate adds something.
- **If the author answers Q-6 with A**, §6's live `ensures` costs `cal` 12
  and the umbrella 14, measured here, and the arm's code is **117**, not
  §6's 110 (the orchestrator's table; 110 is an ordinary failure exit in
  `nitpick-regex`). TM-164 says so.
- **`strip_strings` lives in `checks.py` now.** A check that must not read
  string bodies calls it; `code_lines` blanks comments only.

#### The decisions, drafted at planning and recorded in the work commit

| Draft | Decision | What it settles |
|---|---|---|
| PD-12 | TM-161 | the range's first day −4 371 587, 7 304 484 days, `NTIME_SECS_MIN` −377 705 116 800; the month-length count 239 988; M-8's "seven"; TM-014, TM-026 and TM-122 marked |
| PD-13 | TM-162 | `date_to_days` total and `never fails`; `days_to_date` range-first and built through `civil_date`; C-12 amended |
| PD-14 | TM-163 | C-11 without a list, "positive"; `check_literal_divisors` live, four rows; S-16, S-4b and `VERIFICATION.md` §2 amended; the three departures from §3 |
| PD-15 | TM-164 | the contracts are comments (A′); the live-`ensures` cost measured, and its arm's code 117 |

#### CI on the pushed work commit — run `36164292563`, green, read from its own log

`d83e9ea` pushed to `origin/main`; `gh run watch 36164292563 --exit-status` →
exit 0; the job ran 16:59:44 → 17:01:38 UTC. **Read in the log** (`gh run view
36164292563 --log`), each line the log's own text with its timestamp and colour
codes removed, the harness line cut after `5 pending` where it goes on to the
wall clock:

```
compiler HEAD == c3bdae270d63c93ab6e89825fddeec9425e2c6fd (clean)
Cache restored from key: npkc-c3bdae270d63c93ab6e89825fddeec9425e2c6fd-llvm-20.1.2
npkc   CI: 0cbc150ced5d20f78b69e89e24d5d50fd38e53ecb7a22a4f268f71c91d4dc3d3
npkc   WB: 5fd636b9ab557c19a0738e6f316f3347cc4ed29dd9a77f72199d6380c0c36323
npkrt  CI: 162b897539285a773a6a1a0329750e148a6c9590b45dda2d017704743b591824
npkrt  WB: 162b897539285a773a6a1a0329750e148a6c9590b45dda2d017704743b591824
built from compiler c3bdae2 at LLVM 20.1.2
  ok    the tree checks                                20 planted violation(s) caught, 20 clean control(s) silent
      defect corpus: 24 file(s) = 0 exempt (verdict re-derived above) + 24 asserted (13 run, 11 refusal, 0 unowned)
[5/9] tree checks -- 11 live, 4 pending
  ok    check_literal_divisors   17 division(s) over 1 file(s) in src/cal/, every divisor a positive integer literal
  ok    check_failsafe_arms      7 public module(s) in src/, 7 row(s) generated and diffed against NITPICK-REACH-003
      67 parse cleanly, 22 parse and are refused by a later phase, 2 must not parse and do not, 0 failed
      probe: 39 of 39 file(s) -- 26 run, 13 refusal
      unit: 14 of 14 file(s) -- 14 run, 0 refusal
      conformance: 1 of 1 file(s) -- 1 run, 0 refusal
GREEN -- 78 unit(s), 0 failures; 5 pending
```

The unqualified `GREEN` line at the `c3bdae2` digests, on a cache hit;
`npkc`'s digest differs from the workbench's and `npkrt.o`'s does not, as at
0.1.0b and 0.1.0c. The one annotation is the same as 0.1.0c's — *"Node.js 20
is deprecated … actions/cache@v4, actions/checkout@v4"* — a maintenance item
for the workflow's action pins, not this subcycle's. **The work commit's title
line reads RUNNING and this record commit sets DONE**, 0.1.0b's two-commit
shape, because a CI result cannot live in the commit that caused it.

#### REPORT

REPORT nitpick-time 0.1.1
status: DONE
stream: s2
model: claude-opus-5-5
toolchain: c3bdae2
commits:
  - d83e9ea cycle 0.1.1: Hinnant's algorithms, and NTIME_DAY_MIN corrected by the test that recomputes it
  - HEAD cycle 0.1.1: the record — CI run 36164292563 at c3bdae2
harness: NPKC=$WB/.internal/toolchain/c3bdae2/npkc NPKRT=$WB/.internal/toolchain/c3bdae2/npkrt.o python3 harness/run.py -> GREEN -- 78 unit(s), 0 failures; 5 pending
  (wall clock deliberately not quoted, 0.1.0.md; CI run 36164292563 printed the same line)
check: clean
record: meta/roadmap/0.1/0.1.1.md — DONE (2026-09-25)
next: n/a
findings-for-playbook:
  - an asserted value in a test and the same quantity stated in a spec are two lists nobody diffs: nitpick-time's probe07 asserted the range's first day as -4 371 587 from cycle 0.0.0 while CALENDAR.md and the public NTIME_DAY_MIN said -4 371 588, under a green suite, for three weeks (TM-161)
  - relations checked among numbers derived from one wrong number all hold; only a recomputation from an independent algorithm catches it, and it must be seen red at the old value before the value moves (TM-161)
  - a control must be a case the WRONG implementation gets wrong: `y +% 1i64` as the control for "+% is not a division" also passes a checker that misreads +%, because 1i64 is a valid divisor either way; `y +% m` discriminates (TM-163)
  - a lexical "literal divisor" rule that allows an optional `(` and ends at `\b` admits `/ (400i64 + m)` and `/ 256i64 =>! uint8`, which is 0; the literal must stand alone, with nothing after it that binds tighter than `/` (the compiler's OP_REFERENCE §0)
  - a helper's docstring is a claim to measure: nitpick-time's code_lines never blanked string bodies, though a retired check's docstring and a reviewed plan both said it did
  - a two-method re-derivation whose second method equals the first on part of its domain (a 400-year shift of zero) is one method there; force the second to differ
  - a test reads Result.value after testing is_error (the compiler's NITPICK-TAINT-001 refuses it otherwise), so a refusal exits with the test's own code instead of the anonymous identity arm, which makes mutation results readable
open-questions-raised: none
for-the-author:
  - 0.1.2 (the sweep, the cycle's gate) has no execution-grade file; recommend a planner dispatch, or a ruling to work it from the cycle README's checklist, which now carries the corrected counts (7 304 484 each way, 239 988 month pairs) and 0.1.1.md §4's `for (int64:i in lo..hi)` finding
  - Q-6 is still open; 0.1.1 was worked on A' as the dispatch said (TM-164). If the author answers A, the live `ensures` on date_to_days costs cal 11 -> 12 and the umbrella 13 -> 14 (measured), and its arm exits 117, not the plan's 110
  - check_literal_divisors departs from 0.1.1.md §3 in three measured places (the operand rule, the +% control, a fourth plant: 20 planted, not 19); recommend the verifier read §3 against TM-163 rather than against the count "three"
compiler-defect: none
tokens-and-time: unknown
