# 0.0.4 — `src/core/` — PLANNED

> **Scope:** `Vec<T>`, `Bytes` and `limits.npk`. The storage primitives TM-005
> decided to own rather than import, plus the one file every named bound lives
> in.

## 1. Decisions taken at planning

- **P-23 — `Vec<T>`'s block is `wild`, its lifetime its owner's scope.** One
  `=>!` reinterpretation, in one file, checked once — the shape that survives.
  The compiler's 1.4.2 found five latent unchecked pointer reinterpretations in
  exactly the hand-written-per-family shape this replaces. **Every `Vec` has a
  `vec_free` and every scope that made one calls it**, or `exit 0` traps under
  D-151, which is exactly the enforcement wanted — **for the BLOCK. Read the
  note under §2's API table before relying on this sentence:** D-151 watches
  `wild` allocations, so it does not see an orphaned *element*, and P-23 is
  true only of the block (TM-106, `SAFETY.md` S-18b).
- **P-24 — `Bytes` is over `buffer`, not `wild`.** A `Bytes` is per-call
  scratch for a formatter, so the managed regime's scope-exit drop is right and
  there is nothing to pair. This differs from `Vec<T>` deliberately, and the
  difference is that one is a container the caller keeps and the other is a
  builder the caller consumes.
- **P-25 — `put_uint` is allocation-free**, writing decimal digits backwards
  into a stack array and appending. Every format emits numbers, and
  `int_to_string` allocates a `string` per call — the shape the compiler
  measured as quadratic in `npkg`'s first full run, seventeen minutes of
  fifty-six spent in the kernel.
- **P-26 — `put_int` is written and tested against `int64` MIN first.**
  Negating the most negative value overflows, and D-210 makes that a trap
  rather than a wrap — so the naive `if (v < 0) { v = -v; }` is a *crash* on
  one input. The implementation accumulates in the negative direction instead.
  This is the single most likely defect in this subcycle and it has one test
  before it has an implementation.
- **P-27 — `ntime` declares no map.** The zone lookup is a binary search over a
  sorted index (`ZONE_MODEL.md` Z-9), which at 447 entries beats a hash and has
  one invariant instead of four. If something later wants a map, that is a
  decision with a measurement behind it.

## 2. `Vec<T>`

```nitpick
pub struct:Vec<T> = { wild T->:items; int64:count; int64:cap; };
```

| Function | Contract (written as a comment now, `VERIFICATION.md` P-1) |
|---|---|
| `vec_init<T>(cap)` | `ensures result.count == 0 && result.cap >= 1` |
| `vec_reserve<T>(v, need)` | `ensures v.cap >= v.count + need` |
| `vec_push<T>(v, move T:x)` | `ensures v.count == old(v.count) + 1` |
| `vec_pop<T>(v)` | `requires v.count > 0` |
| `vec_at<T>(v, i)` | `requires i >= 0 && i < v.count` |
| `vec_set<T>(v, i, move T:x)` | `requires i >= 0 && i < v.count` |
| `vec_truncate<T>(v, n)` | `ensures v.count == min(n, old(v.count))` |
| `vec_clear<T>(v)` | `ensures v.count == 0` |
| `vec_free<T>(v)` | `ensures v.cap == 0 && v.items == NULL` |

> **Three of those entries discard elements and none of their postconditions
> says so** — `vec_free`, `vec_clear` and `vec_truncate` speak only of `count`,
> `cap` and `items`. At an **owning** `T` that is a leak, and **`exit 0` does
> not catch it**: D-151 watches `wild` allocations and a `string`'s body is
> managed. Cycle 0.0.0's probe 06 measured 125 MiB retained over 2 000 000
> elements at exit 0, and `HeapOom` under a 64 MiB cap. **TM-106** and
> `SAFETY.md` **S-18b** are the rule; this subcycle is where it is built, so
> each of the three needs an element-drop path for an owning `T` or a stated
> restriction to non-owning ones — and P-23's "or `exit 0` traps under D-151"
> must be read as being about the block alone.
>
> The shape is committed as `free_names` in `tests/probe/probe06_generic_vec.npk`.

**Rule:** `vec_at` returns by value and is therefore for scalar `T` only — for
an owning `T` a copy is refused (TYPE-046), and such a family reads through
`v.items[i]` at the site that knows the element's shape. This is the compiler's
own `List<T>` note and the reason there is no generic `list_at` there either.

## 3. `Bytes`

```nitpick
pub struct:Bytes = { buffer:body; int64:len; };
```

`init`, `push`, `extend`, `extend_str`, `put_uint`, `put_int`, `len`, `view`
(a borrowed `uint8[]`), `take` (an owned `string`), `clear`, `free`. Growth
doubles.

## 4. `limits.npk`

Every named bound in one file, each with the specification rule that set it.
At this subcycle the list is short and grows per cycle:

```
NTIME_PARSE_MAX        128    FORMAT_MODEL.md F-16
NTIME_PATH_MAX        4096    HOST.md H-15
NTIME_YEAR_MIN       -9999    CALENDAR.md C-4
NTIME_YEAR_MAX        9999    CALENDAR.md C-4
NTIME_DAY_MIN     -4371588    CALENDAR.md §2
NTIME_DAY_MAX      2932896    CALENDAR.md §2
NTIME_SECS_MIN  -377705203200 CALENDAR.md §2
NTIME_SECS_MAX   253402300799 CALENDAR.md §2
NTIME_NANOS_PER_SEC 1000000000
NTIME_SECS_PER_DAY       86400
NTIME_OFFSET_MAX         64800    ZONE_MODEL.md Z-19
```

`check_constants_named` (0.0.3) enforces that none of these numbers appears
anywhere else — and cycle 0.1 adds a test that **recomputes** the four range
constants from the calendar algorithm rather than trusting the literals.

## 5. Steps

1. `limits.npk`, with the specification citation per constant. Commit.
2. `Vec<T>` with its suite: both `T` shapes, every accessor at its boundary
   (`i == 0`, `i == count-1`, `i == count`), growth from capacity 1 to 100 000,
   and a leak test that exits 0 so a missed `vec_free` traps. Commit.
3. **`put_int` against `int64` MIN, first** (P-26), then the rest of `Bytes`:
   `put_uint` at 0, 1, 9, 10, 99, 100 and `uint64` max; the million-byte append
   with a bounded reallocation count. Commit.

## 6. Acceptance

- [ ] the two primitives exist with the operations above
- [ ] every accessor has a boundary test at `0`, `count-1` and `count`
- [ ] **`put_int` is correct at `int64` MIN** and the test was written first
- [ ] `put_uint` allocation-free, verified by reading the emitted IR for a call
      to the allocator
- [ ] the leak tests exit 0, so a missing **`vec_free`** is a trap and not a
      pass — **and this gate is about the BLOCK alone.** `exit 0` traps under
      D-151 on a live `wild` allocation, which is what `Vec<T>`'s block is; it
      says **nothing** about a managed body, so an unemptied `Vec<string>`
      passes it (TM-106, `SAFETY.md` S-18b). Corrected 2026-09-04: this line
      read *"a missing free is a trap and not a pass"* unqualified, which is
      unfalsifiable for managed memory — D-151 counts `wild` blocks and D-188
      counts live drivers, and neither sees a managed body
- [ ] **the element leak has its own gate, and it is a memory cap rather than
      an exit code**: `tests/probe/probe06b_element_leak.npk` and
      `probe06c_element_drop.npk` are the committed pair, differing in one
      line, and **both exit 0**. Measured 2026-09-04 at pin `94874ce`: the
      leaking half peaks at 125 184 KiB and takes **`HeapOom`, exit 92**, under
      `ulimit -v 65536`; the corrected half finishes clean in **under 768 KiB**
      of address space. Assert the cap, not the exit code
- [ ] `Bytes` growth amortised linear, measured
- [ ] every contract written as a comment in the syntax it will take, with a
      property test standing in
- [ ] `check_constants_named` green and **seen to fail** against a planted
      magic number

---

## Execution record
