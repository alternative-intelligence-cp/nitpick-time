# 0.1.4b — the managed-memory gate — RUNNING (since 2026-09-25, s2)

> **Scope:** `0.1.0.md` §8's gate, carried from cycle 0.0 and owed since the
> runtime at the pin turned out to carry its instrument (the cycle README's
> *"Why 0.1.4b exists"*). **The harness asserts MANAGED memory for the first
> time.** A `// heap:` marker holds the runtime's own `NPK_HEAP_STATS` report —
> `heap: allocated=N peak_live=N count=N` — to a file's bounds on both legs,
> and a `// cap:` marker runs a program again under TM-131's 64 MiB
> address-space cap. **Six files, not "four pairs"**: §8's four are two twin
> pairs and two single `Bytes` tests. Each leaking twin's high-water mark is
> held at or above its leak's arithmetic, so the instrument meets a known leak
> on every run; each remedy's is held under a ceiling AND its allocation count
> over a floor, so a remedy emptied of its work is red, not green; each `Bytes`
> test's is held under a ceiling its leak mutant exceeds. Every bound is seen red
> on a wrong implementation (§7), and the self-check gains a ninth case (§5.4).
>
> **Found at planning, by reading the runtime's own output against the source —
> two things §8 did not know.** (1) **TM-131's control no longer controls**: at
> `c3bdae2` a program that allocates nothing needs about 10.5 MiB of address
> space before `main`, where `/bin/true` needs 2.75 MiB, so the belt's control
> is that floor program, built by the same toolchain (§3, PD-37). (2) **A
> library defect**: `bytes_take` hands back a VIEW of the sink typed as an
> owned `string` — reused or grown, the sink rewrites or frees the caller's
> text, measured both ways — and has since cycle 0.0.4. It was found because
> the instrument counted **25** allocations where `bytes_growth`'s source makes
> **26** (§4). This subcycle fixes it (PD-39): the gate's numbers for
> `bytes_growth` depend on it, and the fix is two lines.
>
> **Written 2026-09-25 by the stream-2 planner, against compiler `c3bdae2` and
> the tree at `648590f` (0.1.4's record), and REHEARSED IN THE REAL CHECKOUT**:
> every fenced block of §6, §7, §10 and §11 was extracted from this file by §0's
> own mechanism and applied to `$REPO`, every command of §9 was run there, the
> harness ended `GREEN`, and the tree was put back to `648590f` exactly. §16 says
> which command ran where. **The worker re-derives every number below with the
> command beside it, and does not copy it.**
>
> **The pin will probably move before this runs.** The compiler lands its
> 1.6.0 steps 3g and 3h, and the workbench re-pins to the commit carrying 3h.
> §11 names every number this plan rests on, says which would move with the pin
> and why, and step 1 re-derives all of them before anything is changed. The
> runtime — the instrument's home — is byte-identical from `c3bdae2` to the
> compiler's head at planning, `6fb85d3` (step 3f); step 0 confirms that the
> dispatched pin's `npkrt.o` is the same anchor.

**Gate for starting** — every one checked, not assumed:

1. `0.1.4.md` is `DONE` and verified, and CI is green on it — run
   `36210527554` (it is: the workbench's `RECORD.md` records the verifier
   `s2-ntime-0.1.4-verify-2210` returning PASS, at 22:17).
2. `git -C "$REPO" status --porcelain` is empty, and `HEAD` is this plan's
   commit or a descendant of it. **At pin `c3bdae2`** a descendant must have
   touched nothing under `src/`, `tests/`, `harness/`, `tools/`, `.github/`,
   `meta/specs/`, `meta/roadmap/0.4/`, `meta/roadmap/ROADMAP.md`,
   `meta/DECISIONS.md`, `CLAUDE.md` or `nitpick.toml`; if one did, stop. **At any other pin** the descendants are the adoption subcycle's:
   go on to step 0, and let step 0's anchor, step 1's diff, `apply.py`'s
   exactly-once rule and step 3's block decide (§11).
3. The dispatch's pin: `( cd "$WB/.internal/toolchain/$PIN" && sha256sum -c
   SHA256SUMS )` prints two `OK`s, and **the anchor is unchanged** — `npkrt.o`
   is 72 576 bytes with `sha256` `162b8975…` (step 0 prints all 64). If the
   anchor moved, **stop**: every number in §2, §3 and §6.3 is the runtime's.
4. **`../../OPEN_QUESTIONS.md` Q-6 is answered — A′, by the author at 15:56 on
   2026-09-25.** Nothing here writes a contract. **`VERIFICATION.md` P-1's
   replacement by a numbered decision is 0.1.3c's, by the orchestrator's
   decision, and is NOT folded in here.**
5. **Currency: nothing to check.** This plan depends on nothing outside the
   compiler tree. `NPK_HEAP_STATS` is the runtime's, read from its source at the
   pin and measured there — a language fact, never researched on the web
   (the research skill's §9) — so `../../research/CURRENCY.md` is unchanged.
6. **The draft decisions PD-35 … PD-39 (§14) are the next five TM numbers.**
   Read the highest declared: `grep -o '^### TM-[0-9]*'
   "$REPO/meta/DECISIONS.md" | tail -1` — at planning `### TM-183`, so PD-35
   becomes the number after it and the rest follow in order. If it reads
   higher, number from there and say so in the record. **This file never
   writes the five numbers out**: `check_refs` reads a TM number in a roadmap
   file as a citation, and before step 5 none of them is declared.
7. **The order is the orchestrator's: 0.1.4b before 0.1.3c.** If 0.1.3c has
   landed first, stop: every `old` text of §6 and §10 that it also edits, and
   every count here, was measured on the tree without it.
8. **`earlyoom` is live** (the workbench board's hazard 12): read `journalctl
   -u earlyoom --since today` before the first run, and after any step that
   dies by signal. A kill is a stop, never a retry.

---

## 0. Read these first, and set these once

1. [`0.1.0.md`](0.1.0.md) §8 — the gate as it was carried — and the cycle
   README's *"Why 0.1.4b exists"* ([`README.md`](README.md)).
2. [`../../specs/SAFETY.md`](../../specs/SAFETY.md) S-18b, S-18c and S-18e;
   [`../../specs/TESTING.md`](../../specs/TESTING.md) V-1e, V-1f, V-1h, V-1j,
   V-14 and V-14b; [`../../specs/BUILD.md`](../../specs/BUILD.md) B-5 and B-12.
3. [`../../DECISIONS.md`](../../DECISIONS.md) TM-106, TM-120, TM-121, TM-127,
   TM-131 (the pair, and the control this plan moves), TM-139, TM-142, TM-150.
4. [`0.1.4.md`](0.1.4.md) §0 (the apply mechanism, reused here) and §7 (a
   matrix read by its named lines).
5. The compiler at the pin, read with `git show` only, never checked out:
   `git -C "$WB/../nitpick" show c3bdae2:runtime/npkrt.ll` for `npk_hs_arm`,
   `npk_hs_report`, `npk_hs_note_alloc`, `npk_hs_note_free`,
   `npk_hs_note_resize`, `npk_exit`'s `leave` block and
   `npk_string_from_bytes`; `c3bdae2:meta/specs/BUILTIN_REFERENCE.md` for the
   `Views` column and the `string_concat`, `string_slice` and
   `string_from_bytes` rows; and `c3bdae2:bootstrap/harness/harness.py`'s
   `cost` stage, the precedent PD-35 declines.

**The variables.** A Bash call keeps no variables, so they live in a file.
Once, with `REPO` set to the dispatch's absolute `REPO` and `PIN` to its
`COMMIT` — `c3bdae2` at planning:

```bash
mkdir -p "$REPO/.internal/w014b"
cat > "$REPO/.internal/w014b/env.sh" <<'EOF2'
export REPO=$(realpath "$(dirname "${BASH_SOURCE[0]}")/../..")
export WB=$(realpath "$REPO/..")
export PIN=c3bdae2
export NPKC=$WB/.internal/toolchain/$PIN/npkc
export NPKRT=$WB/.internal/toolchain/$PIN/npkrt.o
export W=$REPO/.internal/w014b
export CHECK=$WB/skills/check/scripts
EOF2
```

— writing the dispatch's `COMMIT` for `c3bdae2` in `PIN` if it differs — and
then **at the top of every shell**, with the dispatch's absolute `REPO`
written out: `source <REPO>/.internal/w014b/env.sh`. `$W` is gitignored
scratch (`.internal`, the harness's `WALK_SKIP`); every script this plan uses
lives there and none is committed — **its text is committed, here**.

**The text is applied from this file, not typed from it.** Every block the
worker writes into the tree is a fenced block below headed by a marker line —
`apply GROUP: old PATH` then `apply GROUP: to PATH` for a replacement, in the
groups `code` (§6), `docs` and `decisions` (§10) — and `script: NAME` for a
script; `apply.py` applies one group in document order, each old text asserted
to occur EXACTLY ONCE, stopping at the first that does not. **A tag is
spelled `{{sweep: name=N}}` in this file** and `apply.py` writes it
`[[sweep: name=N]]` into the tree: a whole tag in a roadmap file is a claim
`check_denominators` holds the tree to the moment the file is saved, and the
hunks carry tags for two denominators that do not exist until step 2 — the
first rehearsal went red at step 0 on exactly that. Extract `apply.py`
itself, then every script:

```bash
python3 - "$REPO/meta/roadmap/0.1/0.1.4b.md" "$W" <<'EOF2'
import re, sys
text = open(sys.argv[1], encoding="utf-8").read()
m = re.search(r"^<!-- script: apply\.py -->\n(`{3,})[a-z]*\n(.*?)\n\1\n", text, re.S | re.M)
open(sys.argv[2] + "/apply.py", "w", encoding="utf-8").write(m.group(2) + "\n")
print("apply.py:", m.group(2).count("\n") + 1, "lines")
EOF2
cd "$REPO" && python3 "$W/apply.py" scripts
```

Expected: `apply.py: 69 lines`, then five `script` lines in document order —
`apply.py`, `mut.py`, `renumber.py`, `same.py`, `baseline.py` — and
`scripts written: 5`.

<!-- script: apply.py -->
```python
#!/usr/bin/env python3
"""0.1.4b's text, applied from THIS PLAN rather than transcribed from it
(`0.1.4b.md` §0) -- 0.1.4's `apply.py`, pointed at this file. Every block the
worker applies is a fenced block in the plan headed by one marker line:

    <!-- apply GROUP: new PATH -->   the block is a new file; PATH must not exist
    <!-- apply GROUP: old PATH -->   the block must occur EXACTLY ONCE in PATH ...
    <!-- apply GROUP: to PATH -->    ... and the next block replaces it
    <!-- script: NAME -->            the block is a script, written to $W/NAME

`python3 apply.py GROUP` applies one group's blocks in document order and stops
at the first that does not apply, having written nothing for it;
`python3 apply.py scripts` writes every script. So what runs is the plan's own
text, byte for byte."""
import os, re, sys

REPO, W = os.environ["REPO"], os.environ["W"]
PLAN = os.environ.get("PLAN", os.path.join(REPO, "meta/roadmap/0.1/0.1.4b.md"))
FENCE = r"\n(?P<fence>`{3,})[a-z]*\n(?P<body>.*?)\n(?P=fence)\n"
EDIT = re.compile(r"^<!-- apply (\w+): (new|old|to) (\S+) -->" + FENCE, re.S | re.M)
SCRIPT = re.compile(r"^<!-- script: (\S+) -->" + FENCE, re.S | re.M)
# A TAG IS SPELLED `{{sweep: name=N}}` IN THE PLAN and written `[[sweep: name=N]]`:
# a whole tag in a roadmap file is a claim `check_denominators` holds the tree to.
TAG = re.compile(r"\{\{(sweep:\s*[a-z_]+\s*=\s*-?\d+)\}\}")
text = open(PLAN, encoding="utf-8").read()
want = sys.argv[1]
done = 0
if want == "scripts":
    for m in SCRIPT.finditer(text):
        body = m.group("body") + "\n"
        open(os.path.join(W, m.group(1)), "w", encoding="utf-8").write(body)
        print("  script %-26s %4d lines" % (m.group(1), body.count("\n")))
        done += 1
    print("scripts written: %d" % done)
    sys.exit(0 if done else 1)
pending = None
for m in EDIT.finditer(text):
    grp, kind, path = m.group(1), m.group(2), m.group(3)
    body = TAG.sub(r"[[\1]]", m.group("body") + "\n")
    if grp != want:
        continue
    full = os.path.join(REPO, path)
    if kind == "new":
        if os.path.exists(full):
            sys.exit("STOP: %s already exists; `new` writes only a file that does not" % path)
        os.makedirs(os.path.dirname(full), exist_ok=True)
        open(full, "w", encoding="utf-8").write(body)
        print("  new    %-44s %4d lines" % (path, body.count("\n")))
        done += 1
    elif kind == "old":
        if pending is not None:
            sys.exit("STOP: an `old %s` with no `to` after it" % pending[0])
        pending = (path, body)
    else:
        if pending is None or pending[0] != path:
            sys.exit("STOP: a `to %s` with no `old` for the same file before it" % path)
        cur = open(full, encoding="utf-8").read()
        n = cur.count(pending[1])
        if n != 1:
            sys.exit("STOP: in %s the old text occurs %d times, not once:\n%s"
                     % (path, n, pending[1][:300]))
        open(full, "w", encoding="utf-8").write(cur.replace(pending[1], body))
        print("  edit   %-44s -%d +%d lines" % (path, pending[1].count("\n"), body.count("\n")))
        pending = None
        done += 1
if pending is not None:
    sys.exit("STOP: an `old %s` with no `to` after it" % pending[0])
print("group %s: %d block(s) applied" % (want, done))
sys.exit(0 if done else 1)
```

**Every removal in this plan names its target through `"${W:?}"`**, never a
bare `$W`: an unset variable would otherwise turn `rm -rf "$W/…"` into a
removal at the filesystem's root, and the planner's own shell refused exactly
that command during rehearsal.

---

## 1. What 0.1.4b establishes

### 1.1 The claims, and where each is written

| Claim | Rule | Where it is asserted | Domain |
|---|---|---|---|
| each leaking twin retains every one of its two million bodies at its peak: `peak_live >= 70000000` | S-18b, S-18c, V-17 | `probe06b`, `probe12` — a `heap:` marker each | 2 files, both legs |
| each remedy retains none of them, and did the work: `peak_live` under its ceiling, `count >= 2000000` | S-18b, S-18c, V-17 | `probe06c`, `probe12b` | 2 files, both legs |
| a `Bytes` growth drops the body it replaces: `peak_live` under a ceiling a kept body exceeds | V-17, B-12 | `bytes_growth`, `bytes_view_lifetime` | 2 files, both legs |
| under one shared 64 MiB cap the leaking twins take HeapOom and the remedies exit 0, beside a floor program that starts | TM-131, V-17 | the four twins — a `cap:` marker each; `harness/stages.py`'s `CAP_CONTROL` | 4 files, both legs |
| the harness catches a ceiling missed by a byte, a floor missed by one allocation, a report that never arrives, a capped exit that disagrees, and a cap nothing starts under | V-14, PD-38 | `harness/selfcheck.py` case 9 | 5 plants, 1 control |
| `bytes_take`'s answer is its own: it survives a reuse of the sink and a growth of it | S-18f | `bytes_growth` — four new checks, exits 27 … 30 | 2 shapes, both legs |

### 1.2 What none of this proves

**Any file without a `heap:` marker.** Six files are bounded; every other
file's managed memory is asserted by nothing, and every document this subcycle
touches says so. **A leak at a program's end**: `peak_live` is a high-water
mark, so a program that leaks once, as it exits, reports what one that frees
there reports (V-17). **The instrument's own correctness beyond what the
leaking twins show**: a floor of 70 000 000 on a known leak catches an
instrument that under-counts, and a ceiling on a remedy one that over-counts,
but nothing here audits `npk_hs_note_resize`'s arithmetic line by line — the
compiler owns it, and its `cost` stage uses it. **`Vec<T>` at an owning `T`**:
it is still restricted (TM-132, S-18d), `vec_pop`'s churn measurement of
TM-150 is still uncommitted, and 0.1.3c is where both belong (§15). **F-10's
wrapper**: §4 measures what cycle 0.4 will meet and decides nothing for it.

---

## 2. The instrument, established from the runtime's source and output

The dispatch asked for this from the runtime's own source and output, **because
`0.1.0.md` §8 was wrong once about whether the variable exists at all**. Read
at `c3bdae2` with `git show c3bdae2:runtime/npkrt.ll`, the line numbers that
pin's:

| Where | What it says |
|---|---|
| `@npk_hs_arm`, line 277 on | **decided once, before `main`**: one walk of the kernel's raw envp sets a flag when an entry is exactly `NPK_HEAP_STATS` or begins `NPK_HEAP_STATS=` — "so no value spelling is a second way to switch it on" |
| `@npk_hs_note_alloc`, `_note_free`, `_note_resize`, lines 5817–5899 | four words under the heap mutex — bytes **requested** in total, bytes live, the peak of live, the allocation count — at "the requested sizes the headers already carry … never the rounded class sizes"; a moving `ralloc` is a fresh allocation and then a free |
| `@npk_alloc_impl`, line 5954 on | every allocation counts, **`wild` and managed alike** (its `%wild` argument decides the regime and not the count); `alloc(0)` counts 16; **a failsafe's region allocations are not counted** — `npk_fs_alloc` calls no counter, and the compiler's `done/1.5/1.5.6.md` lists that as a departure from its D-292, whose text still says they are |
| `@npk_hs_report`, line 3983 on | the line: `heap: allocated=<n> peak_live=<n> count=<n>`, decimal, one newline, **on fd 2**; allocation-free, read without the mutex — "a diagnostic, never a verdict" |
| `@npk_exit`'s `leave` block, line 4163 on | printed **"exactly once per process"**, on every exit — a clean `exit`, and a trap's `failsafe` exit, which reaches the same block |

**Measured at `c3bdae2`**, by §11's `baseline.py` part A, both legs, identical:

- **The six files, as they stand before this subcycle**: `probe06b`
  `allocated=118000000 peak_live=70000024 count=4000000`; `probe06c` the same
  allocated and count, **`peak_live=59`**; `probe12` `allocated=70000059
  peak_live=70000059 count=2000002`; `probe12b` the same allocated and count,
  **`peak_live=94`**; `bytes_growth` `allocated=3097163 peak_live=2048587
  count=25`; `bytes_view_lifetime` `allocated=190 peak_live=160 count=7`. **The
  `peak_live` figures equal the cycle README's `aaffb87` ones**, so the
  instrument has not moved across the two pins.
- **Every one is the arithmetic of its source** (§6.3 derives each): `probe06c`'s
  59 is one trip's 24-byte block — `alloc(1 × #size_of<string>)` — and its
  35-byte body, live together; `probe06b`'s 70 000 024 is two million such
  bodies and the block being freed. **Except one**: `bytes_growth`'s 25
  allocations are its sinks' 25 growths and inits, and `bytes_take`'s copy is
  not among them — §4.
- **A program that allocates nothing reports three zeros**, so the runtime's
  startup allocates nothing the counters see. **A held 4 096-byte buffer
  reports exactly 4 096**, allocation count 1.
- **`argv[0]` and the environment move nothing**: `probe06c` run from a
  directory 402 characters deeper, and with a 20 000-byte variable and two
  more, reports the same line. So CI, whose paths differ, sees the same numbers.
- **The variable is switched on by its name**: `NPK_HEAP_STATS=0` and an empty
  value both print the line; `NPK_HEAP_STATSX=1`, `NPK_HEAP_STAT=1` and
  `npk_heap_stats=1` print nothing, as does its absence.
- **A trapped exit reports too**: `probe06b` under a 64 MiB cap takes HeapOom
  (92) and still prints one line.
- **A program that closes fd 2 before it exits prints nothing** — the line has
  nowhere to go. Case 9 uses exactly this to plant a missing report.

---

## 3. The address space, and the control that moved

TM-131 made the managed-memory gate **one shared 64 MiB cap with opposite
outcomes**, and required any address-space bound to be taken beside
`/bin/true` at the same cap, because at `0dfddac` `probe06c` and `/bin/true`
flipped together between 2 688 and 2 816 KiB — below that, every exit was the
dynamic loader's. **It was measured by hand and never run by the harness**:
`harness/` holds no `ulimit`, `RLIMIT` or `resource` call, checked with `git
grep`. Re-measured at `c3bdae2` by `baseline.py` part B — the exit under each
cap, `-O0`:

```text
  ulimit -v       131072   65536   32768   16384   11264   10240    3072    2048
  /bin/true            0       0       0       0       0       0       0     127
  floor program        0       0       0       0       0      92      92      92
  probe12             92      92      92      92      92      92      92      92
  probe12b             0       0       0       0       0      92      92      92
  probe06b            92      92      92      92      92      92      92      92
  probe06c             0       0       0       0       0      92      92      92
```

Bisected in 64 KiB steps at planning: `/bin/true` starts from 2 752 KiB; the
floor program — `main` exits 0, nothing allocated — from **10 752 KiB**, and
below it takes **HeapOom, 92**; `probe06c` and `probe12b` from 10 944;
`bytes_view_lifetime` from 11 008; `bytes_growth` from 13 248. So:

- **The 64 MiB pair still separates**, on both legs: 92 against 0, with six
  times the runtime's floor to spare. TM-131's conclusion stands.
- **`/bin/true` no longer controls for this runtime.** Between 2.75 and
  10.5 MiB it runs clean while every Nitpick program here — the floor program
  included — takes 92. A belt controlled by `/bin/true` at 8 192 KiB would call
  the correct remedy a leak: §7's row D3, measured.
- **The remedies and the floor program flip together**, as `probe06c` and
  `/bin/true` did at `0dfddac`, so a low cap still measures neither; only the
  owner of the floor changed. The runtime grew between the pins — 55 576 to
  72 576 bytes, the failsafe region among what it added (1.5.6) — and the
  machine did not.
- **The leaking halves now take 92 even at 128 MiB**, where `probe12` exited 0
  at `0dfddac`; the table in `probe12`'s header gains a dated row set (§6).

**So the belt's control is the floor program, built by the same toolchain**:
`harness/stages.py`'s `CAP_CONTROL`, the shape of
`tests/probe/probe11d_floor_only.npk`, written into `build/` so that every tree
the harness runs in has one (PD-37).

---

## 4. A library defect found at planning — `bytes_take` hands back a view

**The count.** `bytes_growth`'s source makes 26 allocations: 21 for its first
sink's inits and doublings, 2 for its second's, 2 for its third's, and the
string `bytes_take` hands back. The instrument counted **25**, and `allocated`
matched the 25 exactly. `bytes_take` was, from cycle 0.0.4:

```text
pub func:bytes_take = string(Bytes->:b) never fails {
    // `string_from_bytes(ptr, len)` -- POINTER AND LENGTH, not a slice. It
    // copies, which is what makes the answer owned and independent of this
    // sink's next growth.
    pass string_from_bytes(b.body.ptr, b.len);
};
```

**It does not copy.** `npk_string_from_bytes` stores capacity 0 — a view — and
the compiler's `BUILTIN_REFERENCE.md` says so: *"wraps existing bytes as a view
(cap 0)"*, with a `Views` index of 1, the argument the answer aliases. Read at
every pin this repository has kept — `950bb1d`, `94874ce`, `0dfddac`,
`aaffb87`, `3d15ac9`, `c3bdae2` — each stores `i64 0` as the capacity, and the
comment explaining it entered the runtime at the compiler's 1.4.6 (`900109b`,
2026-08-30), before this repository's first pin. **So the comment above was
false the day it was written**, and the answer is the sink's body under a type
that calls it owned.

**Measured at `c3bdae2`, both legs** (`baseline.py` part C):

| Shape | Result |
|---|---|
| take, then `bytes_clear` and refill the sink — the reuse the comment itself recommends | **exit 13**: the answer reads the NEW bytes; `count=1`, no copy |
| take, then one `bytes_push` that grows the sink | **exit 12**: the answer reads the body the growth dropped — S-18e's use-after-free |
| a formatter that fills a LOCAL `Bytes` and ends `pass raw bytes_take(@sink);` | **refused `NITPICK-BORROW-001`** (D-004 rule 2) |
| the same with a take that genuinely copies, and with its answer bound to a local first | **refused `NITPICK-BORROW-001`**, both |
| the same with a take that CONSUMES the sink — `string(move Bytes:b)` | **compiles; exit 0, the right text, on both legs** |

**Its extent, established before the fix rather than after:**

- **Callers**: `src/lib.npk` re-exports it (line 119), so it is public; in the
  tree one test calls it, `tests/unit/bytes_growth.npk`, which reads the answer
  before either shape above and so passed. No `src/` caller; cycle 0.4's
  formatters are the designed one.
- **Statements that it copies**: `src/core/bytes.npk`'s two comments on it, and
  `tests/unit/bytes_growth.npk`'s section heading *"take copies out"* — swept
  with `git grep -n -E 'bytes_take|take copies|copies out|It copies'`, every
  hit read. `tests/probe/probe16c_bytes_len_write_refused.npk` says take "hands
  out exactly `len` bytes", which is true of both implementations.
- **The most dangerous shape is refused**: a view escaping its sink's frame
  cannot compile. What compiles and misleads is the in-frame reuse and growth.
- **The arm bill does not move with the fix**: `NITPICK-REACH-003`'s own list
  for a root importing `bytes.npk` names the same 12 identities before and
  after, and for one importing `src/lib.npk` the same 13.

**The fix** is `string_concat` of the view and `""` (PD-39): owned by that
builtin's own `Views` column, `never fails`, and it allocates exactly the
prefix — measured, `bytes_growth`'s count becomes 26. `bytes_growth` gains the
two shapes as checks, exits 27 … 30, and both are shown red on the old
implementation (§7, K7 and K8). **The escape shapes are cycle 0.4's**: `FORMAT_MODEL.md`
F-10 says the `string`-returning form of every emitter is a thin wrapper over
a caller-supplied `Bytes`, and the obvious spelling of that wrapper is refused
at this pin whatever `bytes_take` does. A consuming take spells it; choosing
that public name is 0.4's, with its caller (TM-013). §10 carries the
measurement into `meta/roadmap/0.4/README.md`'s "Watch for".

**Why here and not in a subcycle of its own**: this subcycle's instrument found
it; `bytes_growth`'s bound would otherwise be derived on a take that allocates
nothing and re-derived one subcycle later; and the fix is two lines with a test
already in the file that should have caught it. PD-39 records the alternative
declined, and §15 says what changes if the orchestrator strikes it.

---

## 5. The design, and what was measured to choose it

Every choice has a draft decision with the alternatives it declined — §10's
`decisions` block is the text the worker records, and §14 lists the labels.
This section is the evidence.

### 5.1 The marker, and the environment (PD-35)

`// heap: FIELD OP N` — `allocated`, `peak_live` or `count`; `<=` or `>=`; one
bound per line, repeatable. **A file that carries one gets `NPK_HEAP_STATS=1` in
its constructed environment, and no other file does** (TM-120's environment,
extended by one variable the file's own marker asks for). Every run of both
legs must print **exactly one** `heap:` line on stderr, and each bound must
hold; the measured fields print in the unit's verdict line, so a document
quoting a number quotes the run's. An `env:` marker naming the variable is
refused, because `=0` switches the report on. The markers live in the file,
as B-5 puts every expectation, rather than in a unit file of the compiler's
`cost` stage.

### 5.2 The bounds, and how each is placed (PD-36)

| Kind | Bound | Why it is a gate |
|---|---|---|
| leaking twin | `peak_live >= ` the leak's arithmetic, 70 000 000 | a probe asserts its answer (P-6): all two million bodies live; and it commissions the instrument against a known leak on every run — an instrument that under-counted would fail here |
| remedy | `count >= 2000000` and `peak_live <= ` a ceiling | the ceiling alone is passed by an emptied remedy — `count=0 peak_live=0`, measured (K5, D1) — which is TM-131's objection arriving at the new instrument |
| `Bytes` test | `peak_live <= ` a ceiling | a `bytes_reserve` that keeps each old body passes `bytes_growth` at exit 0, measured (K6); only the ceiling sees it |

**Every ceiling is the geometric mean of the correct program's measured peak and
a leak mutant's, rounded DOWN to two significant figures** — equal ratios of
room for a drift in the accounting and for a partial leak. It is one rule for
all four, where "twice the peak" fails two of them (D2), and `mut.py`
recomputes each from the measured numbers on every run of §7.

### 5.3 The belt (PD-37)

`// cap: N KiB, exit C` — run again under `ulimit -v N`, where the exit must be
C — on the two twin pairs only: 64 MiB, 92 for the leaking halves and 0 for the
remedies, on both legs, after the ordinary run. **The floor program runs under
each cap first**, once per invocation; if it does not start clean, every unit
at that cap fails and says the cap measures the runtime. No cap on the `Bytes`
tests: without a leaking twin there are no opposite outcomes, and a clean run
alone is a statement the floor program also makes. `build.py`'s `run_capped`
sets `RLIMIT_AS` in the child between `fork` and `exec`, with no shell.

### 5.4 The self-check's ninth case (PD-38)

One inner run of the harness over a scratch tree holding six copies of one
program — it keeps a 4 096-byte buffer to its exit, so it reports
`allocated=4096 peak_live=4096 count=1`:

| File | Header | Must be |
|---|---|---|
| `heap_held` | all six bounds at exactly the measured values, and `cap: 65536 KiB, exit 0` | **green** — the control, so every comparison is shown inclusive |
| `heap_over` | `peak_live <= 4095` | red, naming `peak_live is 4096` |
| `heap_under` | `count >= 2` | red, naming `count is 1` |
| `heap_silent` | `peak_live <= 8192`, and the program closes fd 2 | red — `found 0` lines |
| `cap_exit` | `cap: 65536 KiB, exit 92` | red — it exits 0 there |
| `cap_machine` | `cap: 1 KiB, exit 0` | red — the floor program cannot start |

V-14 then names nine cases and plants eight (case 6 stays `PEND`), and every
prose statement of those counts moves with them (§10).

### 5.5 `bytes_take` (PD-39)

§4 is the evidence. The body becomes a bound view and `pass
string_concat(view, "")`; the comment above it keeps the false sentence's
history; `bytes_growth` gains, after its existing take, a reuse of the sink
(exits 27, 28) and a second sink taken and grown (exits 29, 30).

---

## 6. The code, applied from this file

Group `code`: **37 blocks, in document order** — the harness (§6.1), then the
library and the six test files (§6.2). Applied at step 2, by `python3
"$W/apply.py" code`, never typed. Every block's prose carries its draft label
(PD-35 … PD-39); step 5's `renumber.py` turns each into its TM number.

### 6.1 The harness — `build.py`, `stages.py`, `checks.py`, `run.py`, `selfcheck.py`

`run_capped` is the only new process-level primitive: no shell, `RLIMIT_AS` in
the child, status and words returned. `stages.py` reads the two markers,
constructs the environment, holds the line, runs the belt and writes the
verdict line's evidence; `checks.py` measures the two new denominators;
`run.py` appends the evidence to each unit's line and carries the counts in
its header; `selfcheck.py` gains case 9.

<!-- apply code: old harness/build.py -->
```text
import os
import re
import subprocess

import elf
```

<!-- apply code: to harness/build.py -->
```text
import os
import re
import resource
import subprocess

import elf
```

<!-- apply code: old harness/build.py -->
```text
    p = subprocess.run(argv, cwd=cwd, env=env, stdout=subprocess.PIPE,
                       stderr=subprocess.PIPE, timeout=timeout)
    return p.returncode, p.stdout, p.stderr
```

<!-- apply code: to harness/build.py -->
```text
    p = subprocess.run(argv, cwd=cwd, env=env, stdout=subprocess.PIPE,
                       stderr=subprocess.PIPE, timeout=timeout)
    return p.returncode, p.stdout, p.stderr


def run_capped(argv, env, kib):
    """Run argv under an ADDRESS-SPACE CAP of `kib` KiB -- `ulimit -v kib`,
    set in the child between `fork` and `exec` -- with its output discarded.
    Cycle 0.1.4b, PD-37: the belt beside `NPK_HEAP_STATS`, and the one
    instrument here that does not read the runtime's own accounting.

    Returns `(status, phrase)`: the exit status -- negative for a signal, as
    `subprocess` reports it -- or None when the child could not be started at
    all, and the same fact in words a failure line can quote. Measured at
    compiler `c3bdae2`: under a 1 KiB cap every binary here, `/bin/true`
    included, is killed by signal 11 before its first instruction, which is a
    status and not an exception.
    """
    limit = kib * 1024

    def cap():
        resource.setrlimit(resource.RLIMIT_AS, (limit, limit))
    try:
        p = subprocess.run(argv, env=env, stdout=subprocess.DEVNULL,
                           stderr=subprocess.DEVNULL, preexec_fn=cap)
    except (OSError, subprocess.SubprocessError) as err:
        return None, "could not be started (%s)" % err
    if p.returncode < 0:
        return p.returncode, "was killed by signal %d" % -p.returncode
    return p.returncode, "exited %d" % p.returncode
```

<!-- apply code: old harness/stages.py -->
```text
from build import BuildError, run, run_split

KEYS = ("expect-exit", "expect-error", "expect-error-at", "expect-golden",
        "stress", "argv", "env", "sweep-count")
```

<!-- apply code: to harness/stages.py -->
```text
from build import BuildError, run, run_capped, run_split

KEYS = ("expect-exit", "expect-error", "expect-error-at", "expect-golden",
        "stress", "argv", "env", "sweep-count", "heap", "cap")
```

<!-- apply code: old harness/stages.py -->
```text
        self.sweep_count = None     # int, the sweep stage's evidence (TM-122)
```

<!-- apply code: to harness/stages.py -->
```text
        self.sweep_count = None     # int, the sweep stage's evidence (TM-122)
        self.heap = []              # (field, op, n): the NPK_HEAP_STATS bounds (PD-35)
        self.cap = None             # (KiB, exit): the address-space belt (PD-37)
        self.measured = {}          # leg label -> {field: n}, as the run printed it
        self.capped = {}            # leg label -> the exit under the cap
```

<!-- apply code: old harness/stages.py -->
```text
            if name in e.env:
                raise MarkerError("%s:%d: `env` sets %s twice" % (rel, lineno,
                                                                  name))
            e.env[name] = val
```

<!-- apply code: to harness/stages.py -->
```text
            if name == HEAP_VAR:
                # PD-35. The harness owns this one: it sets it for a file that
                # carries `heap:` and for no other. And the runtime switches
                # the report on for the NAME -- `NPK_HEAP_STATS=0` prints the
                # line too, measured at `c3bdae2` -- so an `env:` spelling of it
                # would read as a switch it is not.
                raise MarkerError(
                    "%s:%d: `env` may not set %s. The harness sets it for a "
                    "file that carries a `heap:` marker and for no other, and "
                    "any value -- `0` included -- switches the report on "
                    "(PD-35)." % (rel, lineno, HEAP_VAR))
            if name in e.env:
                raise MarkerError("%s:%d: `env` sets %s twice" % (rel, lineno,
                                                                  name))
            e.env[name] = val
        elif key == "heap":
            # PD-35. `// heap: FIELD OP N`, one bound per line, repeatable: a
            # bound on the runtime's own `NPK_HEAP_STATS` line, which the
            # harness then asks for (`env_for`) and holds on both legs.
            m = re.fullmatch(r"(allocated|peak_live|count) (<=|>=) (0|[1-9]\d*)",
                             value)
            if not m:
                raise MarkerError(
                    "%s:%d: `heap` takes `FIELD OP N` -- FIELD one of %s, OP "
                    "`<=` or `>=`, N a non-negative integer -- not %r"
                    % (rel, lineno, ", ".join(HEAP_FIELDS), value))
            bound = (m.group(1), m.group(2), int(m.group(3)))
            if any(b[:2] == bound[:2] for b in e.heap):
                raise MarkerError("%s:%d: a second `%s %s`; a bound has one "
                                  "number" % (rel, lineno, bound[0], bound[1]))
            e.heap.append(bound)
        elif key == "cap":
            # PD-37. `// cap: N KiB, exit C` -- run again under an address-
            # space cap of N KiB (`ulimit -v N`), where the exit must be C.
            m = re.fullmatch(r"([1-9]\d*) KiB, exit (\d+)", value)
            if not m:
                raise MarkerError("%s:%d: `cap` takes `N KiB, exit C`, not %r"
                                  % (rel, lineno, value))
            if e.cap is not None:
                raise MarkerError("%s:%d: a second `cap`; a file is run under "
                                  "one" % (rel, lineno))
            e.cap = (int(m.group(1)), int(m.group(2)))
```

<!-- apply code: old harness/stages.py -->
```text
    if e.error_at and not e.is_refusal:
        raise MarkerError("%s: `expect-error-at` without `expect-error`" % rel)
    return e
```

<!-- apply code: to harness/stages.py -->
```text
    if e.error_at and not e.is_refusal:
        raise MarkerError("%s: `expect-error-at` without `expect-error`" % rel)
    if (e.heap or e.cap) and not e.is_run:
        raise MarkerError("%s: `heap` and `cap` describe a RUN, and a file "
                          "without `expect-exit` is never run (PD-35)" % rel)
    return e
```

<!-- apply code: old harness/stages.py -->
```text
BASE_ENV = {"NTIME_HARNESS": "1"}


def env_for(e):
    env = dict(BASE_ENV)
    env.update(e.env)
    return env
```

<!-- apply code: to harness/stages.py -->
```text
BASE_ENV = {"NTIME_HARNESS": "1"}

# THE RUNTIME'S OWN ALLOCATION COUNTERS, ASKED FOR PER FILE (cycle 0.1.4b,
# PD-35). `NPK_HEAP_STATS` in a program's environment makes the runtime print
# ONE line on fd 2 as the process exits -- by any route, a clean `exit` or a
# trap's `failsafe` -- and nothing when it is absent:
#
#     heap: allocated=N peak_live=N count=N
#
# the bytes REQUESTED in total, the high-water mark of bytes live, and the
# number of allocations, `wild` and managed alike, at the sizes asked for (the
# compiler's `runtime/npkrt.ll`: `npk_hs_note_alloc`, `npk_hs_report`). They
# are a function of the program's own allocation sequence and of nothing
# else: a program that allocates nothing prints three zeros, and a
# 476-character `argv[0]` or a 20 KB environment moves no number -- measured
# at `c3bdae2`. So the variable is set for a file whose header BOUNDS the line
# and for no other: a line nobody asserts is a measurement thrown away.
HEAP_VAR = "NPK_HEAP_STATS"
HEAP_FIELDS = ("allocated", "peak_live", "count")
_HEAP_LINE = re.compile(rb"^heap: allocated=(\d+) peak_live=(\d+) count=(\d+)$",
                        re.MULTILINE)


def env_for(e):
    env = dict(BASE_ENV)
    env.update(e.env)
    if e.heap:
        env[HEAP_VAR] = "1"
    return env


def _heap_problem(e, label, err):
    """Hold one run's `heap:` line to the header's bounds. PD-35, PD-36.

    EXACTLY ONE LINE. None means the report never reached us -- a runtime
    without the instrument, or a program that closed fd 2 -- and there is
    then nothing measured, which is a failure and not a pass. Two means the
    program printed one of its own. Returns `(problem_or_None, numbers)`.
    """
    found = _HEAP_LINE.findall(err)
    if len(found) != 1:
        return ("%s: expected exactly one `heap: allocated=N peak_live=N "
                "count=N` line on stderr and found %d. The header bounds the "
                "runtime's %s report, so a run that printed none measured "
                "nothing, and a second line is not the runtime's (PD-35)."
                % (label, len(found), HEAP_VAR)), None
    got = dict(zip(HEAP_FIELDS, (int(x) for x in found[0])))
    bad = ["%s is %d; the header bounds it %s %d" % (f, got[f], op, n)
           for f, op, n in e.heap
           if (op == "<=" and got[f] > n) or (op == ">=" and got[f] < n)]
    if bad:
        return "%s: %s (PD-36)" % (label, "; ".join(bad)), got
    return None, got


# THE ADDRESS-SPACE BELT (PD-37), and its CONTROL, which is TM-131's rule with
# its instance corrected by measurement. TM-131 took a `ulimit -v` bound only
# beside `/bin/true` at the same cap, because below about 2.7 MiB every exit
# on the workbench is the dynamic loader's. At compiler `c3bdae2` that control
# no longer covers THIS runtime: a program that computes nothing and allocates
# nothing -- `peak_live=0` -- takes HeapOom (92) under every cap up to about
# 10.5 MiB, while `/bin/true` runs clean from 2.75 MiB, so between the two a
# 92 is the runtime's floor and not the program's leak. The control is
# therefore the FLOOR PROGRAM, linked against the same runtime: CAP_CONTROL
# below, the shape of `tests/probe/probe11d_floor_only.npk`, written into
# `build/` so that every tree the harness runs in has one -- the self-check's
# scratch trees included. It is built once and run once per cap, at -O0.
CAP_CONTROL = """mod:cap_control;

func:main = int32(cstring[]:_~argv) {
    exit 0i32;
};

func:failsafe = int32(Error:e) {
    pick (e) {
        (HeapBadRequest) { exit 91i32; },
        (HeapOom)        { exit 92i32; },
        (Unreachable)    { exit 95i32; },
        (WildLeak)       { exit 96i32; },
        (StackExhausted) { exit 106i32; },
        (MachineFault)   { exit 107i32; },
        (*)              { exit 99i32; }
    }
    exit 9i32;
};
"""
_CAP_CONTROL = {}      # KiB -> None when the floor program ran clean, else why not


def _cap_control(bld, kib):
    if kib not in _CAP_CONTROL:
        why = None
        src = os.path.join(bld.out_dir, "cap_control.npk")
        with open(src, "w", encoding="utf-8") as fh:
            fh.write(CAP_CONTROL)
        try:
            exe = bld.build_program(src, "cap_control", False)
        except BuildError as err:
            why = "the floor program did not build (%s)" % err.step
        else:
            st, detail = run_capped([exe], dict(BASE_ENV), kib)
            if st != 0:
                why = "the floor program %s under the same cap" % detail
        _CAP_CONTROL[kib] = why
    return _CAP_CONTROL[kib]


def _capped_problem(bld, exe, e, label):
    kib, want = e.cap
    why = _cap_control(bld, kib)
    if why:
        return ("%s: at %d KiB the cap measures the RUNTIME, not this file: "
                "%s. A bound the floor program also fails is not a statement "
                "about the program under it (TM-131, PD-37)."
                % (label, kib, why))
    st, detail = run_capped([exe] + e.argv, env_for(e), kib)
    e.capped[label] = st
    if st != want:
        return ("%s under a %d KiB address-space cap %s; the header expects "
                "exit %d (PD-37)" % (label, kib, detail, want))
    return None


def memory_note(e):
    """The unit line's evidence: what the run MEASURED, not the bound alone.

    `-O0` first; "on both legs" when the optimised leg printed the same, and
    each leg written out when it did not. `run.py` appends it to every run
    member's verdict line, so the numbers a document quotes are the run's.
    """
    parts = []
    if e.measured:
        fields = [f for f in HEAP_FIELDS if any(b[0] == f for b in e.heap)]

        def show(label):
            return " ".join("%s=%d" % (f, e.measured[label][f]) for f in fields)
        legs = sorted(e.measured)
        if len(legs) == 2 and e.measured[legs[0]] == e.measured[legs[1]]:
            parts.append("heap %s on both legs" % show(legs[0]))
        else:
            parts.append("; ".join("heap %s at %s" % (show(l), l)
                                   for l in legs))
    if e.capped:
        codes = sorted(set(e.capped.values()), key=str)
        parts.append("exit %s under %d KiB%s"
                     % ("/".join(str(c) for c in codes), e.cap[0],
                        " on both legs" if len(e.capped) == 2
                        and len(codes) == 1 else ""))
    return "".join(", " + p for p in parts)
```

<!-- apply code: old harness/stages.py -->
```text
        text = (out + err).decode("utf-8", "replace")
        if text.strip():
            detail.extend("      " + l for l in text.rstrip().splitlines())
        return "\n".join(detail), out
    return None, out
```

<!-- apply code: to harness/stages.py -->
```text
        text = (out + err).decode("utf-8", "replace")
        if text.strip():
            detail.extend("      " + l for l in text.rstrip().splitlines())
        return "\n".join(detail), out
    if e.heap:
        bad, got = _heap_problem(e, label, err)
        if got is not None:
            e.measured.setdefault(label.split(" run ")[0], got)
        if bad:
            return bad, out
    return None, out
```

<!-- apply code: old harness/stages.py -->
```text
            captured.setdefault(label, out)
            if bad:
                problems.append(bad)
                break
    return problems, captured
```

<!-- apply code: to harness/stages.py -->
```text
            captured.setdefault(label, out)
            if bad:
                problems.append(bad)
                break
        # PD-37: the belt runs on every leg, after the run it belts -- a second
        # instrument that shares nothing with the first but the binary.
        if e.cap:
            bad = _capped_problem(bld, exe, e, label)
            if bad:
                problems.append(bad)
    return problems, captured
```

<!-- apply code: old harness/checks.py -->
```text
        d[label + "_exit"] = n_exit
        d[label + "_error"] = n_error
        d[label + "_nomarker"] = n_none
```

<!-- apply code: to harness/checks.py -->
```text
        d[label + "_exit"] = n_exit
        d[label + "_error"] = n_error
        d[label + "_nomarker"] = n_none
    # THE FILES WHOSE MANAGED MEMORY IS ASSERTED -- cycle 0.1.4b, PD-35 and
    # PD-37. Every statement of how many there are is TAGGED, so the day a
    # subcycle bounds another file the sentences are held to it, as every
    # other count here is (TM-142).
    heap_n = cap_n = 0
    for rel in tests:
        try:
            e = stages.read(tree, rel)
        except stages.MarkerError:
            continue                  # `check_expect_headers` names the file
        heap_n += 1 if e.heap else 0
        cap_n += 1 if e.cap else 0
    d["heap_bounded"] = heap_n
    d["cap_belted"] = cap_n
```

<!-- apply code: old harness/run.py -->
```text
  IT IS: the self-check green FIRST (V-15), so the runner has been shown able
  to fail SEVEN ways before it is believed about anything -- V-14 names eight
  and case 6 is `PEND` until cycle 0.5, which is a number this file claimed
  wrong for three cycles (TM-142); the manifest read
```

<!-- apply code: to harness/run.py -->
```text
  IT IS: the self-check green FIRST (V-15), so the runner has been shown able
  to fail EIGHT ways before it is believed about anything -- V-14 names nine,
  the ninth since cycle 0.1.4b (PD-38), and case 6 is `PEND` until cycle 0.5;
  this sentence claimed a wrong number for three cycles (TM-142); the manifest
  read
```

<!-- apply code: old harness/run.py -->
```text
  converted a date to a day.) AND IT IS NOT A MEMORY RESULT
  FOR THE MANAGED HALF: D-151's exit-0 trap counts `wild` allocations and a
  `buffer` is managed (TM-106), so a green run says nothing about `Bytes`
  (S-18b, S-18e).
```

<!-- apply code: to harness/run.py -->
```text
  converted a date to a day.) AND IT IS A MEMORY RESULT FOR THE MANAGED HALF
  ONLY WHERE A HEADER SAYS SO: D-151's exit-0 trap counts `wild` allocations
  and a `buffer` is managed (TM-106), so exit 0 says nothing about `Bytes`
  (S-18b, S-18e). Since cycle 0.1.4b the files whose headers carry `heap:`
  -- six {{sweep: heap_bounded=6}}, the two leak/remedy twin pairs and the
  two `Bytes` tests -- are held on both legs to the runtime's own
  `NPK_HEAP_STATS` line, and the four twins {{sweep: cap_belted=4}} again
  under a 64 MiB address-space cap (PD-35 ... PD-37). Every other file's
  managed memory is asserted by nothing.
```

<!-- apply code: old harness/run.py -->
```text
            rep.unit(rel, stages.program(bld, rel, e),
                     "exit %d, both legs, %.1f s" % (e.exit, time.time() - t0))
```

<!-- apply code: to harness/run.py -->
```text
            problems = stages.program(bld, rel, e)
            rep.unit(rel, problems, "exit %d, both legs, %.1f s%s"
                     % (e.exit, time.time() - t0, stages.memory_note(e)))
```

<!-- apply code: old harness/run.py -->
```text
        if e.env:
            note += ", env %s" % " ".join(sorted(e.env))
        rep.unit(rel, problems, note)
```

<!-- apply code: to harness/run.py -->
```text
        if e.env:
            note += ", env %s" % " ".join(sorted(e.env))
        note += stages.memory_note(e)
        rep.unit(rel, problems, note)
```

<!-- apply code: old harness/run.py -->
```text
    # This line said "eight" for three cycles and `0.0/README.md`'s Gate --
    # which says seven -- was the only place that had it right.
```

<!-- apply code: to harness/run.py -->
```text
    # This line said "eight" for three cycles and `0.0/README.md`'s Gate --
    # which says seven -- was the only place that had it right. (Eight of
    # nine since cycle 0.1.4b added case 9, PD-38 -- still derived, still
    # printed from `selfcheck`'s two constants.)
```

<!-- apply code: old harness/selfcheck.py -->
```text
  V-14: feed the harness wrong expectations and require it to report every one
        as a failure. Seven cases, and this repository's cycle README adds an
        eighth of its own.
```

<!-- apply code: to harness/selfcheck.py -->
```text
  V-14: feed the harness wrong expectations and require it to report every one
        as a failure. Seven cases, and this repository's cycle README adds an
        eighth of its own -- and cycle 0.1.4b a ninth, a program whose managed
        memory disagrees with its header (PD-38).
```

<!-- apply code: old harness/selfcheck.py -->
```text
    if not any(x.startswith("PASS tests/unit/handler.npk") for x in v2):
        c.problems.append(
            "the control run did not pass `tests/unit/handler.npk`.\n"
            "      verdicts: %s" % (v2 or "none"))
    return c
```

<!-- apply code: to harness/selfcheck.py -->
```text
    if not any(x.startswith("PASS tests/unit/handler.npk") for x in v2):
        c.problems.append(
            "the control run did not pass `tests/unit/handler.npk`.\n"
            "      verdicts: %s" % (v2 or "none"))
    return c


# CASE 9's SPECIMEN (cycle 0.1.4b, PD-38). HELD keeps one 4096-byte managed
# buffer live to its exit, so the runtime reports `allocated=4096
# peak_live=4096 count=1` -- measured at `c3bdae2` on both legs, and the
# arithmetic of the one line that allocates. With CLOSE_STDERR it closes fd 2
# before it exits, so the report has nowhere to go and the harness must call
# that no measurement, never a pass.
HELD = """mod:%(mod)s;

error:EW;

func:main = int32(cstring[]:_~argv) {
    buffer:held = buffer_new(4096i64);
    if (held.len != 4096i64) { exit 3i32; }
%(tail)s    exit 0i32;
};
""" + FAILSAFE_EW

CLOSE_STDERR = """    int64:r = sys(3i64, 2i64) ?! EW;
    if (r != 0i64) { exit 4i32; }
"""


def case_9_memory_disagrees(root, man, base):
    c = Case(9, "a program whose managed memory disagrees with its header")

    def held(mod, markers, tail=""):
        return ("// expect-exit: 0\n"
                + "".join("// %s\n" % m for m in markers)
                + HELD % {"mod": mod, "tail": tail})
    where = make_tree(
        os.path.join(base, "case9"), man,
        [
            # THE CONTROL: every field held at BOTH of its bounds, and the
            # belt at a cap the runtime starts under -- so each comparison is
            # shown inclusive, and the reds below are about the plants.
            ("tests/unit/heap_held.npk", held("heap_held", [
                "heap: allocated <= 4096", "heap: allocated >= 4096",
                "heap: peak_live <= 4096", "heap: peak_live >= 4096",
                "heap: count <= 1", "heap: count >= 1",
                "cap: 65536 KiB, exit 0"])),
            # A CEILING ONE BYTE UNDER THE MEASUREMENT.
            ("tests/unit/heap_over.npk", held("heap_over", [
                "heap: peak_live <= 4095"])),
            # A FLOOR ONE ALLOCATION OVER IT -- the other operator, another field.
            ("tests/unit/heap_under.npk", held("heap_under", [
                "heap: count >= 2"])),
            # NO LINE AT ALL: fd 2 closed before the exit that would print it.
            ("tests/unit/heap_silent.npk", held("heap_silent", [
                "heap: peak_live <= 8192"], CLOSE_STDERR)),
            # THE BELT'S EXIT WRONG: clean under the cap, and the header says
            # it takes HeapOom there.
            ("tests/unit/cap_exit.npk", held("cap_exit", [
                "cap: 65536 KiB, exit 92"])),
            # A CAP NOTHING STARTS UNDER -- 1 KiB, on any machine -- so the
            # floor program fails first and the unit must say so (TM-131).
            ("tests/unit/cap_machine.npk", held("cap_machine", [
                "cap: 1 KiB, exit 0"])),
        ],
        [("unit", "program", "tests/unit")])
    st, out, v = invoke(where)
    c.red(st, out)
    for stem in ("heap_over", "heap_under", "heap_silent", "cap_exit",
                 "cap_machine"):
        c.fails(v, "tests/unit/%s.npk" % stem)
    c.passes(v, "tests/unit/heap_held.npk")
    c.says(out, "peak_live is 4096; the header bounds it <= 4095")
    c.says(out, "count is 1; the header bounds it >= 2")
    c.says(out, "line on stderr and found 0")
    c.says(out, "exited 0; the header expects exit 92")
    c.says(out, "the cap measures the RUNTIME")
    c.says(out, "heap allocated=4096 peak_live=4096 count=1 on both legs, "
                "exit 0 under 65536 KiB on both legs")
    return c
```

<!-- apply code: old harness/selfcheck.py -->
```text
#   V14_CASES     what `TESTING.md` V-14 names -- seven, plus this repository's
#                 own eighth (a program whose `failsafe` has been deleted).
#   PLANTED_CASES what is actually planted: case 6 is PEND until cycle 0.5.
```

<!-- apply code: to harness/selfcheck.py -->
```text
#   V14_CASES     what `TESTING.md` V-14 names -- seven, plus this repository's
#                 own eighth (a program whose `failsafe` has been deleted) and,
#                 since cycle 0.1.4b, its ninth (a program whose managed memory
#                 disagrees with its header, PD-38).
#   PLANTED_CASES what is actually planted: case 6 is PEND until cycle 0.5.
```

<!-- apply code: old harness/selfcheck.py -->
```text
V14_CASES = 8
PLANTED_CASES = 7
```

<!-- apply code: to harness/selfcheck.py -->
```text
V14_CASES = 9
PLANTED_CASES = 8
```

<!-- apply code: old harness/selfcheck.py -->
```text
    ok = _report_case(rep, case_8_failsafe_deleted(root, man, base, npkc)) and ok
```

<!-- apply code: to harness/selfcheck.py -->
```text
    ok = _report_case(rep, case_8_failsafe_deleted(root, man, base, npkc)) and ok
    ok = _report_case(rep, case_9_memory_disagrees(root, man, base)) and ok
```

### 6.2 The library and the six test files

`bytes.npk`'s take copies (PD-39); each test file gains its markers at the top
of its header block — contiguous from line 1, so TM-121's reader takes them —
and its "RUN IT AS" paragraph says what the harness now asserts. No prose line
below a header block begins `// heap:` or `// cap:`, which TM-121 would fail.
`probe12`'s `0dfddac` table stays, with `c3bdae2`'s below it, and `probe12b`'s
3 MiB sentence stays under a dated note; `bytes_growth` and
`bytes_view_lifetime` each say in their header how their bound was derived.

<!-- apply code: old src/core/bytes.npk -->
```text
pub func:bytes_take = string(Bytes->:b) never fails {
    // `string_from_bytes(ptr, len)` -- POINTER AND LENGTH, not a slice. It
    // copies, which is what makes the answer owned and independent of this
    // sink's next growth.
    pass string_from_bytes(b.body.ptr, b.len);
};
```

<!-- apply code: to src/core/bytes.npk -->
```text
//
// IT DID NOT COPY UNTIL CYCLE 0.1.4b, AND THIS FUNCTION'S COMMENT SAID IT DID
// (PD-39, `SAFETY.md` S-18f). The body was `pass string_from_bytes(b.body.ptr,
// b.len);` under the words "It copies". `string_from_bytes` WRAPS existing
// bytes as a VIEW -- capacity 0, the compiler's own `BUILTIN_REFERENCE.md`
// row, and so at every pin this repository has kept -- so the answer was the
// sink's body under a type that calls it owned. Measured at `c3bdae2`, both
// legs: taken, then the sink CLEARED AND REFILLED as the paragraph above
// recommends, the answer read the NEW bytes; taken, then the sink GROWN by one
// push, it read freed memory -- S-18e's use-after-free. `bytes_growth` read
// the answer only before either, so both passed; it was found by COUNTING --
// `NPK_HEAP_STATS` reported 25 allocations for that test where its source
// makes 26.
//
// THE COPY IS `string_concat` of the view and "". Its answer is owned -- the
// builtin's `Views` column says it aliases no argument, so an answer that did
// would be a wrong row in the compiler's own table -- it is `never fails`, and
// it allocates exactly the prefix. `view.clone()` and `string_slice` copy too
// and return `Result<string>`, which a `never fails` take would have to
// discard (measured: `NITPICK-TYPE-007`).
//
// WHAT THE COPY DOES NOT BUY: a formatter that fills a LOCAL `Bytes` still
// cannot `pass` this function's answer out of its frame. The borrow tracker
// takes it for a borrow of the sink whatever the callee does, and refuses it
// (`NITPICK-BORROW-001`, D-004 rule 2) -- measured at `c3bdae2` with the copy
// in place. That is `FORMAT_MODEL.md` F-10's thin wrapper, and cycle 0.4's to
// shape (`meta/roadmap/0.4/README.md`, "Watch for").
pub func:bytes_take = string(Bytes->:b) never fails {
    // THE VIEW, bound so the copy below reads a named local rather than a
    // temporary. It owns nothing, so nothing drops at the end of this body.
    string:view = string_from_bytes(b.body.ptr, b.len);
    pass string_concat(view, "");
};
```

<!-- apply code: old tests/unit/bytes_growth.npk -->
```text
// expect-exit: 0
//
// `Bytes` GROWTH IS AMORTISED LINEAR, AND THIS FILE MEASURES IT RATHER THAN
```

<!-- apply code: to tests/unit/bytes_growth.npk -->
```text
// expect-exit: 0
// heap: peak_live <= 2500000
//
// `Bytes` GROWTH IS AMORTISED LINEAR, AND THIS FILE MEASURES IT RATHER THAN
```

<!-- apply code: old tests/unit/bytes_growth.npk -->
```text
// there too; this file asserts the invariant that CAUSES the time.
mod:bytes_growth;
```

<!-- apply code: to tests/unit/bytes_growth.npk -->
```text
// there too; this file asserts the invariant that CAUSES the time.
//
// AND, SINCE CYCLE 0.1.4b, THE MEMORY THAT CAUSES THE SPACE (PD-36). The
// `heap:` marker above holds the runtime's own `NPK_HEAP_STATS` high-water
// mark to 2 500 000 bytes. Measured at compiler `c3bdae2`, both legs:
// 2 048 636 -- the first sink's 1 048 576 beside the second's 1 000 000, and
// the last two sinks' growths and copies -- where a `bytes_reserve` that KEPT
// each old body instead of dropping it peaks at 3 097 238 and still exits 0
// here, because nothing in this file reads a freed body. That is the growth
// path's reliance on D-186 (`src/core/bytes.npk`'s header), asserted on every
// run where cycle 0.0.4 measured it once. The ceiling is the geometric mean
// of the two, rounded down to two significant figures; the mutant is
// `meta/roadmap/0.1/0.1.4b.md` §7's K6.
mod:bytes_growth;
```

<!-- apply code: old tests/unit/bytes_growth.npk -->
```text
    if (raw bytes_len(@d) != 10i64)            { exit 26i32; }
```

<!-- apply code: to tests/unit/bytes_growth.npk -->
```text
    if (raw bytes_len(@d) != 10i64)            { exit 26i32; }

    // ---- and what it hands back is ITS OWN (cycle 0.1.4b, PD-39) ----------
    // The two things a caller does next with a sink: REUSE it -- clear and
    // refill, which `bytes_take`'s own header recommends -- and GROW it. The
    // string `take` handed back must survive both. Until 0.1.4b it was a VIEW
    // of the sink's body (`SAFETY.md` S-18f): the reuse below exited 28 on
    // both legs, and with the reuse cut the growth exited 30
    // (`meta/roadmap/0.1/0.1.4b.md` §7, K7 and K8).
    drop bytes_clear(@d);
    drop bytes_extend_str(@d, "1999-12-31");
    if (raw bytes_len(@d) != 10i64)            { exit 27i32; }
    if (!(raw s.eq("2026-09-05")))             { exit 28i32; }
    Bytes:g = raw bytes_init(1i64);
    drop bytes_extend_str(@g, "2026-09-05");
    string:t = raw bytes_take(@g);
    int64:cap_g = g.body.cap;
    drop bytes_push(@g, 65u8);
    // Growth must actually have happened, or the check after it tests nothing.
    if (g.body.cap == cap_g)                   { exit 29i32; }
    if (!(raw t.eq("2026-09-05")))             { exit 30i32; }
```

<!-- apply code: old tests/unit/bytes_growth.npk -->
```text
    // not catch it -- D-151 watches `wild` allocations and a `buffer` is not
    // one -- which is exactly why the growth path's drop was MEASURED at 256
    // MiB of churn rather than trusted.
```

<!-- apply code: to tests/unit/bytes_growth.npk -->
```text
    // not catch it -- D-151 watches `wild` allocations and a `buffer` is not
    // one -- which is exactly why the growth path's drop was MEASURED at 256
    // MiB of churn rather than trusted. Since cycle 0.1.4b the `heap:` marker
    // in this file's header is what catches it, on every run (PD-36).
```

<!-- apply code: old tests/unit/bytes_view_lifetime.npk -->
```text
// expect-exit: 0
//
// `bytes_view`'s VIEW IS INVALIDATED BY GROWTH, AND THIS FILE IS THE PAIR THAT
```

<!-- apply code: to tests/unit/bytes_view_lifetime.npk -->
```text
// expect-exit: 0
// heap: peak_live <= 180
//
// `bytes_view`'s VIEW IS INVALIDATED BY GROWTH, AND THIS FILE IS THE PAIR THAT
```

<!-- apply code: old tests/unit/bytes_view_lifetime.npk -->
```text
// `bytes_reserve` had NOT released it, and that is a `Bytes` defect either
// way.
mod:bytes_view_lifetime;
```

<!-- apply code: to tests/unit/bytes_view_lifetime.npk -->
```text
// `bytes_reserve` had NOT released it, and that is a `Bytes` defect either
// way.
//
// AND THE RELEASE ITSELF IS ASSERTED SINCE CYCLE 0.1.4b (PD-36), by the
// `heap:` marker above: the runtime's own `NPK_HEAP_STATS` high-water mark is
// at most 180 bytes. Measured at compiler `c3bdae2`, both legs: 160 -- the
// first sink's 64 beside the second's last doubling, 32 and 64 live at once --
// where a `bytes_reserve` that KEPT each old body peaks at 214, and exits 26
// below. `!=` reads the allocator's POISON to see the release, so a runtime
// that stopped poisoning freed memory would turn a correct release into a red
// here; the bound reads the allocator's COUNT, which no change to the poison
// moves. The ceiling is the geometric mean of 160 and 214 rounded down to two
// significant figures; the mutant is `meta/roadmap/0.1/0.1.4b.md` §7's K6.
mod:bytes_view_lifetime;
```

<!-- apply code: old tests/unit/bytes_view_lifetime.npk -->
```text
    // managed memory -- D-151 watches `wild` blocks (TM-106) -- and this file
    // does not claim otherwise.
```

<!-- apply code: to tests/unit/bytes_view_lifetime.npk -->
```text
    // managed memory -- D-151 watches `wild` blocks (TM-106) -- and this file
    // claimed nothing about it until cycle 0.1.4b: its header now bounds the
    // allocator's high-water mark (PD-36).
```

<!-- apply code: old tests/probe/probe06b_element_leak.npk -->
```text
// expect-exit: 0
// NOT A PROBE IN THE ORDINARY SENSE. This file exits 0 and that IS the
```

<!-- apply code: to tests/probe/probe06b_element_leak.npk -->
```text
// expect-exit: 0
// heap: peak_live >= 70000000
// cap: 65536 KiB, exit 92
// NOT A PROBE IN THE ORDINARY SENSE. This file exits 0 and that IS the
```

<!-- apply code: old tests/probe/probe06b_element_leak.npk -->
```text
// RUN IT AS: /usr/bin/time -f "%e s  %M KiB" <binary>
// AND AS:    ( ulimit -v 65536 ; <binary> ) ; echo $?
// because a peak-RSS number is arguable and an exit code is not.
mod:probe06b_element_leak;
```

<!-- apply code: to tests/probe/probe06b_element_leak.npk -->
```text
// THE HARNESS ASSERTS THE LEAK SINCE CYCLE 0.1.4b (PD-35 ... PD-37), by the
// two markers above. `heap:` holds the runtime's own `NPK_HEAP_STATS`
// high-water mark to the leak's arithmetic -- 2 000 000 bodies of 35 bytes,
// every one of them live at the peak, 70 000 000 bytes -- which makes this
// file the instrument's KNOWN-LEAKING control on every run
// (`meta/roadmap/0.1/0.1.0.md` §8's first step). `cap:` runs it again under a
// 64 MiB address space, where it must take HeapOom (92) while its twin exits
// 0: TM-131's pair, which until then the harness had never run. Measured at
// compiler `c3bdae2`, both legs: `allocated=118000000 peak_live=70000024
// count=4000000`, and 92 under the cap. By hand: `NPK_HEAP_STATS=1 <binary>`,
// and `( ulimit -v 65536 ; <binary> ) ; echo $?`.
mod:probe06b_element_leak;
```

<!-- apply code: old tests/probe/probe06c_element_drop.npk -->
```text
// expect-exit: 0
//
// PROBE 06c — TM-106's REMEDY HALF, made reproducible.
```

<!-- apply code: to tests/probe/probe06c_element_drop.npk -->
```text
// expect-exit: 0
// heap: count >= 2000000
// heap: peak_live <= 64000
// cap: 65536 KiB, exit 0
//
// PROBE 06c — TM-106's REMEDY HALF, made reproducible.
```

<!-- apply code: old tests/probe/probe06c_element_drop.npk -->
```text
// RUN IT AS: /usr/bin/time -f "%e s  %M KiB" <binary>
// AND AS:    ( ulimit -v 65536 ; <binary> ) ; echo $?
// because a peak-RSS number is arguable and an exit code is not.
mod:probe06c_element_drop;
```

<!-- apply code: to tests/probe/probe06c_element_drop.npk -->
```text
// THE HARNESS ASSERTS THE REMEDY SINCE CYCLE 0.1.4b (PD-35 ... PD-37), by
// the three markers above, and each is a statement its twin fails.
// `count >= 2000000`: the loop made its two million bodies, so a copy of this
// file whose loop had been emptied -- which retains nothing because it
// allocates nothing -- is red and not green, TM-131's objection to a gate an
// empty program passes. `peak_live <= 64000`: it retained none of them, where
// `probe06b` retains all 70 000 000 bytes. And exit 0 under a 64 MiB cap,
// where `probe06b` takes 92. The ceiling is the geometric mean of this file's
// 59 and the twin's 70 000 024, rounded down to two significant figures
// (PD-36). Measured at compiler `c3bdae2`, both legs: `allocated=118000000
// peak_live=59 count=4000000` -- 59 is one trip's 24-byte block and 35-byte
// body, live together. By hand: `NPK_HEAP_STATS=1 <binary>`.
mod:probe06c_element_drop;
```

<!-- apply code: old tests/probe/probe12_set_overwrite_leak.npk -->
```text
// expect-exit: 0
//
// NOT A PROBE IN THE ORDINARY SENSE. This file exits 0 and that IS the
```

<!-- apply code: to tests/probe/probe12_set_overwrite_leak.npk -->
```text
// expect-exit: 0
// heap: peak_live >= 70000000
// cap: 65536 KiB, exit 92
//
// NOT A PROBE IN THE ORDINARY SENSE. This file exits 0 and that IS the
```

<!-- apply code: old tests/probe/probe12_set_overwrite_leak.npk -->
```text
// inside a 3 MiB address space and the leaking half does not.
```

<!-- apply code: to tests/probe/probe12_set_overwrite_leak.npk -->
```text
// inside a 3 MiB address space and the leaking half does not.
//
// RE-MEASURED AT COMPILER `c3bdae2` (cycle 0.1.4b), AND THE 3 MiB ROW NO
// LONGER HOLDS -- the runtime grew. The same caps, with the control TM-131
// asked for and the one this runtime needs:
//
//   ulimit -v      131072   65536   32768   16384   11264   10240    3072    2048
//   /bin/true           0       0       0       0       0       0       0     127
//   floor program       0       0       0       0       0      92      92      92
//   probe12            92      92      92      92      92      92      92      92
//   probe12b            0       0       0       0       0      92      92      92
//
// The floor program allocates NOTHING -- `peak_live=0` -- and still takes
// HeapOom below about 10.5 MiB, where `/bin/true` runs from 2.75 MiB: between
// the two, 92 is the runtime's floor and not a leak. So the remedy half and
// the floor program flip together, as it and `/bin/true` did at `0dfddac`,
// and a low cap measures neither. One shared 64 MiB cap still separates the
// pair, six times the runtime's floor, and the harness runs exactly that on
// every invocation with the floor program as its control (the `cap:` marker
// above; PD-37).
```

<!-- apply code: old tests/probe/probe12_set_overwrite_leak.npk -->
```text
// RUN IT AS: /usr/bin/time -f "%e s  %M KiB" <binary>
// AND AS:    ( ulimit -v 65536 ; <binary> ) ; echo $?
//
// WHY IT IS NOT A COMPILER DEFECT, WHICH TOOK TWO CONTROLS TO ESTABLISH. The
```

<!-- apply code: to tests/probe/probe12_set_overwrite_leak.npk -->
```text
// THE HARNESS ASSERTS THE LEAK SINCE CYCLE 0.1.4b (PD-35 ... PD-37), by the
// two markers above: the runtime's own `NPK_HEAP_STATS` high-water mark at
// least the leak's arithmetic -- 2 000 000 orphaned bodies of 35 bytes,
// 70 000 000 bytes, all live at the peak -- and HeapOom (92) under a 64 MiB
// cap. Measured at compiler `c3bdae2`, both legs: `allocated=70000059
// peak_live=70000059 count=2000002` -- every byte it ever asked for is still
// live at its peak. By hand: `NPK_HEAP_STATS=1 <binary>`, and
// `( ulimit -v 65536 ; <binary> ) ; echo $?`.
//
// WHY IT IS NOT A COMPILER DEFECT, WHICH TOOK TWO CONTROLS TO ESTABLISH. The
```

<!-- apply code: old tests/probe/probe12b_set_overwrite_drop.npk -->
```text
// expect-exit: 0
//
// PROBE 12b -- the REMEDY half of probe 12, made reproducible.
```

<!-- apply code: to tests/probe/probe12b_set_overwrite_drop.npk -->
```text
// expect-exit: 0
// heap: count >= 2000000
// heap: peak_live <= 81000
// cap: 65536 KiB, exit 0
//
// PROBE 12b -- the REMEDY half of probe 12, made reproducible.
```

<!-- apply code: old tests/probe/probe12b_set_overwrite_drop.npk -->
```text
// the remedy half is clean down to a 3 MiB address space, below which nothing
// on this machine execs at all.
//
// RUN IT AS: /usr/bin/time -f "%e s  %M KiB" <binary>
// AND AS:    ( ulimit -v 65536 ; <binary> ) ; echo $?
```

<!-- apply code: to tests/probe/probe12b_set_overwrite_drop.npk -->
```text
// the remedy half is clean down to a 3 MiB address space, below which nothing
// on this machine execs at all.
//
// NOT AT COMPILER `c3bdae2` (cycle 0.1.4b): the runtime now takes about
// 10.5 MiB before `main` runs, so this half -- and a program that allocates
// nothing at all -- takes HeapOom below that. `probe12`'s header has the
// re-measured table. The 64 MiB pair is unchanged: 92 against 0.
//
// THE HARNESS ASSERTS THE REMEDY SINCE CYCLE 0.1.4b (PD-35 ... PD-37), by the
// three markers above, each a statement its twin fails. `count >= 2000000`:
// the loop made its two million bodies, so a copy whose loop was emptied --
// which retains nothing because it allocates nothing -- is red and not green,
// TM-131's objection to a gate an empty program passes. `peak_live <= 81000`:
// it retained none of them, where `probe12` retains 70 000 059 bytes. And
// exit 0 under a 64 MiB cap, where `probe12` takes 92. The ceiling is the
// geometric mean of this file's 94 and the twin's 70 000 059, rounded down to
// two significant figures (PD-36). Measured at compiler `c3bdae2`, both legs:
// `allocated=70000059 peak_live=94 count=2000002` -- 94 is the block's 24
// bytes and two 35-byte bodies, the slot's and the incoming one, live
// together inside one call. By hand: `NPK_HEAP_STATS=1 <binary>`.
```

### 6.3 Every bound, derived from the source before it was measured

Measured at `c3bdae2` on the applied tree, both legs identical — §7's row K0
prints them. **The "derived" column is arithmetic from the program's text, and
it equals the measurement in every row**, which is what a bound on the number
rests on (V-17). The mutant column is the leak each ceiling exists for, §7.

| File | Field | Derived from the source | Measured | Leak mutant | Bound |
|---|---|---|---|---|---|
| `probe06b` | `peak_live` | 2 000 000 bodies × 35 bytes, all live, + the 24-byte block of the trip in progress | 70 000 024 | — it is the leak | `>= 70000000` |
| `probe06c` | `count` | 2 000 000 trips × 2 allocations — the block, `alloc(1 × #size_of<string>)` = 24, and the body | 4 000 000 | K5: 0 | `>= 2000000` |
| `probe06c` | `peak_live` | one trip's block and body, 24 + 35 | 59 | K1: 70 000 024 | `<= 64000` |
| `probe12` | `peak_live` | the block 24, the first body 35, and 2 000 000 overwritten bodies × 35, none freed | 70 000 059 | — it is the leak | `>= 70000000` |
| `probe12b` | `count` | the block, the first body, 2 000 000 bodies | 2 000 002 | K5b: 2 | `>= 2000000` |
| `probe12b` | `peak_live` | the block 24 and two bodies live inside one call, 35 + 35 | 94 | K2: 70 000 059 | `<= 81000` |
| `bytes_growth` | `peak_live` | at the last sink's growth: 1 048 576 (the first sink, doubled 20 times from 1) + 1 000 000 (the second, reserved once) + 10 + 10 (the third, and its taken copy) + 10 + 10 + 20 (the fourth's old body, its copy, its new body) | 2 048 636 | K6: 3 097 238 | `<= 2500000` |
| `bytes_view_lifetime` | `peak_live` | the first sink 64, and the second's last doubling, 32 and 64 live at once | 160 | K6: 214 | `<= 180` |

**The ceilings by PD-36's rule** — the geometric mean, rounded down to two
significant figures: √(59 × 70 000 024) = 64 265 → **64 000**; √(94 × 70 000 059)
= 81 117 → **81 000**; √(2 048 636 × 3 097 238) = 2 518 950 → **2 500 000**;
√(160 × 214) = 185.0 → **180**. Each sits strictly between its correct peak and
its mutant's, and `mut.py` checks both facts on every run.

**Before this subcycle**, `bytes_growth` measured `allocated=3097163
peak_live=2048587 count=25`: its first sink's 21 allocations (1 + 2 + … +
1 048 576 = 2 097 151 bytes), its second's 2 (1 000 001), its third's 2 (11) —
and nothing for the take (§4). After: `allocated=3097214 peak_live=2048636
count=30` — the take's copy, and the fourth sink's init, growth to ten, copy,
and growth to twenty.

---

## 7. Every bound shown to fail — the matrix

`mut.py` judges each row with the harness's own `stages.program`, in a copy of
the APPLIED tree's `src/` and `tests/` under `$W/mut/<row>/` — **a mutant is
never applied to the tree**. Every substitution asserts that it matched exactly
once, so a row whose mutation matched nothing cannot pass as a caught mutant;
every red row is read by its NAMED LINES, the texts its problems must contain;
and each declined formulation is shown FOOLED where the chosen one is not.
Labels carry no hyphen (`K1`, `D3`), because `check_specs_current` reads a
hyphenated letter-number as a rule.

| Row | The wrong implementation | Caught by |
|---|---|---|
| K0 | none — the applied tree | nothing: all six green, with §6.3's numbers |
| K1 | `probe06c` without its remedy — `free_names` replaced by `vec_free` | its ceiling, and the cap (92 where 0 is declared) |
| K2 | `probe12b` without its remedy — the outgoing element not moved out | its ceiling, and the cap |
| K3 | `probe06b`'s header over `probe06c`'s body — the leak fixed | its floor, and the cap (0 where 92 is declared) |
| K4 | `probe12`'s header over `probe12b`'s body | its floor, and the cap |
| K5, K5b | each remedy's loop emptied — `while (i < 0i64)` | the work floor, `count` |
| K6 | `bytes_reserve` keeps each old body — moved into a `wild` slot freed without a drop, `probe06b`'s own leak shape | `bytes_growth`'s ceiling; `bytes_view_lifetime`'s exit 26 |
| K6b | K6, with `bytes_view_lifetime`'s poison check cut | `bytes_view_lifetime`'s ceiling alone |
| K7 | `bytes_take` restored to the view | `bytes_growth` exit 28 — the reuse |
| K8 | K7, with the reuse checks cut | `bytes_growth` exit 30 — the growth |
| D1 | K5 judged by a ceiling alone | nothing — FOOLED, which is PD-36's case for the work floor |
| D2 | K6 judged by twice the measured peak | nothing — FOOLED |
| D3 | the correct `probe06c` under an 8 192 KiB cap | the floor program's control ("measures the RUNTIME"); `/bin/true` as the control is FOOLED into calling the remedy a leak |

Then the four ceilings recomputed by PD-36's rule from the measured numbers, and
compared with the headers.

<!-- script: mut.py -->
```python
#!/usr/bin/env python3
"""0.1.4b §7: EVERY BOUND SHOWN RED ON A WRONG IMPLEMENTATION, judged by the
harness's own `stages.program` -- the code that judges the real tree -- in a
copy of the APPLIED tree's `src/` and `tests/` under $W/mut/<row>/. Every
substitution is asserted to match EXACTLY ONCE, so a row whose mutation
matched nothing cannot read as a caught mutant (`PLAYBOOK.md` §9). A row is
read by its NAMED LINES -- the text each problem must contain -- and never by
a status alone. Then the declined formulations, each shown FOOLED where the
chosen one is not, and last the ceilings recomputed from the measured numbers
(PD-36's rule). Prints `differences from the plan: N`; exit 0 only at 0."""
import math, os, shutil, sys

REPO, W = os.environ["REPO"], os.environ["W"]
sys.path.insert(0, os.path.join(REPO, "harness"))
import build as build_mod, manifest as manifest_mod, stages   # noqa: E402

MUT = os.path.join(W, "mut")
MAN = manifest_mod.load(REPO)
P06B, P06C = "tests/probe/probe06b_element_leak.npk", "tests/probe/probe06c_element_drop.npk"
P12, P12B = "tests/probe/probe12_set_overwrite_leak.npk", "tests/probe/probe12b_set_overwrite_drop.npk"
GROW, VIEW = "tests/unit/bytes_growth.npk", "tests/unit/bytes_view_lifetime.npk"
BYTES = "src/core/bytes.npk"
diffs = 0


def tree(name, subs=()):
    root = os.path.join(MUT, name)
    if os.path.isdir(root):
        shutil.rmtree(root)
    for d in ("src", "tests"):
        shutil.copytree(os.path.join(REPO, d), os.path.join(root, d))
    for rel, old, new in subs:
        p = os.path.join(root, rel)
        t = open(p, encoding="utf-8").read()
        if t.count(old) != 1:
            sys.exit("STOP: row %s: in %s the old text occurs %d times, not once"
                     % (name, rel, t.count(old)))
        open(p, "w", encoding="utf-8").write(t.replace(old, new))
    return root


def graft(leak, remedy):
    """The LEAK's header markers over the REMEDY's body: the leak fixed."""
    lt = open(os.path.join(REPO, leak), encoding="utf-8").read().splitlines(True)
    rt = open(os.path.join(REPO, remedy), encoding="utf-8").read().splitlines(True)
    marks = [l for l in lt[:6] if l.startswith(("// expect-exit:", "// heap:", "// cap:"))]
    body = "".join(rt[len([l for l in rt[:6] if l.startswith(("// expect-exit:", "// heap:", "// cap:"))]):])
    old_mod = "mod:%s;" % os.path.basename(remedy)[:-4]
    new_mod = "mod:%s;" % os.path.basename(leak)[:-4]
    if body.count(old_mod) != 1:
        sys.exit("STOP: graft: %s appears %d times" % (old_mod, body.count(old_mod)))
    return "".join(marks) + body.replace(old_mod, new_mod)


def judge(root, rel, markers=None):
    bld = build_mod.Build(root, MAN, os.environ["NPKC"], os.environ["NPKRT"],
                          os.path.join(root, "build"))
    if markers is not None:
        p = os.path.join(root, rel)
        t = open(p, encoding="utf-8").read().splitlines(True)
        keep = [l for l in t if not l.startswith(("// heap:", "// cap:"))]
        open(p, "w", encoding="utf-8").write(keep[0] + "".join("// %s\n" % m for m in markers) + "".join(keep[1:]))
    e = stages.read(root, rel)
    problems = stages.program(bld, rel, e)
    return problems, e


def row(name, rel, problems, e, want):
    """`want`: None for green, else the texts the problems must contain."""
    global diffs
    text = " | ".join(" ".join(p.split()) for p in problems)
    peak = " ".join("%s=%s" % (f, e.measured.get("-O0", {}).get(f, "-"))
                    for f in ("peak_live", "count"))
    got = "green" if not problems else "red"
    ok = (want is None and not problems) or (
        want is not None and problems and all(w in text for w in want))
    if not ok:
        diffs += 1
    print("%-4s %-34s %-5s %-34s %s" % (name, os.path.basename(rel), got, peak,
                                        "MATCH" if ok else "DIFFER"))
    for w in (want or []):
        print("       named: %s -- %s" % (w, "found" if w in text else "MISSING"))
    if not ok:
        print("       said:  %s" % (text or "nothing"))


HEAP_OK = {}
# ---- K0: the applied tree, unchanged -- the six files green -------------------
root = tree("K0")
for rel in (P06B, P06C, P12, P12B, GROW, VIEW):
    pr, e = judge(root, rel)
    HEAP_OK[rel] = e.measured.get("-O0", {}).get("peak_live")
    row("K0", rel, pr, e, None)

# ---- the implementations, each wrong in the way its bound exists for ----------
REMEDY06 = "        drop free_names(@b);\n"
root = tree("K1", [(P06C, REMEDY06, "        drop vec_free(@b);\n")])
pr, e = judge(root, P06C)
K1 = e.measured["-O0"]["peak_live"]
row("K1", P06C, pr, e, ["peak_live is 70000024; the header bounds it <= 64000",
                        "exited 92; the header expects exit 0"])

REMEDY12 = ("    string:outgoing = move(v.items[i]);\n    v.items[i] = move(x);\n"
            "    discard(string_byte_length(outgoing));\n")
root = tree("K2", [(P12B, REMEDY12, "    v.items[i] = move(x);\n")])
pr, e = judge(root, P12B)
K2 = e.measured["-O0"]["peak_live"]
row("K2", P12B, pr, e, ["peak_live is 70000059; the header bounds it <= 81000",
                        "exited 92; the header expects exit 0"])

root = tree("K3")
open(os.path.join(root, P06B), "w", encoding="utf-8").write(graft(P06B, P06C))
pr, e = judge(root, P06B)
row("K3", P06B, pr, e, ["peak_live is 59; the header bounds it >= 70000000",
                        "exited 0; the header expects exit 92"])

root = tree("K4")
open(os.path.join(root, P12), "w", encoding="utf-8").write(graft(P12, P12B))
pr, e = judge(root, P12)
row("K4", P12, pr, e, ["peak_live is 94; the header bounds it >= 70000000",
                       "exited 0; the header expects exit 92"])

LOOP = "    while (i < 2000000i64) decreases 2000000i64 - i {\n"
EMPTY = "    while (i < 0i64) decreases 2000000i64 - i {\n"
root = tree("K5", [(P06C, LOOP, EMPTY)])
pr, e = judge(root, P06C)
row("K5", P06C, pr, e, ["count is 0; the header bounds it >= 2000000"])
K5_ROOT = root
root = tree("K5b", [(P12B, LOOP, EMPTY)])
pr, e = judge(root, P12B)
row("K5b", P12B, pr, e, ["count is 2; the header bounds it >= 2000000"])

KEEP_OLD = ("    b.body = move(fresh);\n    pass NIL;\n")
KEPT = ("    wild buffer->:slot = alloc(#size_of<buffer>()) =>! wild buffer->;\n"
        "    slot[0i64] = move(b.body);\n"
        "    dalloc(slot =>! wild int8->);\n"
        "    b.body = move(fresh);\n    pass NIL;\n")
root = tree("K6", [(BYTES, KEEP_OLD, KEPT)])
pr, e = judge(root, GROW)
K6_GROW = e.measured["-O0"]["peak_live"]
row("K6", GROW, pr, e, ["peak_live is %d; the header bounds it <= 2500000" % K6_GROW])
K6_ROOT = root
pr, e = judge(root, VIEW)
row("K6", VIEW, pr, e, ["exited 26; the header expects 0"])
POISON = "    if (w[0i64] == A)                           { exit 26i32; }\n"
root = tree("K6b", [(BYTES, KEEP_OLD, KEPT), (VIEW, POISON, "")])
pr, e = judge(root, VIEW)
K6_VIEW = e.measured["-O0"]["peak_live"]
row("K6b", VIEW, pr, e, ["peak_live is 214; the header bounds it <= 180"])

COPY = "    string:view = string_from_bytes(b.body.ptr, b.len);\n    pass string_concat(view, \"\");\n"
VIEWTAKE = "    pass string_from_bytes(b.body.ptr, b.len);\n"
root = tree("K7", [(BYTES, COPY, VIEWTAKE)])
pr, e = judge(root, GROW)
row("K7", GROW, pr, e, ["exited 28; the header expects 0"])
REUSE = ("    drop bytes_clear(@d);\n    drop bytes_extend_str(@d, \"1999-12-31\");\n"
         "    if (raw bytes_len(@d) != 10i64)            { exit 27i32; }\n"
         "    if (!(raw s.eq(\"2026-09-05\")))             { exit 28i32; }\n")
root = tree("K8", [(BYTES, COPY, VIEWTAKE), (GROW, REUSE, "")])
pr, e = judge(root, GROW)
row("K8", GROW, pr, e, ["exited 30; the header expects 0"])

# ---- THE DECLINED FORMULATIONS, each FOOLED where the chosen one is not -------
print("declined formulations -- each must be FOOLED:")
pr, e = judge(K5_ROOT, P06C, ["heap: peak_live <= 64000", "cap: 65536 KiB, exit 0"])
row("D1", P06C, pr, e, None)       # a ceiling alone, on the emptied remedy (K5)
double = 2 * HEAP_OK[GROW]
pr, e = judge(K6_ROOT, GROW, ["heap: peak_live <= %d" % double])
row("D2", GROW, pr, e, None)       # twice the measured peak, on the kept bodies (K6)
root = tree("D3")
pr, e = judge(root, P06C, ["heap: count >= 2000000", "heap: peak_live <= 64000",
                           "cap: 8192 KiB, exit 0"])
row("D3", P06C, pr, e, ["at 8192 KiB the cap measures the RUNTIME"])
bld = build_mod.Build(root, MAN, os.environ["NPKC"], os.environ["NPKRT"],
                      os.path.join(root, "build"))
exe = bld.build_program(os.path.join(root, P06C), "probe06c_element_drop", False)
t_st, t_said = build_mod.run_capped(["/bin/true"], {}, 8192)
p_st, p_said = build_mod.run_capped([exe], dict(stages.BASE_ENV), 8192)
fooled = t_st == 0 and p_st == 92
if not fooled:
    diffs += 1
print("D3'  /bin/true as the control at 8192 KiB: /bin/true %s, the correct remedy %s"
      " -> the remedy reported as a leak: %s" % (t_said, p_said, "FOOLED, as planned" if fooled else "NOT FOOLED -- DIFFER"))

# ---- THE CEILINGS, RECOMPUTED BY PD-36's RULE ---------------------------------
def rule(ok, bad):
    g = math.sqrt(ok * bad)
    k = 10 ** (len(str(int(g))) - 2)
    return int(g) // k * k
print("ceilings -- the geometric mean of the correct peak and the mutant's, rounded down to two significant figures:")
for rel, bad, marked in ((P06C, K1, 64000), (P12B, K2, 81000), (GROW, K6_GROW, 2500000),
                         (VIEW, K6_VIEW, 180)):
    ok = HEAP_OK[rel]
    c = rule(ok, bad)
    good = c == marked and ok <= c < bad
    if not good:
        diffs += 1
    print("  %-34s correct %9d  mutant %9d  rule %9d  header %9d  %s"
          % (os.path.basename(rel), ok, bad, c, marked, "SAME" if good else "DIFFER"))
print("differences from the plan: %d" % diffs)
sys.exit(0 if diffs == 0 else 1)
```

Run at step 4, after the `code` group and before the documents:

```bash
cd "$REPO" && python3 "$W/mut.py" > "$W/mut.txt" 2>&1; echo "exit $?"
python3 "$W/same.py" matrix "$W/mut.txt"; echo "exit $?"
```

Expected: `exit 0`, then `matrix: 44 line(s) expected, 0 difference(s)` and
`exit 0` — the output is this block, line for line. **About 30 s**, one build
at a time.

<!-- expect: matrix -->
```text
K0   probe06b_element_leak.npk          green peak_live=70000024 count=4000000   MATCH
K0   probe06c_element_drop.npk          green peak_live=59 count=4000000         MATCH
K0   probe12_set_overwrite_leak.npk     green peak_live=70000059 count=2000002   MATCH
K0   probe12b_set_overwrite_drop.npk    green peak_live=94 count=2000002         MATCH
K0   bytes_growth.npk                   green peak_live=2048636 count=30         MATCH
K0   bytes_view_lifetime.npk            green peak_live=160 count=7              MATCH
K1   probe06c_element_drop.npk          red   peak_live=70000024 count=4000000   MATCH
       named: peak_live is 70000024; the header bounds it <= 64000 -- found
       named: exited 92; the header expects exit 0 -- found
K2   probe12b_set_overwrite_drop.npk    red   peak_live=70000059 count=2000002   MATCH
       named: peak_live is 70000059; the header bounds it <= 81000 -- found
       named: exited 92; the header expects exit 0 -- found
K3   probe06b_element_leak.npk          red   peak_live=59 count=4000000         MATCH
       named: peak_live is 59; the header bounds it >= 70000000 -- found
       named: exited 0; the header expects exit 92 -- found
K4   probe12_set_overwrite_leak.npk     red   peak_live=94 count=2000002         MATCH
       named: peak_live is 94; the header bounds it >= 70000000 -- found
       named: exited 0; the header expects exit 92 -- found
K5   probe06c_element_drop.npk          red   peak_live=0 count=0                MATCH
       named: count is 0; the header bounds it >= 2000000 -- found
K5b  probe12b_set_overwrite_drop.npk    red   peak_live=59 count=2               MATCH
       named: count is 2; the header bounds it >= 2000000 -- found
K6   bytes_growth.npk                   red   peak_live=3097238 count=54         MATCH
       named: peak_live is 3097238; the header bounds it <= 2500000 -- found
K6   bytes_view_lifetime.npk            red   peak_live=- count=-                MATCH
       named: exited 26; the header expects 0 -- found
K6b  bytes_view_lifetime.npk            red   peak_live=214 count=12             MATCH
       named: peak_live is 214; the header bounds it <= 180 -- found
K7   bytes_growth.npk                   red   peak_live=- count=-                MATCH
       named: exited 28; the header expects 0 -- found
K8   bytes_growth.npk                   red   peak_live=- count=-                MATCH
       named: exited 30; the header expects 0 -- found
declined formulations -- each must be FOOLED:
D1   probe06c_element_drop.npk          green peak_live=0 count=0                MATCH
D2   bytes_growth.npk                   green peak_live=3097238 count=54         MATCH
D3   probe06c_element_drop.npk          red   peak_live=59 count=4000000         MATCH
       named: at 8192 KiB the cap measures the RUNTIME -- found
D3'  /bin/true as the control at 8192 KiB: /bin/true exited 0, the correct remedy exited 92 -> the remedy reported as a leak: FOOLED, as planned
ceilings -- the geometric mean of the correct peak and the mutant's, rounded down to two significant figures:
  probe06c_element_drop.npk          correct        59  mutant  70000024  rule     64000  header     64000  SAME
  probe12b_set_overwrite_drop.npk    correct        94  mutant  70000059  rule     81000  header     81000  SAME
  bytes_growth.npk                   correct   2048636  mutant   3097238  rule   2500000  header   2500000  SAME
  bytes_view_lifetime.npk            correct       160  mutant       214  rule       180  header       180  SAME
differences from the plan: 0
```

**A red row's `said:` line is what to read when a row DIFFERS**: it prints the
harness's own problems, whole. A row that differs is a stop, and so is any row
whose `named:` text says `MISSING`.

---

## 8. Cost — the thresholds, set before the numbers

**Measured at planning, at `c3bdae2`, on the workbench** (`free -g` read 143–145
GiB available across the rehearsal's runs):

| What | Time | Peak resident |
|---|---|---|
| `baseline.py` | about 23 s | 125 MB |
| `mut.py` | about 29 s | 125 MB |
| a full invocation, before this subcycle | 111.4 s | 125 MB |
| a full invocation, after it | 122.7 s — 11.3 s more | 125 MB |

**The thresholds, set here and not after:**

- **A full invocation may grow by at most 30 s** over step 0's own baseline,
  on the same machine and the same day. The growth is the self-check's case 9 —
  one inner run over six programs, two legs each — and eight capped runs, about
  a second each. Under it: record both numbers. **Over it: stop and report**; a
  harness that grew by more than a quarter for six files' bounds has a cost
  nobody measured.
- **Nothing here is large.** The leaking twins hold 70 MB of heap at their peak
  and about 50 MB under the cap, one at a time; the harness's peak resident
  stays at the baseline's. **The run is sequential and stays that way**: do not
  fan the matrix out, and do not run two harness invocations at once, on a
  machine where `earlyoom` has fired today.

---

## 9. Steps, in order

Each step's expected output is the planning run's; the worker's own run is the
record, and a difference is a finding, not a number to copy. **Anything red
that a step does not predict is a stop.** Steps 1, 4 and 7 are compared by
`same.py`, the rest by reading. At a pin other than `c3bdae2`, §11 says which
lines may differ, and how.

**Step 0 — the start, the anchor and the baseline.** After `source`-ing
`env.sh` and §0's extraction:

```bash
( cd "$WB/.internal/toolchain/$PIN" && sha256sum -c SHA256SUMS )
echo "$(stat -c %s "$NPKRT") $(sha256sum < "$NPKRT" | cut -d' ' -f1)"
git -C "$REPO" status --porcelain | wc -l
git -C "$REPO" log --oneline -1
python3 --version
free -g | sed -n 2p
cd "$REPO" && /usr/bin/time -o "$W/full0.time" -f '%e s wall, %M KiB peak RSS' python3 "$REPO/harness/run.py" > "$W/full0.log" 2>&1; echo "exit $?"
tail -1 "$W/full0.log"; cat "$W/full0.time"
grep -o "[0-9]* unresolved\|report: [A-Z]-[0-9a-z]* cited" "$W/full0.log"
python3 "$CHECK/check_refs.py" "$REPO"
```

Expected: two `OK`s; **`72576 162b897539285a773a6a1a0329750e148a6c9590b45dda2d017704743b591824`
— the anchor, and anything else is a stop**; `1` — this file, whose title the
worker has just set `RUNNING` (`PLAYBOOK.md` §12's *"N plus the plan file"*);
this plan's commit; `Python 3.12.3`; then:

<!-- expect: step0 -->
```text
exit 0
GREEN -- 91 unit(s), 0 failures; 5 pending, 111.4 s. Read this file's header for what that does not mean.
111.43 s wall, 125184 KiB peak RSS
2 unresolved
report: S-18f cited
report: V-17 cited
  nitpick-time       clean            (77 md files via git ls-files; leak scan 223 text of 223 tracked)

All clean.
```

**The two `report:` lines naming `S-18f` and `V-17` are expected**: this plan
cites the two rules its work commit declares, and `check_specs_current`
reports an unresolved citation rather than failing on one; after step 5 they
are gone. The grep prints the rule and the count only, because the report's
own line carries a citation count that this file's text moves. **At a pin
other than `c3bdae2`** the `GREEN` line's unit count is the adoption's, and it
is the number step 7 must equal.

**Step 1 — every pin-dependent fact, re-derived before anything changes** (§11):

```bash
cd "$REPO" && python3 "$W/baseline.py" > "$W/base.txt" 2>&1; echo "exit $?"
python3 "$W/same.py" baseline "$W/base.txt"; echo "exit $?"
```

Expected: `exit 0`, `baseline: 44 line(s) expected, 0 difference(s)`, `exit 0`
— about 23 s. **Any difference is a stop**, and §11 says what each line rests
on.

**Step 2 — the code, applied from this file** (§6):

```bash
cd "$REPO" && python3 "$W/apply.py" code; echo "exit $?"
git -C "$REPO" status --short
```

Expected — 37 blocks, then this file and the twelve files §6 edits:

<!-- expect: step2 -->
```text
  edit   harness/build.py                             -5 +6 lines
  edit   harness/build.py                             -3 +30 lines
  edit   harness/stages.py                            -4 +4 lines
  edit   harness/stages.py                            -1 +5 lines
  edit   harness/stages.py                            -4 +42 lines
  edit   harness/stages.py                            -3 +6 lines
  edit   harness/stages.py                            -7 +149 lines
  edit   harness/stages.py                            -5 +11 lines
  edit   harness/stages.py                            -5 +11 lines
  edit   harness/checks.py                            -3 +17 lines
  edit   harness/run.py                               -4 +5 lines
  edit   harness/run.py                               -4 +9 lines
  edit   harness/run.py                               -2 +3 lines
  edit   harness/run.py                               -3 +4 lines
  edit   harness/run.py                               -2 +4 lines
  edit   harness/selfcheck.py                         -3 +4 lines
  edit   harness/selfcheck.py                         -5 +80 lines
  edit   harness/selfcheck.py                         -3 +5 lines
  edit   harness/selfcheck.py                         -2 +2 lines
  edit   harness/selfcheck.py                         -1 +2 lines
  edit   src/core/bytes.npk                           -6 +33 lines
  edit   tests/unit/bytes_growth.npk                  -3 +4 lines
  edit   tests/unit/bytes_growth.npk                  -2 +14 lines
  edit   tests/unit/bytes_growth.npk                  -1 +21 lines
  edit   tests/unit/bytes_growth.npk                  -3 +4 lines
  edit   tests/unit/bytes_view_lifetime.npk           -3 +4 lines
  edit   tests/unit/bytes_view_lifetime.npk           -3 +14 lines
  edit   tests/unit/bytes_view_lifetime.npk           -2 +3 lines
  edit   tests/probe/probe06b_element_leak.npk        -2 +4 lines
  edit   tests/probe/probe06b_element_leak.npk        -4 +12 lines
  edit   tests/probe/probe06c_element_drop.npk        -3 +6 lines
  edit   tests/probe/probe06c_element_drop.npk        -4 +13 lines
  edit   tests/probe/probe12_set_overwrite_leak.npk   -3 +5 lines
  edit   tests/probe/probe12_set_overwrite_leak.npk   -1 +20 lines
  edit   tests/probe/probe12_set_overwrite_leak.npk   -4 +10 lines
  edit   tests/probe/probe12b_set_overwrite_drop.npk  -3 +6 lines
  edit   tests/probe/probe12b_set_overwrite_drop.npk  -5 +20 lines
group code: 37 block(s) applied
exit 0
 M harness/build.py
 M harness/checks.py
 M harness/run.py
 M harness/selfcheck.py
 M harness/stages.py
 M meta/roadmap/0.1/0.1.4b.md
 M src/core/bytes.npk
 M tests/probe/probe06b_element_leak.npk
 M tests/probe/probe06c_element_drop.npk
 M tests/probe/probe12_set_overwrite_leak.npk
 M tests/probe/probe12b_set_overwrite_drop.npk
 M tests/unit/bytes_growth.npk
 M tests/unit/bytes_view_lifetime.npk
```

**Step 3 — one filtered run: the self-check with case 9, the tree checks over
the new markers and tags, and one pair.** A filtered run concludes nothing and
is for exactly this:

```bash
cd "$REPO" && python3 "$REPO/harness/run.py" --only probe06 > "$W/only.log" 2>&1; echo "exit $?"
grep -n "V-14\|case 9\|check_denominators\|probe06\|^GREEN\|^RED" "$W/only.log" | cut -c1-200
grep -o "[0-9]* unresolved\|report: [A-Z]-[0-9a-z]* cited" "$W/only.log"
```

Expected — `exit 0`; case 9 caught; `check_denominators` clean over two more
tags — the run.py docstring's — and two more measured denominators; both
twins' verdict lines carrying their evidence; and the two citations still
reported, as the documents that declare them come at step 5:

<!-- expect: step3 -->
```text
exit 0
2:this runner has been shown able to fail 8 ways (V-14), of the 9 V-14 names.
4:*** --only 'probe06': THIS RUN CONCLUDES NOTHING. A filtered run is for iterating; ***
7:[1/9] self-check -- 8 of V-14's 9 cases (case 6 is PEND), then 23 tree-check violations,
25:  ok    case 9  a program whose managed memory disagrees with its header
66:  ok    check_denominators       48 tagged number(s) over 214 file(s), against 24 measured denominator(s)
107:  ok    tests/probe/probe06_generic_vec.npk            exit 0, both legs, 0.4 s
108:  ok    tests/probe/probe06b_element_leak.npk          exit 0, both legs, 2.7 s, heap peak_live=70000024 on both legs, exit 92 under 65536 KiB on both legs
109:  ok    tests/probe/probe06c_element_drop.npk          exit 0, both legs, 3.9 s, heap peak_live=59 count=4000000 on both legs, exit 0 under 65536 KiB on both legs
115:GREEN under --only 'probe06' in 77.6 s -- 28 unit(s) ran; 5 pending.
2 unresolved
report: S-18f cited
report: V-17 cited
```

**Step 4 — the matrix** (§7): its two commands. Expected: §7's block,
`matrix: 44 line(s) expected, 0 difference(s)`. **About 30 s.**

**Step 5 — the documents** (§10), in this order, each exit checked:

```bash
cd "$REPO" && python3 "$W/apply.py" docs; echo "exit $?"
cd "$REPO" && python3 "$W/apply.py" decisions; echo "exit $?"
python3 "$W/renumber.py" 184 "$(date +%F)"; echo "exit $?"
```

— `renumber.py` with the first new number (the gate's item 6; 184 at
planning) and today's date. **Renumber by script, never by hand.** Expected:

<!-- expect: step5 -->
```text
  edit   meta/specs/TESTING.md                        -3 +5 lines
  edit   meta/specs/TESTING.md                        -3 +9 lines
  edit   meta/specs/TESTING.md                        -1 +2 lines
  edit   meta/specs/TESTING.md                        -3 +62 lines
  edit   meta/specs/BUILD.md                          -3 +11 lines
  edit   meta/specs/BUILD.md                          -2 +7 lines
  edit   meta/specs/BUILD.md                          -1 +2 lines
  edit   meta/specs/SAFETY.md                         -3 +15 lines
  edit   meta/specs/SAFETY.md                         -2 +8 lines
  edit   meta/specs/SAFETY.md                         -2 +4 lines
  edit   meta/specs/SAFETY.md                         -2 +33 lines
  edit   tests/probe/README.md                        -3 +8 lines
  edit   harness/README.md                            -1 +1 lines
  edit   harness/README.md                            -1 +1 lines
  edit   harness/README.md                            -3 +5 lines
  edit   harness/README.md                            -6 +12 lines
  edit   harness/README.md                            -1 +11 lines
  edit   CLAUDE.md                                    -1 +25 lines
  edit   CLAUDE.md                                    -3 +7 lines
  edit   CLAUDE.md                                    -1 +2 lines
  edit   CLAUDE.md                                    -3 +5 lines
  edit   .github/workflows/ci.yml                     -1 +3 lines
  edit   .github/workflows/ci.yml                     -3 +6 lines
  edit   src/cal/cal.npk                              -3 +5 lines
  edit   meta/roadmap/ROADMAP.md                      -3 +6 lines
  edit   meta/roadmap/0.4/README.md                   -1 +12 lines
group docs: 26 block(s) applied
exit 0
  edit   meta/DECISIONS.md                            -2 +9 lines
  edit   meta/DECISIONS.md                            -7 +161 lines
group decisions: 2 block(s) applied
exit 0
  meta/DECISIONS.md                            7 placeholder(s), 7 date(s)
  CLAUDE.md                                    4 label(s)
  harness/README.md                            2 label(s)
  harness/build.py                             1 label(s)
  harness/checks.py                            2 label(s)
  harness/run.py                               4 label(s)
  harness/selfcheck.py                         3 label(s)
  harness/stages.py                            16 label(s)
  meta/roadmap/0.1/README.md                   5 label(s)
  meta/specs/SAFETY.md                         3 label(s)
  meta/specs/TESTING.md                        4 label(s)
  src/core/bytes.npk                           1 label(s)
  tests/probe/probe06b_element_leak.npk        2 label(s)
  tests/probe/probe06c_element_drop.npk        3 label(s)
  tests/probe/probe12_set_overwrite_leak.npk   3 label(s)
  tests/probe/probe12b_set_overwrite_drop.npk  3 label(s)
  tests/unit/bytes_growth.npk                  3 label(s)
  tests/unit/bytes_view_lifetime.npk           2 label(s)
labels renumbered: 61; PD-35 ... PD-39 -> TM numbers 184 ... 188
exit 0
```

The seven placeholders are the five headings, one cross-reference and
TM-131's marker; the seven dates, the section's title, the five decisions'
first lines and the marker. The cycle README's five labels come from this
plan's own commit.

**Step 6 — the stale-claim sweep** (§10.2): its commands, each hit read.

**Step 7 — the full run:**

```bash
free -g | sed -n 2p
cd "$REPO" && /usr/bin/time -o "$W/final.time" -f '%e s wall, %M KiB peak RSS' python3 "$REPO/harness/run.py" > "$W/final.log" 2>&1; echo "exit $?"
cat "$W/final.time"
grep "V-14\|case 9\|planted violation\|check_denominators\|exemption verdicts\|parse cleanly\|probe06\|probe12\|bytes_growth\|bytes_view\|^GREEN\|^RED" "$W/final.log" | cut -c1-230 > "$W/final.txt"
grep -o "[0-9]* declared rule(s)\|[0-9]* unresolved" "$W/final.log" >> "$W/final.txt"
python3 "$W/same.py" final "$W/final.txt"; echo "exit $?"
```

Expected: `exit 0`; the wall clock, within §8's 30 s of step 0's; and
`final: 17 line(s) expected, 0 difference(s)`, `exit 0` — the file is this
block, the times set aside:

<!-- expect: final -->
```text
this runner has been shown able to fail 8 ways (V-14), of the 9 V-14 names.
[1/9] self-check -- 8 of V-14's 9 cases (case 6 is PEND), then 23 tree-check violations,
  ok    case 9  a program whose managed memory disagrees with its header
  ok    the tree checks                                23 planted violation(s) caught, 23 clean control(s) silent
      exemption verdicts: 7 of 7 re-derived, 0 moved
  ok    check_denominators       55 tagged number(s) over 214 file(s), against 24 measured denominator(s)
      82 parse cleanly, 24 parse and are refused by a later phase, 2 must not parse and do not, 0 failed
  ok    tests/probe/probe06_generic_vec.npk            exit 0, both legs, 0.4 s
  ok    tests/probe/probe06b_element_leak.npk          exit 0, both legs, 2.9 s, heap peak_live=70000024 on both legs, exit 92 under 65536 KiB on both legs
  ok    tests/probe/probe06c_element_drop.npk          exit 0, both legs, 3.9 s, heap peak_live=59 count=4000000 on both legs, exit 0 under 65536 KiB on both legs
  ok    tests/probe/probe12_set_overwrite_leak.npk     exit 0, both legs, 1.3 s, heap peak_live=70000059 on both legs, exit 92 under 65536 KiB on both legs
  ok    tests/probe/probe12b_set_overwrite_drop.npk    exit 0, both legs, 2.6 s, heap peak_live=94 count=2000002 on both legs, exit 0 under 65536 KiB on both legs
  ok    tests/unit/bytes_growth.npk                    exit 0, both legs, 0.4 s, heap peak_live=2048636 on both legs
  ok    tests/unit/bytes_view_lifetime.npk             exit 0, both legs, 0.4 s, heap peak_live=160 on both legs
GREEN -- 91 unit(s), 0 failures; 5 pending, 122.7 s. Read this file's header for what that does not mean.
322 declared rule(s)
0 unresolved
```

`322 declared rule(s)` is 315 and seven — V-17, S-18f and the five new
decisions — and `0 unresolved` is step 0's two citations resolved.
**At a pin other than `c3bdae2`** the `exemption verdicts`, `parse cleanly`
and `GREEN` lines are the adoption's, and `same.py` names them: each must
equal step 0's run, and every other line this block's.

**Step 8 — the references, the list, and the commit.**

```bash
python3 "$CHECK/check_refs.py" "$REPO"
git -C "$REPO" add -A && git -C "$REPO" status --short
python3 "$CHECK/check_refs.py" "$REPO"
```

Expected: clean before and after staging, at the same counts, **77 md files
and 223 of 223 tracked** — no file is added — and `git status --short` lists
**exactly** these twenty-five, this file among them:

<!-- expect: step8 -->
```text
M  .github/workflows/ci.yml
M  CLAUDE.md
M  harness/README.md
M  harness/build.py
M  harness/checks.py
M  harness/run.py
M  harness/selfcheck.py
M  harness/stages.py
M  meta/DECISIONS.md
M  meta/roadmap/0.1/0.1.4b.md
M  meta/roadmap/0.1/README.md
M  meta/roadmap/0.4/README.md
M  meta/roadmap/ROADMAP.md
M  meta/specs/BUILD.md
M  meta/specs/SAFETY.md
M  meta/specs/TESTING.md
M  src/cal/cal.npk
M  src/core/bytes.npk
M  tests/probe/README.md
M  tests/probe/probe06b_element_leak.npk
M  tests/probe/probe06c_element_drop.npk
M  tests/probe/probe12_set_overwrite_leak.npk
M  tests/probe/probe12b_set_overwrite_drop.npk
M  tests/unit/bytes_growth.npk
M  tests/unit/bytes_view_lifetime.npk
```

**Anything else is a stop.** One work commit, then one record commit —
0.1.4's shape, because a CI result cannot live in the commit that caused it.
Work: `cycle 0.1.4b: the managed-memory gate — NPK_HEAP_STATS held to six files' bounds, and bytes_take owns its answer`,
with this file's title reading `RUNNING`. Push; `gh run watch <id>
--exit-status`; read the job's own log through `gh api
repos/<owner>/<repo>/actions/jobs/<job>/logs` (`gh run view --log` can print
nothing) for exactly one unqualified `GREEN -- ` line, **and for the six unit
lines with their `heap` evidence — equal to step 7's, since CI builds its own
`npkc` from the same commit**; a CI heap number that differs from the
workbench's is a finding to record, not a failure to explain away. Record:
`cycle 0.1.4b: the record — CI run <id> at <pin>`, the title `DONE`.

---

## 10. The documents

**All of them are applied, not typed** — group `docs` (26 blocks) and group
`decisions` (2 blocks — TM-131's supersession marker, then the new section),
at step 5, then `renumber.py`. 0.1.4's worker found that
a rehearsal which stubbed `DECISIONS.md` and skipped the hand-edited paragraphs
could not predict the stale-claim sweep's counts, so this plan has **no hand
edit in the work commit** but the execution record itself. **No hunk holds a
markdown link** — `check_refs` resolves links inside this file's fences from
THIS file's directory — so every path in them is plain text in backticks.
**No hunk states a number that moves with the pin** unless it is dated to the
pin that measured it: the unit count, the defect corpus's arithmetic and the
exemption count all move at the adoption to the pin carrying the compiler's
DEF-99 fix, so the documents say what this subcycle changed, not what the
totals are.

### 10.1 The hunks

A statement of a list's membership travels with the list (the dispatch's
lesson from 0.1.4): the self-check's case count is stated in `TESTING.md`,
`CLAUDE.md`, `harness/README.md` twice, the CI header, `run.py` twice and
`selfcheck.py` twice, and every one moves here — the four in code in §6's
group, the rest below. Cycle 0.0's use-after-free count — *"two"* — is stated
in `CLAUDE.md`, `harness/README.md`, `SAFETY.md` S-18e, `ROADMAP.md` and
`src/cal/cal.npk`, and each gains the third. *"A green run is not a memory
result"* is stated in `run.py`, `harness/README.md` and the CI header, and each
now says which files it is one for.

**`TESTING.md`** — V-1e's dated sentence, V-14's ninth case and its count, and
the new §10 (PD-35 … PD-37):

<!-- apply docs: old meta/specs/TESTING.md -->
```text
whoever ran it. **The base is deliberately non-empty**: under an empty
environment `probe09_environ_split` exits 10, a substantive code, so an empty
base would recreate V-1d's defect at the runner instead of the file.
```

<!-- apply docs: to meta/specs/TESTING.md -->
```text
whoever ran it. **The base is deliberately non-empty**: under an empty
environment `probe09_environ_split` exits 10, a substantive code, so an empty
base would recreate V-1d's defect at the runner instead of the file. *(Since
cycle 0.1.4b one marker adds a variable its file does not spell: `heap:` sets
`NPK_HEAP_STATS`, and no `env:` marker may — V-17.)*
```

<!-- apply docs: old meta/specs/TESTING.md -->
```text
8. **a program whose `failsafe` has been deleted** — this repository's own
   addition, because `npkc` exit 0 is not well-formedness (B-4b, TM-112) and
   V-14's seven do not cover it.
```

<!-- apply docs: to meta/specs/TESTING.md -->
```text
8. **a program whose `failsafe` has been deleted** — this repository's own
   addition, because `npkc` exit 0 is not well-formedness (B-4b, TM-112) and
   V-14's seven do not cover it;
9. **a program whose managed memory disagrees with its header** — this
   repository's own too, since cycle 0.1.4b (PD-38): a `heap:` ceiling one
   byte under the measurement, a floor one allocation over it, a run whose
   report never reaches stderr, a `cap:` exit the program does not take, and a
   cap the floor program cannot start under — beside a control held exactly at
   both bounds of every field and at a cap it starts under (V-17).
```

<!-- apply docs: old meta/specs/TESTING.md -->
```text
had it right. The number is now derived from `selfcheck.PLANTED_CASES` rather
```

<!-- apply docs: to meta/specs/TESTING.md -->
```text
had it right. *(Eight of nine since cycle 0.1.4b: case 9 is planted, and case
6 is still `PEND`.)* The number is now derived from `selfcheck.PLANTED_CASES` rather
```

<!-- apply docs: old meta/specs/TESTING.md -->
```text
prove it ran, and not a gate because it trusts Python (TM-026). What it checks
each year beside the digest, and why it hands no reverse constructor anything,
is TM-182.
```

<!-- apply docs: to meta/specs/TESTING.md -->
```text
prove it ran, and not a gate because it trusts Python (TM-026). What it checks
each year beside the digest, and why it hands no reverse constructor anything,
is TM-182.

---

## 10. Managed memory

**Rule V-17 (PD-35, PD-36, PD-37) — a test whose managed memory is a claim
bounds it in its header, and the harness holds the runtime's own count to the
bound on both legs.** D-151's exit-0 trap counts `wild` allocations and cannot
see a managed body (TM-106), so until cycle 0.1.4b nothing in the suite
asserted managed memory at all: TM-131's `ulimit -v` pair was measured by hand
at cycle 0.0.4 and never run by the harness.

- **`// heap: FIELD OP N`** — FIELD one of `allocated`, `peak_live`, `count`,
  OP `<=` or `>=`, one bound per line — holds the line the runtime prints on fd
  2 as a process exits, by any route, when `NPK_HEAP_STATS` is in its
  environment: `heap: allocated=N peak_live=N count=N`, the bytes requested,
  the high-water mark of bytes live, and the number of allocations, `wild` and
  managed alike, at the sizes asked for (the compiler's `runtime/npkrt.ll`).
  The harness sets the variable for a file that carries a `heap:` marker and
  for no other, requires **exactly one** such line on every run of both legs
  — none is no measurement, and a second is not the runtime's — and prints the
  measured fields in the unit's verdict line. An `env:` marker may not set the
  variable: any value, `0` included, switches the report on.
- **The numbers are the program's own**, so they are exact and a bound can be
  derived from the source before it is measured: a program that allocates
  nothing reports three zeros, and neither `argv[0]` nor the environment moves
  them (measured at compiler `c3bdae2`). A number that disagrees with its
  derivation is a finding before it is a bound — `SAFETY.md` S-18f is one.
- **A remedy's ceiling travels with a work floor.** `peak_live <= N` alone is
  passed by a copy whose loop was emptied — what allocates nothing retains
  nothing, TM-131's objection to a gate an empty program passes — so each
  remedy also bounds `count` from below by the allocations its loop must make.
  **A leaking twin bounds `peak_live` from below** by its leak's arithmetic,
  which makes it the instrument's known-leaking control on every run.
- **A ceiling sits between the correct program and its leak, by one rule**:
  the geometric mean of the correct program's measured peak and a leak
  mutant's, rounded down to two significant figures, so a drift in the
  accounting and a partial leak are given the same ratio of room.
  `meta/roadmap/0.1/0.1.4b.md` §6 holds each file's numbers and §7 each mutant.
- **`peak_live` is a high-water mark.** It sees a leak only if the leaked bytes
  accumulate before the peak, so a program that leaks once, at its end,
  reports what one that frees there reports. A bound belongs on a CHURN — the
  operation under test repeated until a per-repetition leak dominates the
  peak — and nowhere else.
- **`// cap: N KiB, exit C`** runs the program again under an address-space cap
  of N KiB, `ulimit -v N`, where it must exit C: the one instrument here that
  does not read the runtime's own accounting. **Its control is the FLOOR
  PROGRAM** — a `main` that exits 0 and allocates nothing, linked against the
  same runtime — run under each cap first, and a cap it cannot start under fails
  every unit at that cap. TM-131's control was `/bin/true`, which at compiler
  `c3bdae2` no longer controls: the floor program takes HeapOom below about
  10.5 MiB, where `/bin/true` runs from 2.75 MiB. **A cap goes only on a pair
  with opposite outcomes under it**; a clean run alone is a statement the floor
  program also makes.

Six files carry a bound <!-- {{sweep: heap_bounded=6}} --> and four of them a
cap <!-- {{sweep: cap_belted=4}} --> — `SAFETY.md` S-18b and S-18c name the
twin pairs, and the two `Bytes` tests say why theirs in their own headers.
Both counts are measured denominators (TM-142).
```

**`BUILD.md`** — B-5's marker block, the `cost` stage's sentence, and B-12's
`Bytes` bullet:

<!-- apply docs: old meta/specs/BUILD.md -->
````text
// expect-golden: name       the golden file this test asserts against
// sweep-count: 7304484      the domain a `sweep` must visit (TM-122)
```
````

<!-- apply docs: to meta/specs/BUILD.md -->
````text
// expect-golden: name       the golden file this test asserts against
// sweep-count: 7304484      the domain a `sweep` must visit (TM-122)
// heap: peak_live <= 64000  a bound on NPK_HEAP_STATS's line; repeatable (V-17)
// cap: 65536 KiB, exit 92   run again under `ulimit -v 65536`; its exit there (V-17)
```

*(The last two since cycle 0.1.4b: `heap:` bounds a field of the runtime's
`heap: allocated=N peak_live=N count=N` line, `allocated`, `peak_live` or
`count`, with `<=` or `>=`, and sets `NPK_HEAP_STATS` for its file and no
other; `cap:` reruns the program under an address-space cap, beside the floor
program as its control. `TESTING.md` V-17 is the rule.)*
````

<!-- apply docs: old meta/specs/BUILD.md -->
```text
instrument cycle 0.0.4's managed-memory gate is waiting for, and the reason
that gate is a `ulimit -v` cap today).
```

<!-- apply docs: to meta/specs/BUILD.md -->
```text
instrument cycle 0.0.4's managed-memory gate is waiting for, and the reason
that gate is a `ulimit -v` cap today). *(Cycle 0.1.4b: the instrument arrived
in the runtime, and the gate is a MARKER on the file rather than the `cost`
stage — `heap:` and `cap:` in B-5, `TESTING.md` V-17 — because a stage and a
unit format for six files would split each file from its own stage and
header. The `ulimit -v` cap stays, as a belt, and is run on every invocation
for the first time. `cost` stays absent.)*
```

<!-- apply docs: old meta/specs/BUILD.md -->
```text
  `extend_str`, `put_uint` (decimal, allocation-free) and `take`. Every
```

<!-- apply docs: to meta/specs/BUILD.md -->
```text
  `extend_str`, `put_uint` (decimal, allocation-free) and `take` — an owned
  copy since cycle 0.1.4b, a view before it (`SAFETY.md` S-18f). Every
```

**`SAFETY.md`** — S-18b and S-18c's dated notes, S-18e's count, and S-18f (PD-39):

<!-- apply docs: old meta/specs/SAFETY.md -->
```text
numbers. **Take any address-space bound with a `/bin/true` control at the same
cap**, because below about 2 MiB every exit code on this machine is the
loader's.
```

<!-- apply docs: to meta/specs/SAFETY.md -->
```text
numbers. **Take any address-space bound with a `/bin/true` control at the same
cap**, because below about 2 MiB every exit code on this machine is the
loader's.

**SINCE CYCLE 0.1.4b THE HARNESS ASSERTS THE PAIR ON EVERY RUN, AND THE
CONTROL HAS MOVED** (PD-35 … PD-37, `TESTING.md` V-17). `probe06b` carries
`heap: peak_live >= 70000000` — two million 35-byte bodies, every one live at
the peak — and `probe06c` `heap: count >= 2000000` and `heap: peak_live <=
64000`, so the leak, its remedy and a remedy emptied of its work are three
different verdicts; both run again under one shared 64 MiB cap, 92 against 0.
Measured at compiler `c3bdae2`, both legs: `probe06b` `peak_live=70000024`,
`probe06c` `peak_live=59`. **The `/bin/true` control above no longer controls
for this runtime**: at `c3bdae2` a program that allocates nothing takes
HeapOom below about 10.5 MiB, where `/bin/true` runs from 2.75 MiB, so the
harness's control is that floor program, built by the same toolchain.
```

<!-- apply docs: old meta/specs/SAFETY.md -->
```text
the outgoing element into a dying scope first finishes in **1 596 KiB** and is
clean down to a 3 MiB cap.
```

<!-- apply docs: to meta/specs/SAFETY.md -->
```text
the outgoing element into a dying scope first finishes in **1 596 KiB** and is
clean down to a 3 MiB cap. *(At pin `0dfddac`. At compiler `c3bdae2` it and a
program that allocates nothing take HeapOom below about 10.5 MiB, the
runtime's own floor; `probe12`'s header has the re-measured table. Since cycle
0.1.4b the harness asserts this pair as it does S-18b's: `peak_live >=
70000000` for the overwrite, `count >= 2000000` and `peak_live <= 81000` for
the remedy, and 92 against 0 under the 64 MiB cap — measured at `c3bdae2`,
`peak_live=70000059` against `94`.)*
```

<!-- apply docs: old meta/specs/SAFETY.md -->
```text
It is the second use-after-free cycle 0.0 shipped on this library's own surface
— `vec_pop<T>` at 0.0.4 was the first (S-18d) — and both were invisible for the
```

<!-- apply docs: to meta/specs/SAFETY.md -->
```text
It is the second use-after-free cycle 0.0 shipped on this library's own surface
— `vec_pop<T>` at 0.0.4 was the first (S-18d) — *(and there was a third,
found at cycle 0.1.4b by a COUNT rather than by reading: `bytes_take`, S-18f)*
— and both were invisible for the
```

<!-- apply docs: old meta/specs/SAFETY.md -->
```text
  — but **no function in `vec.npk` returns a slice**, so there is no view for a
  caller to hold. `vec_at<T>` returns by value.
```

<!-- apply docs: to meta/specs/SAFETY.md -->
```text
  — but **no function in `vec.npk` returns a slice**, so there is no view for a
  caller to hold. `vec_at<T>` returns by value.

**Rule S-18f (PD-39) — no function hands back a `string` made by
`string_from_bytes`, because the type cannot tell a view from a copy.**
`string_from_bytes` wraps existing bytes as a VIEW — capacity 0, by the
compiler's own `BUILTIN_REFERENCE.md` row, at every pin this repository has
kept — and its answer is typed `string`, which a caller reads as owned.
`bytes_take` handed out exactly that from cycle 0.0.4 to 0.1.4b, under a
comment saying it copied. Measured at compiler `c3bdae2`, both legs: taken,
and then the sink cleared and refilled as that comment recommended, the answer
read the NEW bytes; taken, and then the sink grown, it read freed memory —
S-18e's use-after-free. **Nothing caught it**: `tests/unit/bytes_growth.npk`
read the answer only before either.

**It was found by COUNTING.** The runtime's `NPK_HEAP_STATS` reported 25
allocations for `bytes_growth` where its source makes 26 — a copy is an
allocation, and a view is not. So it is the third use-after-free cycle 0.0
shipped on this library's surface, beside `vec_pop<T>` (S-18d) and
`bytes_view` (S-18e), and the only one that reading had not found: a number an
instrument prints is derived from the source before a bound is put on it, and
one that disagrees is a finding (`TESTING.md` V-17).

`bytes_take` copies now — `string_concat` of the view and `""`, owned by that
builtin's own `Views` column — and `bytes_growth` asserts that its answer
survives both a reuse of the sink and a growth of it. **The obligation this
puts on `src/fmt/` at cycle 0.4**: a function whose answer is a `string`
returns a copy. And `FORMAT_MODEL.md` F-10's thin wrapper — fill a local
`Bytes`, hand back its text — cannot `pass bytes_take(@sink)` at `c3bdae2`
whatever `bytes_take` does, because the borrow tracker refuses the call's
answer as a borrow of `sink` (`NITPICK-BORROW-001`), while a take that
consumes the sink by `move` compiles and is correct on both legs; measured,
and carried to `meta/roadmap/0.4/README.md`.
```

**`tests/probe/README.md`** — the rule that promised the `peak_live` bound:

<!-- apply docs: old tests/probe/README.md -->
```text
  over an owning container therefore asserts its memory too — a `ulimit -v` cap
  today, a `peak_live` bound from the compiler's `NPK_HEAP_STATS` after the
  1.5.1b re-pin. **Assert the cap, not a peak-RSS number:**
```

<!-- apply docs: to tests/probe/README.md -->
```text
  over an owning container therefore asserts its memory too — **since cycle
  0.1.4b by a `heap:` marker on the runtime's own `NPK_HEAP_STATS` count and a
  `cap:` marker under a 64 MiB address space** (`../../meta/specs/TESTING.md`
  V-17), where this said *"a `ulimit -v` cap today, a `peak_live` bound from
  the compiler's `NPK_HEAP_STATS` after the 1.5.1b re-pin"* and neither was
  run by the harness. `probe06b`/`probe06c` and `probe12`/`probe12b` carry
  both. **What follows was the rule until then — assert the cap, not a
  peak-RSS number:**
```

**`harness/README.md`** — the files table, the self-check's cases, the two
things a green run did not mean, and the 0.1.4b paragraph:

<!-- apply docs: old harness/README.md -->
```text
| `stages.py` | the marker grammar, and the `program`, refusal, `parse`, `golden` and `sweep` stages |
```

<!-- apply docs: to harness/README.md -->
```text
| `stages.py` | the marker grammar, and the `program`, refusal, `parse`, `golden` and `sweep` stages — and, since cycle 0.1.4b, the `heap:` and `cap:` markers: the runtime's own `NPK_HEAP_STATS` line held to a file's bounds, and the address-space belt with the floor program as its control (`TESTING.md` V-17) |
```

<!-- apply docs: old harness/README.md -->
```text
| `selfcheck.py` | **the only thing here that demonstrates the checks can fail.** V-14's eight cases, the tree checks on planted violations, and the arm generator against the compiler |
```

<!-- apply docs: to harness/README.md -->
```text
| `selfcheck.py` | **the only thing here that demonstrates the checks can fail.** V-14's nine cases, the tree checks on planted violations, and the arm generator against the compiler |
```

<!-- apply docs: old harness/README.md -->
```text
- **V-14's eight cases** — a wrong `expect-exit`, a missing code, an unexpected
  code (D-237), a golden differing by one byte, a file that does not parse, a
  sweep that ran short, and a program whose `failsafe` has been deleted. Case 6
```

<!-- apply docs: to harness/README.md -->
```text
- **V-14's nine cases** — a wrong `expect-exit`, a missing code, an unexpected
  code (D-237), a golden differing by one byte, a file that does not parse, a
  sweep that ran short, a program whose `failsafe` has been deleted, and —
  since cycle 0.1.4b — a program whose managed memory disagrees with its
  header, five ways beside one control (PD-38). Case 6
```

<!-- apply docs: old harness/README.md -->
```text
- **Not a MEMORY result for the managed half.** D-151's exit-0 trap counts
  `wild` allocations and a `buffer` is managed (TM-106), so a green run says
  nothing about `Bytes` (`SAFETY.md` S-18b).
- **Not that a view into a `Bytes` is used correctly.** Every gate here is a
  leak gate and a use-after-free is a WRONG ANSWER (S-18e, TM-139). Two shipped
  in cycle 0.0 and both were found by reading, not by a gate.
```

<!-- apply docs: to harness/README.md -->
```text
- **Not a MEMORY result for the managed half — except for the files whose
  headers bound it.** D-151's exit-0 trap counts `wild` allocations and a
  `buffer` is managed (TM-106), so exit 0 says nothing about `Bytes`
  (`SAFETY.md` S-18b). Since cycle 0.1.4b six files <!-- {{sweep: heap_bounded=6}} -->
  are held to the runtime's own `NPK_HEAP_STATS` count on both legs, and four
  <!-- {{sweep: cap_belted=4}} --> again under a 64 MiB cap (`TESTING.md` V-17);
  every other file's managed memory is asserted by nothing.
- **Not that a view into a `Bytes` is used correctly.** Every gate here is a
  leak gate and a use-after-free is a WRONG ANSWER (S-18e, TM-139). Two shipped
  in cycle 0.0 and both were found by reading, not by a gate — and a third
  shipped with them, `bytes_take`'s answer, found at cycle 0.1.4b by the heap
  instrument's COUNT (S-18f).
```

<!-- apply docs: old harness/README.md -->
```text
The floor under all of it is still TM-117's: every root re-emits the prelude,
```

<!-- apply docs: to harness/README.md -->
```text
**At cycle 0.1.4b, no unit added**: the unit count is the subcycle's before
it, because nothing joined the suite — what changed is what six units assert
and what the self-check plants. The self-check plants **8** of V-14's **9**
cases: case 9, a program whose managed memory disagrees with its header,
joined (PD-38). The tree checks still run `11 live`, and `check_denominators`
measures two more denominators, `heap_bounded` and `cap_belted`. The two twin
pairs and the two `Bytes` tests carry `heap:` bounds on the runtime's own
`NPK_HEAP_STATS` line, and the twins a `cap:` belt under 64 MiB, each unit's
verdict line printing what it measured (`TESTING.md` V-17).

The floor under all of it is still TM-117's: every root re-emits the prelude,
```

**`CLAUDE.md`** — the status paragraph, the `bytes_view` bullet's count, the
self-check's, and TM-106's bullet:

<!-- apply docs: old CLAUDE.md -->
```text
**Status, after cycle 0.1.4: the civil cross-oracle.** Every date of years
```

<!-- apply docs: to CLAUDE.md -->
```text
**Status, after cycle 0.1.4b: the managed-memory gate.** For the first time
the harness asserts MANAGED memory. D-151's exit-0 trap sees only `wild` blocks
(TM-106), and the `ulimit -v` pair that stood in for a gate since cycle 0.0.4
(TM-131) was measured by hand and never run. Now a `// heap:` marker holds the
runtime's own `NPK_HEAP_STATS` report — bytes requested, the high-water mark
of bytes live, allocations — to a file's bounds on both legs, and a `// cap:`
marker runs a program again under a 64 MiB address-space cap (`TESTING.md`
V-17). Six files carry bounds <!-- {{sweep: heap_bounded=6}} -->: each leaking
twin's high-water mark is at least its leak's arithmetic, so the instrument
meets a known leak on every run; each remedy's is under a ceiling and its
allocation count over a floor, so an emptied remedy is red; and the two `Bytes`
tests' ceilings catch a growth that keeps its old bodies, which
`bytes_growth` otherwise passes at exit 0. Every ceiling is the geometric mean
of the correct program's peak and a leak mutant's (PD-36), and every bound was
seen red on its mutant (`meta/roadmap/0.1/0.1.4b.md` §7). **The cap's control
moved**: at compiler `c3bdae2` a program that allocates nothing needs about
10.5 MiB before `main`, where `/bin/true` needs 2.75 MiB, so the control is
that floor program, built by the same toolchain (PD-37). **And the count found
a defect**: `bytes_take` handed back a VIEW of the sink typed as an owned
`string` — reused or grown, the sink rewrote or freed the caller's text —
from cycle 0.0.4 until now. It copies now, and `bytes_growth` asserts both
(PD-39, `SAFETY.md` S-18f). The self-check plants **8** of V-14's **9**
faults; no test file was added, so the unit count is 0.1.4's.

**After cycle 0.1.4: the civil cross-oracle.** Every date of years
```

<!-- apply docs: old CLAUDE.md -->
```text
  is a LEAK gate, and a use-after-free is found by a WRONG ANSWER.** Cycle 0.0
  shipped two of them — this and `vec_pop<T>` — and both were found by reading,
  under a green suite. `tests/unit/bytes_view_lifetime.npk` is the pair.
```

<!-- apply docs: to CLAUDE.md -->
```text
  is a LEAK gate, and a use-after-free is found by a WRONG ANSWER.** Cycle 0.0
  shipped two of them — this and `vec_pop<T>` — and both were found by reading,
  under a green suite. `tests/unit/bytes_view_lifetime.npk` is the pair.
  **It shipped a third, and no reading found it**: `bytes_take` handed back a
  view of the body as an owned `string` until cycle 0.1.4b, found by the heap
  instrument's COUNT — 25 allocations where the source makes 26 (S-18f). It
  copies now.
```

<!-- apply docs: old CLAUDE.md -->
```text
V-15) and plants **seven** of V-14's eight faults — case 6 is `PEND` until 0.5
```

<!-- apply docs: to CLAUDE.md -->
```text
V-15) and plants **eight** of V-14's nine faults (seven of eight until cycle
0.1.4b's case 9, PD-38) — case 6 is `PEND` until 0.5
```

<!-- apply docs: old CLAUDE.md -->
```text
- **`exit 0` says nothing about managed memory** (TM-106). D-151 watches `wild`
  allocations only. A `Vec<string>` whose block is freed and whose elements are
  not retained 125 MiB over two million elements **and exited 0**.
```

<!-- apply docs: to CLAUDE.md -->
```text
- **`exit 0` says nothing about managed memory** (TM-106). D-151 watches `wild`
  allocations only. A `Vec<string>` whose block is freed and whose elements are
  not retained 125 MiB over two million elements **and exited 0**. What says
  something, since cycle 0.1.4b, is the runtime's own `NPK_HEAP_STATS` line,
  held to a `heap:` marker's bounds — and only for a file that carries one.
```

**`.github/workflows/ci.yml`** — the header's self-check count and its memory
sentence:

<!-- apply docs: old .github/workflows/ci.yml -->
```text
# V-15): SEVEN of V-14's eight faults planted in scratch trees -- case 6 is
```

<!-- apply docs: to .github/workflows/ci.yml -->
```text
# V-15): EIGHT of V-14's nine faults planted in scratch trees (seven of eight
# until cycle 0.1.4b's case 9, a program whose managed memory disagrees with
# its header) -- case 6 is
```

<!-- apply docs: old .github/workflows/ci.yml -->
```text
# a green run is not a MEMORY result for the managed
# half: D-151's exit-0 trap counts `wild` allocations and a `buffer` is managed
# (TM-106), so `exit 0` says nothing about `Bytes` (S-18b, S-18e).
```

<!-- apply docs: to .github/workflows/ci.yml -->
```text
# a green run is not a MEMORY result for the managed
# half: D-151's exit-0 trap counts `wild` allocations and a `buffer` is managed
# (TM-106), so `exit 0` says nothing about `Bytes` (S-18b, S-18e) -- except
# that, since cycle 0.1.4b, six files {{sweep: heap_bounded=6}} are held on
# both legs to the runtime's own NPK_HEAP_STATS count, and four
# {{sweep: cap_belted=4}} again under a 64 MiB cap (`TESTING.md` V-17).
```

**`src/cal/cal.npk`** — a comment that counts cycle 0.0's use-after-frees:

<!-- apply docs: old src/cal/cal.npk -->
```text
// and by nothing else** -- and cycle 0.0 shipped two of them on this library's
// own public surface, `vec_pop<T>` (S-18d) and `bytes_view` (S-18e), both under
// a green suite, a review and an independent verification.
```

<!-- apply docs: to src/cal/cal.npk -->
```text
// and by nothing else** -- and cycle 0.0 shipped two of them on this library's
// own public surface, `vec_pop<T>` (S-18d) and `bytes_view` (S-18e), both under
// a green suite, a review and an independent verification. (And a third,
// `bytes_take`'s answer, found at cycle 0.1.4b by the heap instrument's COUNT
// rather than by a test that reads -- S-18f.)
```

**`ROADMAP.md`** — cycle 0.0's count:

<!-- apply docs: old meta/roadmap/ROADMAP.md -->
```text
**Two things it got wrong and found itself**, both use-after-frees on the
public surface — `vec_pop<T>` (TM-136) and `bytes_view` (TM-139) — and both
found by READING, under a green suite and an independent verification. The
```

<!-- apply docs: to meta/roadmap/ROADMAP.md -->
```text
**Two things it got wrong and found itself**, both use-after-frees on the
public surface — `vec_pop<T>` (TM-136) and `bytes_view` (TM-139) — and both
found by READING, under a green suite and an independent verification. *(A
third shipped with them and was not found in the cycle: `bytes_take`'s answer
was a view typed as an owned `string`, found at cycle 0.1.4b by the heap
instrument's count, `SAFETY.md` S-18f.)* The
```

**`meta/roadmap/0.4/README.md`** — what cycle 0.4 meets in F-10's wrapper:

<!-- apply docs: old meta/roadmap/0.4/README.md -->
```text
- **`Bytes` and `Vec<T>` are indexed WITHOUT a bounds check** (TM-108, S-17b).
```

<!-- apply docs: to meta/roadmap/0.4/README.md -->
```text
- **`FORMAT_MODEL.md` F-10's `string`-returning wrapper cannot be spelled the
  obvious way** — measured at compiler `c3bdae2`, cycle 0.1.4b's planning. A
  function that fills a LOCAL `Bytes` and ends `pass raw bytes_take(@sink);` is
  refused `NITPICK-BORROW-001` (D-004 rule 2): the borrow tracker takes the
  call's answer for a borrow of `sink`, whatever the callee does — refused
  with `bytes_take` a copy, and with the answer bound to a local first. **A
  take that CONSUMES the sink** — `string(move Bytes:b)`, copying out before
  `b` dies — compiles, and the wrapper built on it is correct on both legs.
  So the wrapper wants a consuming take, a public name this cycle chooses with
  a caller in hand (TM-013), or a re-measurement at the pin it plans against.
  And every `string` it hands back is a copy (`SAFETY.md` S-18f).
- **`Bytes` and `Vec<T>` are indexed WITHOUT a bounds check** (TM-108, S-17b).
```

<!-- apply decisions: old meta/DECISIONS.md -->
```text
### TM-131 — `ulimit -v` cannot measure the remedy half AT ALL, because its bound and `/bin/true`'s are the same bound; the element-drop gate is the LEAKING half's refusal and the peak-RSS pair
**2026-09-05, cycle 0.0.4. Refines TM-128, which is confirmed in every figure
```

<!-- apply decisions: to meta/DECISIONS.md -->
```text
### TM-131 — `ulimit -v` cannot measure the remedy half AT ALL, because its bound and `/bin/true`'s are the same bound; the element-drop gate is the LEAKING half's refusal and the peak-RSS pair
> **SUPERSEDED IN PART by TM-<PD-37> (@DATE@).** Its CONTROL, not its
> conclusion. `/bin/true` measured the dynamic loader at `0dfddac`; at
> compiler `c3bdae2` a Nitpick program that allocates nothing takes HeapOom
> below about 10.5 MiB, where `/bin/true` runs from 2.75 MiB, so the control
> for this runtime is that floor program. The pair — one shared 64 MiB cap,
> opposite outcomes — stands, and since cycle 0.1.4b the harness runs it. The
> text below is left exactly as written.
**2026-09-05, cycle 0.0.4. Refines TM-128, which is confirmed in every figure
```

<!-- apply decisions: old meta/DECISIONS.md -->
```text
*Declined:* the check live now — 10.7 s every run, and every run and CI then
depending on the host's Python, with V-14's case 6 to be planted against the
first of two generators before the second exists; never — `tools/README.md`'s
rule, and a hand-edited corpus is exactly the edit a red cross-oracle invites;
a recorded `sha256` checked on every run — it sees an edit to the corpus and
not a generator changed without regenerating, and it is a second statement of
the file's identity to keep in step.
```

<!-- apply decisions: to meta/DECISIONS.md -->
```text
*Declined:* the check live now — 10.7 s every run, and every run and CI then
depending on the host's Python, with V-14's case 6 to be planted against the
first of two generators before the second exists; never — `tools/README.md`'s
rule, and a hand-edited corpus is exactly the edit a red cross-oracle invites;
a recorded `sha256` checked on every run — it sees an edit to the corpus and
not a generator changed without regenerating, and it is a second statement of
the file's identity to keep in step.

---

# Cycle 0.1.4b — the managed-memory gate, ratified @DATE@

Five decisions, drafted at planning (`meta/roadmap/0.1/0.1.4b.md` §14, PD-35 …
PD-39, in that order) and recorded here by the worker in the commit that makes
the change each describes. Every measurement is at compiler `c3bdae2`, taken
at planning and re-derived at execution by the plan's `baseline.py` and
`mut.py` — the facts of `0.1.4b.md` §11 and the matrix of §7, line for line.

### TM-<PD-35> — the harness reads the runtime's `NPK_HEAP_STATS` line through a per-file `// heap: FIELD OP N` marker; it sets the variable for a file that carries one and for no other, requires exactly one line on every run of both legs, and prints what it measured in the unit's verdict line; an `env:` marker may not set it

**@DATE@, cycle 0.1.4b (PD-35). Adds `TESTING.md` §10's V-17 and two markers
to `BUILD.md` B-5.** D-151's exit-0 trap counts `wild` allocations and cannot
see a managed body (TM-106), so until this cycle the harness asserted no
managed memory at all: `meta/roadmap/0.1/0.1.0.md` §8 carried the gate from
cycle 0.0 on the reading that the instrument was not in the pin, and it was —
at `aaffb87` and at `c3bdae2`. Read from the compiler's `runtime/npkrt.ll` at
`c3bdae2` and measured there: with `NPK_HEAP_STATS` in its environment a
program prints one line on fd 2 as it exits, by a clean `exit` or through a
trap's `failsafe` — `heap: allocated=N peak_live=N count=N`, the bytes
requested, the high-water mark of bytes live and the number of allocations,
`wild` and managed alike, at the sizes asked for — and nothing when the
variable is absent. The numbers are the program's own: a program that
allocates nothing prints three zeros, and a 402-character-longer `argv[0]` or
a 20 000-byte variable moves none of them. **The variable is switched on by its
NAME**: `NPK_HEAP_STATS=0` and an empty value both print the line.

*Declined:* the compiler's `cost` stage and its TOML units — a stage and a
unit format for six files, splitting each from the stage its other
expectations live in and from its header, where B-5 puts every expectation;
and its ratio between two programs states neither a leaking twin's floor nor a
remedy's work floor (TM-<PD-36>); the variable in `BASE_ENV` for every program
— every program's stderr gains a line nobody asserts, and every program's
environment a variable the two `environ()` probes read, a measurement taken
and thrown away; `// env: NPK_HEAP_STATS=1` as the switch beside a bound — two
markers that must agree, a bound without its switch failing for a reason the
header cannot show, and `=0` switching the report ON, so the spelling reads as
a switch it is not; the LAST `heap:` line, as the compiler's harness reads it
— a program printing a line of that shape would be read in the runtime's
place, and requiring exactly one costs nothing; exact equality — the file
becomes a test of the allocator's accounting, which `bytes_view_lifetime.npk`
already argues against for `!=` over `== 170`.

### TM-<PD-36> — six files carry bounds: each leaking twin's `peak_live` at least its leak's arithmetic; each remedy's `peak_live` under a ceiling and its `count` over the allocations its loop must make; the two `Bytes` tests' `peak_live` under a ceiling; every ceiling the geometric mean of the correct program's measured peak and a leak mutant's, rounded down to two significant figures

**@DATE@, cycle 0.1.4b (PD-36).** `0.1.0.md` §8 named four: the two twin pairs
and the two `Bytes` tests, six files. Measured at `c3bdae2`, identical on both
legs, and each the arithmetic of its source: `probe06b` 70 000 024 bytes at its
peak — two million 35-byte bodies and the 24-byte block — and `probe06c` 59,
one trip's block and body; `probe12` 70 000 059 and `probe12b` 94;
`bytes_growth` 2 048 636 and `bytes_view_lifetime` 160. The leak mutants
measured: each twin for its remedy; a `bytes_reserve` that keeps every old
body, 3 097 238 and 214. So the ceilings are 64 000, 81 000, 2 500 000 and 180,
and the floors 70 000 000 on both leaking twins. **The work floor is what
makes a remedy's ceiling a gate**: a remedy whose loop was emptied allocates
nothing and so retains nothing — `count=0 peak_live=0`, measured — and passes
a ceiling alone, TM-131's objection to a gate an empty program passes, met again
at the new instrument. **The `Bytes` ceilings are the only thing in their files
that sees a growth keeping its old bodies** where `bytes_growth` exits 0, and
`bytes_view_lifetime` sees it only through the allocator's poison (exit 26).

*Declined:* a ceiling alone on each remedy — fooled by the emptied remedy,
measured; twice the measured peak — it does not separate the `Bytes` tests
from their mutant, 4 097 272 above 3 097 238 and 320 above 214, measured
fooled; the tightest separating bound, the correct peak itself — one
allocation added anywhere, or any change to the accounting, reddens a file
whose subject is not the allocator; one number per pair, the leaking twin's
floor at the same geometric mean — a probe asserts its answer (P-6), and the
answer is every one of two million bodies live, where a floor of 64 000 would
pass a leak of two thousand; bounding only the twin pairs, as TM-131's gate did
— `bytes_growth` would then see nothing of its mutant; leaving
`bytes_view_lifetime` unbounded because its exit 26 already sees the mutant —
exit 26 reads the POISON (D-183), so a runtime that stopped poisoning freed
memory would turn a correct release into a red, and the bound reads the count;
`allocated` as the remedies' work floor — the same verdict on every row, but
it restates the prelude's string size where `count` states the loop's own
arithmetic, one allocation per trip.

### TM-<PD-37> — the `ulimit -v` pair is run by the harness as a belt: a `// cap: N KiB, exit C` marker on the two twin pairs, 64 MiB, 92 for the leaking halves and 0 for the remedies, on both legs; its control is the FLOOR PROGRAM built by the same toolchain, run under each cap first, superseding `/bin/true` as TM-131's control

**@DATE@, cycle 0.1.4b (PD-37). Refines TM-131, whose conclusion stands and
whose control is replaced by measurement.** The pair has been this
repository's managed-memory gate since cycle 0.0.4, measured by hand and never
run by the harness. Measured at `c3bdae2`: under 64 MiB the leaking halves
take HeapOom (92) and the remedies exit 0, on both legs. **And `/bin/true` no
longer controls**: a program that allocates nothing takes HeapOom below about
10.5 MiB — the runtime's own floor, grown since `0dfddac` — while `/bin/true`
runs from 2.75 MiB; at 10 240 KiB and below, the remedies and that floor
program take 92. So between the two, a 92 is the runtime's and not a leak, and a belt
controlled by `/bin/true` would report a correct remedy as leaking (at 8 192
KiB, measured). The control is therefore the floor program, `harness/stages.py`'s
`CAP_CONTROL`, in `tests/probe/probe11d_floor_only.npk`'s shape.

*Declined:* leaving the cap manual — a belt nothing runs is a rule that asks a
reader to remember, and this one had asked since 0.0.4; retiring it — it is
the one instrument that does not read the runtime's accounting, and the
runtime's 10.5 MiB floor is exactly what the count cannot see; `/bin/true` as
the control — measured to call a correct remedy a leak at 8 192 KiB;
`probe11d_floor_only.npk` by path — the self-check's scratch trees have no
such file, and a control that exists in one tree is not the harness's; caps on
the `Bytes` tests — no leaking twin, so no opposite outcomes, and a clean run
alone is a statement the floor program also makes (TM-131).

### TM-<PD-38> — V-14 gains a ninth case, this repository's own: a program whose managed memory disagrees with its header, planted five ways in one inner run beside a control held at both bounds of every field

**@DATE@, cycle 0.1.4b (PD-38). Amends `TESTING.md` V-14 and the self-check's
counts, `V14_CASES` 9 and `PLANTED_CASES` 8.** The plants: a `peak_live`
ceiling one byte under the measurement, a `count` floor one allocation over
it, a run whose report never reaches stderr (fd 2 closed before the exit), a
`cap:` exit the program does not take, and a 1 KiB cap under which the floor
program cannot start. The control holds one 4 096-byte buffer and carries
every field's `<=` and `>=` at exactly its measured value and a 64 MiB cap, so
each comparison is shown inclusive and the reds are the plants'.

*Declined:* commissioning the markers in part D beside the verdict mechanisms
— a marker in a test's header is V-14's subject, and case 7 is this shape for
`sweep-count`; the ceiling alone — the floor, the missing line and the cap are
separate code paths, each a way to be green and wrong; a real low cap for the
machine plant, 2 048 KiB say — what starts there depends on the runtime and
the machine, where under 1 KiB nothing starts anywhere (signal 11, measured).

### TM-<PD-39> — `bytes_take` hands back an owned copy, `string_concat` of a `string_from_bytes` view and `""`, and `bytes_growth` asserts that the answer survives a reuse and a growth of its sink; `SAFETY.md` gains S-18f

**@DATE@, cycle 0.1.4b (PD-39). A library defect found at planning, by this
cycle's instrument.** `bytes_take` was `pass string_from_bytes(b.body.ptr,
b.len);` under a comment saying it copied, from cycle 0.0.4. `string_from_bytes`
wraps bytes as a VIEW — capacity 0, by the compiler's own
`BUILTIN_REFERENCE.md`, at every pin this repository has kept — so the answer
was the sink's body typed as an owned `string`. Measured at `c3bdae2`, both
legs: taken, then the sink cleared and refilled, the answer read the new bytes
(exit 13); taken, then the sink grown, it read freed memory (exit 12).
`bytes_growth` read the answer only before either. **Found by counting**:
`NPK_HEAP_STATS` reported 25 allocations for `bytes_growth` where its source
makes 26. The copy allocates the prefix and is `never fails`; the arm bills do
not move — `bytes.npk` owes 12 identities and the umbrella 13, before and
after, by `NITPICK-REACH-003`'s own list. And the copy does not make
`FORMAT_MODEL.md` F-10's wrapper spellable: a formatter's `pass raw
bytes_take(@sink)` is refused `NITPICK-BORROW-001` whatever the callee does,
while a take that consumes the sink by `move` compiles and is correct on both
legs — cycle 0.4's to choose, carried to its "Watch for".

*Declined:* documenting `bytes_take` as a view — its type says owned, and
`bytes_view` already is the view, typed as a slice the borrow tracker follows;
`view.clone()` or `string_slice` — each returns `Result<string>`
(`NITPICK-TYPE-007`, measured for `clone`), which a `never fails` take would
have to discard; a consuming `bytes_into(move Bytes)` now — the shape F-10's
wrapper will want, but a public name chosen without its caller is a MAJOR
commitment chosen without a requirement (TM-013); removing `bytes_take` —
MAJOR too, and F-10 needs the handover it names; a separate subcycle — the
defect is this cycle's instrument's finding, the fix is two lines, and
`bytes_growth`'s bound would otherwise be derived on a take that allocates
nothing and re-derived a subcycle later.
```

<!-- script: renumber.py -->
```python
#!/usr/bin/env python3
"""0.1.4b (`0.1.4b.md` §9 step 5): the draft labels PD-35 ... PD-39 become the
next TM numbers, in order. In `meta/DECISIONS.md` the new section's headings
and cross-references are written `TM-<PD-35>` ... and its date `@DATE@`; each
becomes its number and the date given, and its `(PD-35)` provenance labels
stay, as TM-179 ... TM-183 keep theirs. EVERYWHERE ELSE outside this plan,
every `PD-35` ... `PD-39` becomes its number. 0.1.4's `renumber.py`, extended.

    renumber.py FIRST_TM DATE

FIRST_TM is read by the plan's gate; DATE is the day of the work commit,
YYYY-MM-DD. Tracked AND untracked files -- `git grep` skips untracked files
unless told. Prints every file it changed and the counts; exit 1 if a label or
a placeholder is left, or a heading is missing."""
import os, re, subprocess, sys

REPO = os.environ["REPO"]
first, date = int(sys.argv[1]), sys.argv[2]
if not re.fullmatch(r"\d{4}-\d{2}-\d{2}", date):
    sys.exit("STOP: DATE must be YYYY-MM-DD, not %r" % date)
pd0, count = 35, 5
MAP = {"PD-%d" % (pd0 + i): "TM-%d" % (first + i) for i in range(count)}
LABEL = re.compile(r"\b(?:%s)\b" % "|".join(re.escape(k) for k in MAP))
PATTERN = "|".join(r"PD-%d\b" % (pd0 + i) for i in range(count))
PLAN, DEC = "meta/roadmap/0.1/0.1.4b.md", "meta/DECISIONS.md"

path = os.path.join(REPO, DEC)
dec = open(path, encoding="utf-8").read()
placeholders = 0
for k, v in MAP.items():
    placeholders += dec.count("TM-<%s>" % k)
    dec = dec.replace("TM-<%s>" % k, v)
dates = dec.count("@DATE@")
dec = dec.replace("@DATE@", date)
open(path, "w", encoding="utf-8").write(dec)
print("  %-44s %d placeholder(s), %d date(s)" % (DEC, placeholders, dates))

files = subprocess.run(["git", "-C", REPO, "grep", "-l", "--untracked", "-E", PATTERN],
                       stdout=subprocess.PIPE, text=True).stdout.split()
changed = 0
for rel in files:
    if rel in (PLAN, DEC):
        continue
    p = os.path.join(REPO, rel)
    text = open(p, encoding="utf-8").read()
    new, n = LABEL.subn(lambda m: MAP[m.group(0)], text)
    open(p, "w", encoding="utf-8").write(new)
    changed += n
    print("  %-44s %d label(s)" % (rel, n))
left = subprocess.run(["git", "-C", REPO, "grep", "-l", "--untracked", "-E", PATTERN],
                      stdout=subprocess.PIPE, text=True).stdout.split()
left = [r for r in left if r not in (PLAN, DEC)]
missing = [v for v in MAP.values() if not re.search(r"^### %s — " % v, dec, re.M)]
stale = "TM-<PD-" in dec or "@DATE@" in dec
print("labels renumbered: %d; PD-%d ... PD-%d -> TM numbers %d ... %d"
      % (changed, pd0, pd0 + count - 1, first, first + count - 1))
if missing:
    print("  STOP: meta/DECISIONS.md declares none of: %s" % ", ".join(missing))
if left:
    print("  STOP: labels left in: %s" % ", ".join(left))
if stale:
    print("  STOP: a placeholder is left in meta/DECISIONS.md")
sys.exit(1 if missing or left or stale else 0)
```

### 10.2 The stale-claim sweep — a stale claim found once is a list

Run after step 5, and read every hit. **Each must be history — dated, in a
settled decision, in an older subcycle's record, in a `CLAUDE.md` or
`harness/README.md` paragraph about an earlier cycle — or have been changed by
this subcycle; any other is a stop.** `PLAYBOOK.md` §12: for each thing a
commit moves or resizes, sweep for statements of its old place and its old
size. The counts come from the command, never from a reading of its output.

```bash
cd "$REPO"
X=(-- . ':!meta/roadmap/0.1/0.1.[0-4]*.md' ':!meta/roadmap/done/*' ':!tests/probe/defect/*/TRANSCRIPT.txt')
git grep -n -i --untracked -E "seven of V-14|V-14's eight|V-14 names eight|SEVEN OF THE EIGHT|eight cases|fail SEVEN" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "cap today|after the 1\.5\.1b re-pin|is waiting for|is (still )?not (in|at) (the )?pin" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "\b3 MiB|clean down to" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "shipped two|two shipped|second use-after-free" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "It copies|take copies|copies out|owned and independent" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "not a MEMORY|memory result|says nothing about .Bytes" "${X[@]}" | cut -c1-160
git grep -n -i --untracked -E "four leak|four pairs|/bin/true" "${X[@]}" | cut -c1-160
```

**At planning, after step 5, every hit read as one of these, and nothing
else:**

| Search | Lines at planning | Hits that are LIVE, and the edit they got | Hits that are history, left as they are |
|---|---|---|---|
| the self-check's old counts | 3 | none — every live statement moved in §6 and §10 | `run.py`'s comment heading and `TESTING.md` V-14's paragraph heading, each followed now by its dated update; `selfcheck.py`'s note on the first run of `check_layering`'s node half "in every one of the eight cases" |
| the gate promised and not run | 6 | none | `BUILD.md`'s `cost` sentence and `tests/probe/README.md`'s rule, each quoting the old words beside the new; the cycle README's "Why 0.1.4b exists"; an unrelated "waiting for nothing" in `DECISIONS.md` |
| the 3 MiB remedy | 11 | none | TM-128 and TM-131, settled; `SAFETY.md` S-18b's TM-131 history and S-18c with its dated note; `probe12`'s `0dfddac` table above its `c3bdae2` one; `probe12b`'s sentence above its dated note |
| cycle 0.0's two use-after-frees | 4 | all four — `CLAUDE.md`, `harness/README.md`, S-18e and `cal.npk` — each now names the third | — |
| `bytes_take` copies | 4 | none — `bytes.npk`'s "`take` copies out" and `bytes_growth`'s section heading are true since the fix | `bytes.npk`'s quotation of the old words; `CLAUDE.md`'s new paragraph |
| a green run and managed memory | 6 | all six — the CI header, `harness/README.md` and `run.py` each say which files it is a memory result for | — |
| four pairs; `/bin/true` | 38 | none | TM-128 and TM-131, settled — TM-131 gains its marker at step 5; `SAFETY.md` S-18b's rule text, followed by its 0.1.4b paragraph; `probe06_generic_vec.npk`'s dated 0.0.x header; `probe12`'s `0dfddac` table; and this subcycle's own text — `CLAUDE.md`, `build.py`, `stages.py`, `TESTING.md` V-17, S-18b's new paragraph, the new decision, the cycle README |

**At planning the seven searches printed 3, 6, 11, 4, 4, 6 and 38 lines**,
each read. Two patterns were tightened after the first pass, because `3 MiB`
matched `473 MiB` and `two of them` matched prose about other things; the
counts are the tightened commands', as printed above. **The raw output is not
quoted here**: some of its lines carry whole sweep tags, and a tag written
whole in this file is a claim `check_denominators` would hold the tree to.

### 10.3 By hand, at the close only

| File | What changes |
|---|---|
| this file | the title `DONE`; §13's boxes; the execution record |
| `meta/roadmap/0.1/README.md` | in the record commit: 0.1.4b's checklist ticked, its row `DONE` with the CI run |
| `meta/roadmap/ROADMAP.md` | in the record commit: the 0.1 row — the managed-memory gate done, 0.1.3c next against the pin carrying the compiler's 1.6.0 step 3h, then the close |

---

## 11. The pin — what moves with it, and step 1's re-baseline

**Every number this plan rests on, what it rests on, and whether the pin
carrying the compiler's 1.6.0 steps 3g and 3h can move it.** Step 0 checks the
anchor and step 1 re-derives everything in `baseline.py` before anything is
changed; the matrix (step 4) and the full run (step 7) re-derive the rest.

| The number | Where this plan states it | It rests on | At the pin carrying 3h |
|---|---|---|---|
| the anchor, 72 576 bytes, `162b8975…` | the gate, step 0 | the runtime object | **must be identical** — the compiler plans no floor move tonight, and `runtime/npkrt.ll` is byte-identical from `c3bdae2` to its head at planning, `6fb85d3`. Otherwise stop |
| part A: the six files' lines on both legs; the floor, held and silent programs; `argv[0]`, the environment, the spellings, the trapped exit | §2, §6.3, the six headers, the documents, the decisions | the runtime's counters, AND `npkc`'s emission of each program's allocations | **expected identical**: 3g and 3h add refusals and move where a `fixed` binding's type resolves, and none of these programs writes through a lent owning parameter or imports a `fixed` table. A difference is a stop |
| part B: the cap table | §3, `probe12`'s header, PD-37's text | the runtime's address space at startup; the machine's loader, for `/bin/true` | **expected identical**. The 64 MiB column is what the belt runs on; a difference anywhere is a stop |
| part C: `take_reuse` 13 and `take_grown` 12 | §4, S-18f, the decisions | the runtime's `string_from_bytes` | **identical** — the runtime |
| part C: the four ways out of a formatter's frame | §4, `bytes.npk`'s comment, S-18f, 0.4's "Watch for", PD-39's text | `npkc`'s borrow tracker — and 3g changes the loan rules (DEF-102) | **MAY MOVE.** The documents date the measurement to `c3bdae2`, so they stay true, but a moved line changes what cycle 0.4 is told: stop, and report the new lines for the hunk to be amended |
| the matrix, §7 | §7, §6.3, the headers | as part A | identical, or stop |
| the harness's totals: the unit count, the parse split, the defect corpus, the exemption verdicts | steps 0, 3 and 7's blocks | `npkc`'s refusals — at the pin carrying 3h the adoption moves `fixed_move_out`'s three exemptions to asserted refusals, `NITPICK-TYPE-084` | **MOVE.** Step 0 must be `GREEN`; steps 3 and 7 may then differ from this plan's blocks only in lines naming those totals, each by exactly what step 0's run shows — so step 7 is compared with step 0's run, the unit count equal and the new evidence present, not with the planning numbers |
| the arm bills, 12 and 13 | §4 | `NITPICK-REACH-003` | re-derived on every run by `check_failsafe_arms` |
| the floor program's six arms | `stages.py`'s `CAP_CONTROL`, `selfcheck.py`'s `FAILSAFE` | the floor `NITPICK-REACH-002` demands at the pin | if the pin's floor grows, case 9 and the first capped unit fail to build it and REACH names the arm — the adoption subcycle's sweep, and a stop here |

`same.py` compares a saved output with a block this plan expects, line for
line, with each wall-clock figure (`N.N s`) set aside:

<!-- script: same.py -->
```python
#!/usr/bin/env python3
"""0.1.4b: a saved output against the block this plan expects, line for line.

    same.py NAME FILE

reads the fenced block after the marker line `<!-- expect: NAME -->` in the
plan and the lines of FILE, sets every wall-clock figure (`12.3 s`) aside in
both, and prints each line that differs, both ways, then `NAME: N line(s)
expected, D difference(s)`. Exit 0 only at 0 -- so the comparison is the
script's and not a reading."""
import os, re, sys

REPO = os.environ["REPO"]
PLAN = os.environ.get("PLAN", os.path.join(REPO, "meta/roadmap/0.1/0.1.4b.md"))
name, path = sys.argv[1], sys.argv[2]
text = open(PLAN, encoding="utf-8").read()
m = re.search(r"^<!-- expect: %s -->\n(`{3,})[a-z]*\n(.*?)\n\1\n" % re.escape(name),
              text, re.S | re.M)
if not m:
    sys.exit("STOP: the plan has no expected block named %r" % name)
CLOCK = re.compile(r"\b\d+\.\d+ s\b")


def lines(s):
    return [CLOCK.sub("N s", l.rstrip()) for l in s.splitlines()]


want, got = lines(m.group(2)), lines(open(path, encoding="utf-8").read())
diffs = 0
for i in range(max(len(want), len(got))):
    w = want[i] if i < len(want) else "(no line)"
    g = got[i] if i < len(got) else "(no line)"
    if w != g:
        diffs += 1
        print("  line %d\n    plan: %s\n    run:  %s" % (i + 1, w, g))
print("%s: %d line(s) expected, %d difference(s)" % (name, len(want), diffs))
sys.exit(0 if diffs == 0 else 1)
```

`baseline.py` — step 1:

<!-- script: baseline.py -->
```python
#!/usr/bin/env python3
"""0.1.4b step 1: EVERY PIN-DEPENDENT FACT THIS PLAN RESTS ON, re-derived at
the dispatched pin BEFORE anything is changed (§11 names each). Every program
is built through the harness's own `Build` -- the manifest's flags, both legs
-- into $W/base/, and run in the harness's constructed environment. Prints one
line per fact, relative names only; the worker diffs the output against §11's
block, and any difference is a stop.

  A  what `NPK_HEAP_STATS` reports and when: the six files as they stand, a
     program that allocates nothing, one that holds 4 096 bytes, the same with
     fd 2 closed, a trapped exit, argv[0] and the environment, and which
     spellings switch it on.
  B  the address space: the smallest cap each program starts under, as a table.
  C  `bytes_take`: its answer after reuse and after growth, and which of four
     ways out of a formatter's frame compile.
  D  the anchor: `npkrt.o`'s size and digest."""
import hashlib, os, re, resource, shutil, subprocess, sys

REPO, W = os.environ["REPO"], os.environ["W"]
sys.path.insert(0, os.path.join(REPO, "harness"))
import build as build_mod, manifest as manifest_mod   # noqa: E402

OUT = os.path.join(W, "base")
if os.path.isdir(OUT):
    shutil.rmtree(OUT)
os.makedirs(os.path.join(OUT, "src"))
BLD = build_mod.Build(REPO, manifest_mod.load(REPO), os.environ["NPKC"],
                      os.environ["NPKRT"], os.path.join(OUT, "build"))
BASE = {"NTIME_HARNESS": "1"}
LINE = re.compile(rb"^heap: allocated=(\d+) peak_live=(\d+) count=(\d+)$", re.M)
FLOOR_ARMS = """
func:failsafe = int32(Error:e) {
    pick (e) {
%s        (HeapBadRequest) { exit 91i32; },
        (HeapOom)        { exit 92i32; },
        (IntOverflow)    { exit 93i32; },
        (OutOfBounds)    { exit 94i32; },
        (Unreachable)    { exit 95i32; },
        (WildLeak)       { exit 96i32; },
        (DivByZero)      { exit 97i32; },
        (DivOverflow)    { exit 98i32; },
        (StackExhausted) { exit 106i32; },
        (MachineFault)   { exit 107i32; },
        (DecreasesViolated) { exit 108i32; },
        (LimitViolated)  { exit 109i32; },
        (*)              { exit 99i32; }
    }
    exit 9i32;
};
"""
BARE_ARMS = """
func:failsafe = int32(Error:e) {
    pick (e) {
%s        (HeapBadRequest) { exit 91i32; },
        (HeapOom)        { exit 92i32; },
        (Unreachable)    { exit 95i32; },
        (WildLeak)       { exit 96i32; },
        (StackExhausted) { exit 106i32; },
        (MachineFault)   { exit 107i32; },
        (*)              { exit 99i32; }
    }
    exit 9i32;
};
"""


def src(stem, text):
    path = os.path.join(OUT, "src", stem + ".npk")
    open(path, "w", encoding="utf-8").write(text)
    return path


def exe_of(path, optimised):
    return BLD.build_program(path, os.path.basename(path)[:-4], optimised)


def heap(exe, env):
    p = subprocess.run([exe], env=env, stdout=subprocess.DEVNULL,
                       stderr=subprocess.PIPE)
    found = LINE.findall(p.stderr)
    said = ("no line" if not found else "; ".join(
        "heap: allocated=%s peak_live=%s count=%s" % tuple(x.decode() for x in f)
        for f in found))
    return p.returncode, said


# ---- A. what the report says, and when --------------------------------------
print("A. NPK_HEAP_STATS")
SIX = ("tests/probe/probe06b_element_leak.npk", "tests/probe/probe06c_element_drop.npk",
       "tests/probe/probe12_set_overwrite_leak.npk", "tests/probe/probe12b_set_overwrite_drop.npk",
       "tests/unit/bytes_growth.npk", "tests/unit/bytes_view_lifetime.npk")
ON = dict(BASE, NPK_HEAP_STATS="1")
bins = {}
for rel in SIX:
    for opt in (False, True):
        exe = exe_of(os.path.join(REPO, rel), opt)
        bins[(os.path.basename(rel)[:-4], opt)] = exe
        st, said = heap(exe, ON)
        print("  %-30s %-7s exit %d  %s" % (os.path.basename(rel)[:-4],
                                           "opt -O2" if opt else "-O0", st, said))
FLOOR = src("floor", "mod:floor;\n\nfunc:main = int32(cstring[]:_~argv) {\n"
            "    exit 0i32;\n};\n" + BARE_ARMS % "")
HELD = ("mod:%s;\n\nerror:EW;\n\nfunc:main = int32(cstring[]:_~argv) {\n"
        "    buffer:held = buffer_new(4096i64);\n"
        "    if (held.len != 4096i64) { exit 3i32; }\n%s    exit 0i32;\n};\n"
        + BARE_ARMS % "        (EW)             { exit 90i32; },\n")
held = src("held", HELD % ("held", ""))
silent = src("silent", HELD % ("silent", "    int64:r = sys(3i64, 2i64) ?! EW;\n"
                               "    if (r != 0i64) { exit 4i32; }\n"))
for name, path in (("floor", FLOOR), ("held", held), ("silent", silent)):
    for opt in (False, True):
        exe = exe_of(path, opt)
        bins[(name, opt)] = exe
        st, said = heap(exe, ON)
        print("  %-30s %-7s exit %d  %s" % (name, "opt -O2" if opt else "-O0", st, said))
e06c = bins[("probe06c_element_drop", False)]
deep = os.path.join(OUT, "d" * 200, "e" * 200)
os.makedirs(deep)
shutil.copy(e06c, os.path.join(deep, "x"))
st, said = heap(os.path.join(deep, "x"), ON)
print("  probe06c, argv[0] 402 characters longer:     exit %d  %s" % (st, said))
st, said = heap(e06c, dict(ON, BIG="X" * 20000, A1="1", A2="2"))
print("  probe06c, a 20 000-byte variable and two more: exit %d  %s" % (st, said))
st, said = heap(e06c, BASE)
print("  probe06c, the variable absent:                exit %d  %s" % (st, said))
for k, v in (("NPK_HEAP_STATS", "0"), ("NPK_HEAP_STATS", ""), ("NPK_HEAP_STATSX", "1"),
             ("NPK_HEAP_STAT", "1"), ("npk_heap_stats", "1")):
    st, said = heap(bins[("held", False)], dict(BASE, **{k: v}))
    print("  held, %-24s exit %d  %s" % ("%s=%s" % (k, v), st, said))
LIM = 65536 * 1024
p = subprocess.run([bins[("probe06b_element_leak", False)]], env=ON,
                   stdout=subprocess.DEVNULL, stderr=subprocess.PIPE,
                   preexec_fn=lambda: resource.setrlimit(resource.RLIMIT_AS, (LIM, LIM)))
print("  probe06b under a 65536 KiB cap, a TRAPPED exit: exit %d  %s"
      % (p.returncode, "a line" if len(LINE.findall(p.stderr)) == 1 else "NO LINE"))

# ---- B. the address space ----------------------------------------------------
print("B. ulimit -v, KiB -- the exit under each cap")


def capped(argv, kib):
    lim = kib * 1024

    def cap():
        resource.setrlimit(resource.RLIMIT_AS, (lim, lim))
    try:
        r = subprocess.run(argv, env=BASE, stdout=subprocess.DEVNULL,
                           stderr=subprocess.DEVNULL, preexec_fn=cap).returncode
    except (OSError, subprocess.SubprocessError):
        return "start"
    return str(r) if r >= 0 else "sig%d" % -r


CAPS = (131072, 65536, 32768, 16384, 11264, 10240, 3072, 2048)
print("  %-14s" % "ulimit -v" + "".join("%8d" % c for c in CAPS))
for name, argv in (("/bin/true", ["/bin/true"]),
                   ("floor program", [bins[("floor", False)]]),
                   ("probe12", [bins[("probe12_set_overwrite_leak", False)]]),
                   ("probe12b", [bins[("probe12b_set_overwrite_drop", False)]]),
                   ("probe06b", [bins[("probe06b_element_leak", False)]]),
                   ("probe06c", [bins[("probe06c_element_drop", False)]])):
    print("  %-14s" % name + "".join("%8s" % capped(argv, c) for c in CAPS))

# ---- C. bytes_take -----------------------------------------------------------
print("C. bytes_take")
TAKE = ("mod:%(m)s;\nuse \"../../../../src/core/bytes.npk\".*;\n%(fn)s"
        "func:main = int32(cstring[]:_~argv) {\n%(body)s    exit 0i32;\n};\n"
        + FLOOR_ARMS % "")
REUSE = ("    Bytes:d = raw bytes_init(16i64);\n    drop bytes_extend_str(@d, \"2026-09-05\");\n"
         "    string:s = raw bytes_take(@d);\n    if (!(raw s.eq(\"2026-09-05\"))) { exit 11i32; }\n"
         "    drop bytes_clear(@d);\n    drop bytes_extend_str(@d, \"1999-12-31\");\n"
         "    if (!(raw s.eq(\"2026-09-05\"))) { exit 13i32; }\n")
GROWN = ("    Bytes:d = raw bytes_init(1i64);\n    drop bytes_extend_str(@d, \"2026-09-05\");\n"
         "    string:s = raw bytes_take(@d);\n    if (!(raw s.eq(\"2026-09-05\"))) { exit 11i32; }\n"
         "    drop bytes_push(@d, 65u8);\n    if (!(raw s.eq(\"2026-09-05\"))) { exit 12i32; }\n")
COPYFN = ("func:take_copy = string(Bytes->:b) never fails {\n"
          "    string:view = string_from_bytes(b.body.ptr, b.len);\n"
          "    pass string_concat(view, \"\");\n};\n")
INTOFN = ("func:take_into = string(move Bytes:b) never fails {\n"
          "    string:view = string_from_bytes(b.body.ptr, b.len);\n"
          "    pass string_concat(view, \"\");\n};\n")
FMT = ("func:format_date = string() never fails {\n    Bytes:sink = raw bytes_init(16i64);\n"
       "    drop bytes_extend_str(@sink, \"2026-09-05\");\n%s};\n")
MAIN = "    string:s = raw format_date();\n    if (!(raw s.eq(\"2026-09-05\"))) { exit 12i32; }\n"
cases = (
    ("take_reuse", "", REUSE),
    ("take_grown", "", GROWN),
    ("escape_take", FMT % "    pass raw bytes_take(@sink);\n", MAIN),
    ("escape_copy", COPYFN + FMT % "    pass raw take_copy(@sink);\n", MAIN),
    ("escape_copy_bound", COPYFN + FMT % "    string:answer = raw take_copy(@sink);\n    pass answer;\n", MAIN),
    ("escape_into", INTOFN + FMT % "    pass raw take_into(move(sink));\n", MAIN),
)
for stem, fn, body in cases:
    path = src(stem, TAKE % {"m": stem, "fn": fn, "body": body})
    st, out = BLD.emit_expecting_refusal(path, os.path.join(OUT, "build", stem + ".chk.ll"))
    codes = sorted(set(re.findall(r"NITPICK-[A-Z]+-\d+", out)))
    if st != 0:
        print("  %-18s npkc exit %d  %s" % (stem, st, " ".join(codes)))
        continue
    legs = []
    for opt in (False, True):
        legs.append(heap(exe_of(path, opt), ON))
    print("  %-18s npkc exit 0  -O0 exit %d %s  |  opt -O2 exit %d %s"
          % (stem, legs[0][0], legs[0][1], legs[1][0], legs[1][1]))

# ---- D. the anchor -----------------------------------------------------------
rt = open(os.environ["NPKRT"], "rb").read()
print("D. npkrt.o %d bytes, sha256 %s" % (len(rt), hashlib.sha256(rt).hexdigest()))
```

Expected, at `c3bdae2` — its whole output:

<!-- expect: baseline -->
```text
A. NPK_HEAP_STATS
  probe06b_element_leak          -O0     exit 0  heap: allocated=118000000 peak_live=70000024 count=4000000
  probe06b_element_leak          opt -O2 exit 0  heap: allocated=118000000 peak_live=70000024 count=4000000
  probe06c_element_drop          -O0     exit 0  heap: allocated=118000000 peak_live=59 count=4000000
  probe06c_element_drop          opt -O2 exit 0  heap: allocated=118000000 peak_live=59 count=4000000
  probe12_set_overwrite_leak     -O0     exit 0  heap: allocated=70000059 peak_live=70000059 count=2000002
  probe12_set_overwrite_leak     opt -O2 exit 0  heap: allocated=70000059 peak_live=70000059 count=2000002
  probe12b_set_overwrite_drop    -O0     exit 0  heap: allocated=70000059 peak_live=94 count=2000002
  probe12b_set_overwrite_drop    opt -O2 exit 0  heap: allocated=70000059 peak_live=94 count=2000002
  bytes_growth                   -O0     exit 0  heap: allocated=3097163 peak_live=2048587 count=25
  bytes_growth                   opt -O2 exit 0  heap: allocated=3097163 peak_live=2048587 count=25
  bytes_view_lifetime            -O0     exit 0  heap: allocated=190 peak_live=160 count=7
  bytes_view_lifetime            opt -O2 exit 0  heap: allocated=190 peak_live=160 count=7
  floor                          -O0     exit 0  heap: allocated=0 peak_live=0 count=0
  floor                          opt -O2 exit 0  heap: allocated=0 peak_live=0 count=0
  held                           -O0     exit 0  heap: allocated=4096 peak_live=4096 count=1
  held                           opt -O2 exit 0  heap: allocated=4096 peak_live=4096 count=1
  silent                         -O0     exit 0  no line
  silent                         opt -O2 exit 0  no line
  probe06c, argv[0] 402 characters longer:     exit 0  heap: allocated=118000000 peak_live=59 count=4000000
  probe06c, a 20 000-byte variable and two more: exit 0  heap: allocated=118000000 peak_live=59 count=4000000
  probe06c, the variable absent:                exit 0  no line
  held, NPK_HEAP_STATS=0         exit 0  heap: allocated=4096 peak_live=4096 count=1
  held, NPK_HEAP_STATS=          exit 0  heap: allocated=4096 peak_live=4096 count=1
  held, NPK_HEAP_STATSX=1        exit 0  no line
  held, NPK_HEAP_STAT=1          exit 0  no line
  held, npk_heap_stats=1         exit 0  no line
  probe06b under a 65536 KiB cap, a TRAPPED exit: exit 92  a line
B. ulimit -v, KiB -- the exit under each cap
  ulimit -v       131072   65536   32768   16384   11264   10240    3072    2048
  /bin/true            0       0       0       0       0       0       0     127
  floor program        0       0       0       0       0      92      92      92
  probe12             92      92      92      92      92      92      92      92
  probe12b             0       0       0       0       0      92      92      92
  probe06b            92      92      92      92      92      92      92      92
  probe06c             0       0       0       0       0      92      92      92
C. bytes_take
  take_reuse         npkc exit 0  -O0 exit 13 heap: allocated=16 peak_live=16 count=1  |  opt -O2 exit 13 heap: allocated=16 peak_live=16 count=1
  take_grown         npkc exit 0  -O0 exit 12 heap: allocated=31 peak_live=30 count=3  |  opt -O2 exit 12 heap: allocated=31 peak_live=30 count=3
  escape_take        npkc exit 1  NITPICK-BORROW-001
  escape_copy        npkc exit 1  NITPICK-BORROW-001
  escape_copy_bound  npkc exit 1  NITPICK-BORROW-001
  escape_into        npkc exit 0  -O0 exit 0 heap: allocated=26 peak_live=26 count=2  |  opt -O2 exit 0 heap: allocated=26 peak_live=26 count=2
D. npkrt.o 72576 bytes, sha256 162b897539285a773a6a1a0329750e148a6c9590b45dda2d017704743b591824
```

---

## 12. What will bite

- **`earlyoom` is live and has fired today** (the workbench board's hazard
  12; SIGTERM, exit 143, before SIGKILL, 137). A step that dies by signal may
  be the machine: read `journalctl -u earlyoom --since today` **before**
  diagnosing anything, and treat a kill as a stop, never a retry.
- **Every removal names its target through `"${W:?}"`.** The planner's shell
  refused `rm -rf $P/dev` because an unset `P` makes it a removal of `/dev`.
- **`NPK_HEAP_STATS=0` switches the report ON.** Never set the variable by
  hand to turn it off — unset it; and the harness refuses it in an `env:`
  marker for exactly this reason.
- **A prose line below a header block that begins `// heap:` or `// cap:`
  fails the run** (TM-121): the marker block is contiguous from line 1, and a
  marker-shaped line below it is a silent no-op, so the harness names it.
- **A tag written whole in `meta/roadmap/` is a claim** `check_denominators`
  holds the tree to, the moment the file is saved — the execution record
  included. Quote a tag's name and value, never the bracketed form — this
  file spells its hunks' tags `{{sweep: …}}`, and `apply.py` writes them whole.
- **A decision number in this file before step 5 is a citation of an
  undeclared decision** — `check_refs` counts inline code too. Write *"PD-35,
  the number after TM-183"*. Mutant labels carry no hyphen.
- **`apply.py` stops at the first block that does not apply**, having written
  nothing for it — but the blocks before it ARE applied. After a stop, read
  `git -C "$REPO" status --short`, find the cause, `git checkout --` the files
  of that group, and re-run the group; never hand-apply the rest.
- **`renumber.py` runs once.** It rewrites `DECISIONS.md`'s placeholders and
  every label outside this file; a second run finds no labels and stops on
  nothing, but a hand edit between the two is not re-checked. If the gate's
  item 6 read a number above TM-183, pass that number.
- **The leaking twins hold 70 MB and take HeapOom under the cap on purpose.**
  Their `92` is the assertion (V-1j's exception: a test asserting the trap
  itself), not a crash to investigate.
- **The floor program is written into `build/`** — gitignored and outside the
  walk (`checks.WALK_SKIP`) — so it is neither committed nor counted; a
  `cap_control.npk` anywhere else is a mistake.
- **`grep` here is ugrep**: given several files it prints in completion order,
  so the steps grep one file at a time.
- **No absolute path goes into this file's record**: `check_refs`' leak scan
  reads every tracked text file, and a home directory in a committed record
  fails every later commit. The scripts print relative names; anything pasted
  from `$W` is written as `$W/…`.
- **A capped run's output is discarded**; only its exit is read. The belt says
  nothing about the heap line, and the `heap:` bounds nothing under the cap.

---

## 13. Acceptance

- [ ] step 0: the anchor unchanged (72 576 bytes, `162b8975…`), `GREEN` before
      anything changes, `check_refs` clean
- [ ] step 1: `baseline.py`'s output equal to §11's block — `0 difference(s)`
- [ ] §6's 37 `code` blocks applied by `apply.py`, not typed; step 2's status
      list exactly
- [ ] step 3: case 9 caught, and `check_denominators` clean over
      `heap_bounded` 6 and `cap_belted` 4
- [ ] §7's matrix: `0 difference(s)` — every bound red on its mutant, each
      declined formulation fooled, the four ceilings recomputed and `SAME`
- [ ] §10's 26 `docs` blocks and its two `decisions` blocks applied; `renumber.py`
      clean — PD-35 … PD-39 as the next five TM numbers, every label replaced
      but `DECISIONS.md`'s provenance
- [ ] §10.2's sweep read, no live stale claim left, the counts from the
      commands
- [ ] step 7: `GREEN`; the six units' verdict lines carrying their `heap` and
      cap evidence at §6.3's numbers; `0 unresolved` citations; the wall clock
      within §8's 30 s of step 0's
- [ ] `check_refs` clean before and after staging; the commit lists exactly
      step 8's files
- [ ] CI green on the pushed work commit, read from its own log, with the six
      units' evidence equal to step 7's; the record commit names the run

---

## 14. Decisions drafted at planning

Recorded by the worker in `../../DECISIONS.md` as the next free TM numbers, in
this order, in the work commit — **§10's second `decisions` block is their text**,
each with its alternatives declined and the numbers measured here, applied
verbatim and numbered by `renumber.py`.

- **PD-35** — the harness reads the runtime's `NPK_HEAP_STATS` line through a
  per-file `heap:` marker; the variable only for a file that carries one;
  exactly one line per run, both legs; the evidence in the verdict line; no
  `env:` marker may set it.
- **PD-36** — six files and their bounds: leaking twins' floors at the leak's
  arithmetic; remedies' ceilings with a work floor; the `Bytes` tests'
  ceilings; every ceiling the geometric mean of the correct peak and a leak
  mutant's, rounded down to two significant figures.
- **PD-37** — the `ulimit -v` pair run by the harness as a belt, `cap:` on the
  twin pairs, with the floor program as its control, superseding `/bin/true`
  as TM-131's.
- **PD-38** — V-14's ninth case: five plants and one control in one inner run.
- **PD-39** — `bytes_take` hands back an owned copy; `bytes_growth` asserts it;
  `SAFETY.md` S-18f.

**For the author**, accepted by default unless the orchestrator says otherwise:
**PD-39** widens this subcycle past the gate its README row names — a library
fix on the public surface — and **PD-37** runs a belt the harness never ran,
with a control that replaces TM-131's `/bin/true`. §15 says what striking either costs.

---

## 15. What 0.1.4b hands on, and in what order

**The order is 0.1.4b, then 0.1.3c, then the close, 0.1.5** — the
orchestrator's, one writer in one repository, so serial.

- **0.1.3c — `Vec` move-only by construction**, planned against the pin
  carrying the compiler's 1.6.0 step 3h, where its loan tests are written as
  `TYPE-085` refusals from the start. **It inherits from this subcycle**: the
  `heap:` marker, for the churn pair TM-150 measured and never committed —
  two million push-then-pop cycles at `peak_live` 120 against `vec_clear`'s
  48 000 096, taken in a scratch directory at 0.1.0b and quoted in `SAFETY.md`
  S-18d, `src/core/vec.npk` and `PLAYBOOK.md` §12 with no program behind it,
  which is the state `probe06b`'s header says cost cycle 0.0 a re-measurement.
  0.1.3c changes the type it measures, so it is where it is committed. And, as
  before, `VERIFICATION.md` P-1's replacement by A′ and Q-6's strike.
- **0.1.5 — the close** — owes nothing new from here beyond its README row's
  list, but its sweep-stage headroom item should read the whole invocation's:
  this subcycle adds about 11 s to it (§8).
- **Cycle 0.4** gains a "Watch for" item by §10's hunk: F-10's
  `string`-returning wrapper, as measured here, and what spells it.
- **If the orchestrator strikes PD-39** before dispatch: drop §6's
  `src/core/bytes.npk` block and `bytes_growth`'s third block, §10's S-18f,
  BUILD.md B-12, the three `bytes_take` sentences in `CLAUDE.md`,
  `harness/README.md` and `ROADMAP.md`, and PD-39's decision; `bytes_growth`'s
  measured peak is then 2 048 587 and its mutant's 3 097 187, and PD-36's rule
  still gives 2 500 000 — so the gate itself does not change, and the defect
  goes to a subcycle of its own, with §4 as its evidence. **If it strikes
  PD-37**, drop the `cap:` markers, `run_capped`, `CAP_CONTROL` and case 9's
  two cap plants; the heap bounds stand alone, and TM-131's pair stays manual.
- **For the workbench**, three things. (1) **The borrow tracker refuses an
  owned `string` returned from a call handed `@local`** (§4) — the shape
  `nitpick-regex` recorded as its colliding local O-N17 and owes a
  re-measurement at the new pin; this is a second repository's measurement of
  it, at `c3bdae2`, for the registry question if one is filed. It is not raised
  as a defect here: the tracker cannot tell an owned answer from a view by the
  signature, and a view escaping is exactly what it exists to refuse.
  (2) **The compiler's D-292 says `NPK_HEAP_STATS` counts a failsafe's region
  bytes; its runtime does not**, and its own 1.5.6 record says so as a
  departure — a documentation item for the compiler seat, not a defect.
  (3) **For the playbook**: a low-cap control must share the program's runtime
  (TM-131's `/bin/true` measured the loader, and the runtime's own floor has
  grown past it); every number an instrument prints is derived from the source
  before a bound is put on it, and one that disagrees is a finding
  (`bytes_take`'s 25 against 26); and a view typed as an owned value is a
  use-after-free that no test reading the answer at once can see.

---

## 16. How this plan was rehearsed — which command ran where

**In the real checkout, `$REPO` at `648590f`**, in two full passes after a
first start that stopped at step 0; before each, this plan commit's three files
— this file, the cycle README and `ROADMAP.md` — were placed AND staged, so
that `check_refs`, `check_denominators`, `check_specs_current` and
`renumber.py` read what the worker will read, and the title was set `RUNNING`.
Measurements before the passes — the instrument's facts, the address-space
bisection, the `bytes_take` shapes and spellings, the mutants — were taken in
the planner's own `.internal/p014b/`, never in the tree; the harness change was
first developed in a scratch copy there (`dev/`), where the matrix and a full
run first went green.

**Found before and during the passes, each corrected above:**

1. **`bytes_take`'s missing allocation** — 25 counted where the source makes
   26 — which is §4 and PD-39.
2. **TM-131's control no longer controls** — the floor program needs about
   10.5 MiB — which is §3 and PD-37.
3. **The first start went red at step 0**, before anything was applied: this
   file's hunks carried whole sweep tags for two denominators nothing measured
   yet. Tags are spelled `{{sweep: …}}` here now, and `apply.py` writes them
   whole (§0).
4. **§14 declared TM-131 replaced in words `check_refs` reads as a
   supersession**, while TM-131's heading carried no marker — the planning
   commit would have failed its own gate. §14 says "replaces" now, and the
   `decisions` group gains TM-131's `SUPERSEDED IN PART` marker, the
   convention TM-014, TM-026 and TM-109 already carry.
5. **The baseline block carried a `/usr/bin/time` line** from the planner's
   measuring run, which step 1's command does not print: 44 lines, not 45.
6. **Two stale-claim patterns matched noise** — `3 MiB` matched `473 MiB`, and
   `two of them` matched prose about other things — and were tightened.
7. **Pass 2's first step 0 found two more, both in text written after pass
   1**: step 5's expected block quoted `renumber.py` printing a TM number,
   which `check_specs_current` reported as a citation of an undeclared
   decision — `renumber.py` now prints the numbers bare; and a row of §10.2's
   table set TM-128 close enough to a past participle that `check_refs` read a
   supersession — it is reworded. Pass 2 then started again from a restored
   tree. **And the planning commit's own gate, run again after staging, caught
   this list's first draft doing the same twice** — the rule that a commit
   adding a file is gated after `git add` as well as before.
8. **Steps 0 and 3 grepped `check_specs_current`'s whole line**, whose citation
   count includes this file's own blocks — pasting the output in would have
   changed it. The greps print the rule and the count of unresolved citations
   only, and step 7's comparison is `same.py`'s.
9. **The planner's shell refused `rm -rf $P/dev`**, whose target an unset `P`
   turns into `/dev` — hence §0's `"${W:?}"` rule; and a wildcard removal of
   `$W`'s scripts took `env.sh` with it mid-pass, recreated from §0's text.
   This plan removes no file by wildcard.

**Pass 2 ran the final text end to end** — every script extracted by §0's
own bootstrap, and every step's output saved and compared with its block by
`same.py`, the steps the worker only reads included:

| Command | Where | Result |
|---|---|---|
| §0's `env.sh`, bootstrap and `apply.py scripts` | `$REPO` | `apply.py: 69 lines`; five scripts in document order |
| step 0 | `$REPO` | `step0: 9 line(s) expected, 0 difference(s)` — the anchor, `GREEN -- 91 unit(s)` in 111.8 s, `check_refs` clean at 77 and 223; the porcelain count **3**, not the worker's 1 — this file `AM` and the README and ROADMAP staged, the rehearsal's own |
| step 1, `baseline.py` | `$REPO`; its programs under `$W/base/` by design | `baseline: 44 line(s) expected, 0 difference(s)` |
| step 2 | `$REPO` | the 37 blocks, the group line and `exit 0` equal; the status list differs by the same three staged lines and nothing else |
| step 3, `--only probe06` | `$REPO` | `step3: 13 line(s) expected, 0 difference(s)` |
| step 4, `mut.py` | `$REPO`; the mutants under `$W/mut/` by design — none is applied to the tree | `matrix: 44 line(s) expected, 0 difference(s)` |
| step 5 | `$REPO` | the gate's `grep` first, `### TM-183`; then `step5: 52 line(s) expected, 0 difference(s)` |
| step 6 | `$REPO` | §10.2's printed commands, extracted from this file: 3, 6, 11, 4, 4, 6 and 38 lines |
| step 7 | `$REPO` | `final: 17 line(s) expected, 0 difference(s)`; 122.9 s against step 0's 111.8 |
| step 8 | `$REPO` | `check_refs` clean at 77 and 223, before and after staging; 25 paths, this file `A` where the worker's is `M` |

Pass 1, on the text before items 7 and 8, gave the same results at every
step: `GREEN` 91 → 91 in 111.4 s and 122.7 s, and the matrix and the baseline
equal to their blocks.

**Every leg ran**, at both passes. Nothing in this repository's harness finds
a tool by a checkout-relative path — `$NPKC`, `$NPKRT` and `PATH` only — and
`baseline.py`'s `bytes_take` programs import `src/core/bytes.npk` by a path
relative to `$W`, which is inside `$REPO` for the worker exactly as for the
rehearsal. **Not rehearsed:** the commits, the push and CI; §10.3's hand edits
at the close; and any pin but `c3bdae2`, since none carrying 3h exists yet —
§11 is the plan for that.

**Then restored**, after each pass: `git reset -q`, `git checkout -- .`, this
file removed, `git status --porcelain` empty at `648590f`, no stash; and at the
end `$W` removed, so the worker starts from nothing of the rehearsal's. The
planner's own scratch, `.internal/p014b/` — the instrument's programs, the
bisection, the mutants, the scratch copy, the draft this file was assembled
from, and both passes' outputs in `pass1/` and `pass2/` — is gitignored and
left for the verifier to read.

**`earlyoom` during the planning**: its journal, read before the first pass
and after the last, shows no signal sent since 18:00; nothing here died by
signal, and every run above was sequential.

---

## Execution record

*(Appended as the work is done. The REPORT block is its last entry.)*

### Execution — 2026-09-25 into 2026-09-26, stream `s2`, at compiler `c3bdae2`

**The dispatch.** `s2-ntime-0.1.4b-2355` (`npk:worker`, 23:55 — the board's
stream-2 row), with `TREE: clean`, `AUDIT: none`, `DIGESTS: none`, and
`NOTES:` carrying five things this record answers:

1. **The pin does not move during this dispatch** — `c3bdae2`, the workbench
   re-pinning only between subcycles — so every "at a pin other than
   `c3bdae2`" branch of §9 and §11 is idle, step 0's anchor holds, and the
   borrow-tracker lines §11 flags for the compiler's 3g cannot move.
2. **PD-39 and PD-37 were put to the author and accepted by default**, so
   they are recorded as drafted: `bytes_take`'s fix is TM-188, and TM-131's
   `/bin/true` control, replaced in part, is TM-186. The orchestrator had
   reproduced the defect at `c3bdae2` on both legs before dispatch —
   `take_reuse` exit 13, `take_grow` exit 12, and both 0 with a copying take
   in a scratch copy of the module — and `nitpick-regex`'s twin already
   copies, so only this repository was exposed. Step 1's part C re-derived
   the same two exits here.
3. **The compiler seat has been asked two things from this plan** — the
   D-292 documentation item (`NPK_HEAP_STATS` and the failsafe region, §15's
   second workbench item) and whether "no mutation of a view's root while the
   view is live" is meant to be enforced. Neither changes this subcycle, and
   nothing here waited on either.
4. **The compiler defects open at the pin, to stay clear of — none of this
   subcycle's programs meets one.** The new `bytes_take` reads `b.body.ptr`
   and `b.len` and writes through nothing; `bytes_growth`'s new lines take
   `@` of two LOCAL sinks, `d` and `g`; nothing moves out of or writes into
   `fixed` storage, imports a `fixed` table, or puts `""` inside a block
   string (the one new `""` is an ordinary argument to `string_concat`).
   Every new `main` — `stages.py`'s `CAP_CONTROL`, `selfcheck.py`'s `HELD`,
   and `baseline.py`'s programs — is `int32(cstring[]:_~argv)`. No loop is
   added, so the spurious `(BadStep)` never arises; the new `failsafe`s use
   the convention's codes, 91, 92, 95, 96, 106, 107 and 99.
5. **`earlyoom`.** Its journal, read at 23:57 before the first run: the last
   signal it sent today was a SIGTERM at 18:00:15, to the compiler seat's
   `clam`, and nothing since; `free -g` read 143 GiB available. Nothing here
   died by signal.

**Before touching anything.** The board's stream table reads
`` `nitpick-time` … `CLAIMED s2` ``; `git status --porcelain` was empty at
`b695788`; `sha256sum` of `npkc` and `npkrt.o` equal to the pin's
`SHA256SUMS`. The title was set `RUNNING` first, uncommitted, as the worker's
§4 asks.

**The gate for starting, each item checked:** (1) 0.1.4 verified PASS and CI
green, run `36210527554` — the board's stream-2 row; (2) the status empty and
`HEAD` this plan's commit, `b695788`; (3) two `OK`s and the anchor unchanged —
step 0; (4) Q-6 answered A′, and nothing here writes a contract; (5) no
currency to check; (6) `grep -o '^### TM-[0-9]*' meta/DECISIONS.md | tail -1`
read `### TM-183`, so PD-35 … PD-39 are TM-184 … TM-188 in order; (7) 0.1.3c
has not landed — it is not yet planned; (8) `earlyoom`'s journal, above.

**§0.** `env.sh` written as given, `PIN` the dispatch's `COMMIT`, `c3bdae2`;
then the bootstrap: `apply.py: 69 lines`, the five scripts in document order —
`apply.py` 69, `mut.py` 201, `renumber.py` 63, `same.py` 37, `baseline.py` 208
lines — and `scripts written: 5`, exit 0.

**Step 0 — the start, the anchor and the baseline.** Two `OK`s; **`72576
162b897539285a773a6a1a0329750e148a6c9590b45dda2d017704743b591824`, the
anchor**; the porcelain count **1** — this file, ` M` for its title; `b695788`;
`Python 3.12.3`; `free -g` 143 GiB available. The rest, the whole of what the
printed commands wrote, saved as `$W/step0.txt`:

```text
exit 0
GREEN -- 91 unit(s), 0 failures; 5 pending, 112.4 s. Read this file's header for what that does not mean.
112.49 s wall, 125184 KiB peak RSS
2 unresolved
report: S-18f cited
report: V-17 cited
  nitpick-time       clean            (77 md files via git ls-files; leak scan 223 text of 223 tracked)

All clean.
```

`same.py step0`: `step0: 9 line(s) expected, 0 difference(s)`, exit 0 — the
wall clock set aside, the peak resident equal to the plan's to the kilobyte.

**Step 1 — every pin-dependent fact, re-derived before anything changed.**
`baseline.py`, exit 0, in 22.70 s at a 125 184 KiB peak; `same.py baseline`:
**`baseline: 44 line(s) expected, 0 difference(s)`**, exit 0. Its 44 lines are
§11's block line for line and are not re-quoted here: the six files' lines on
both legs — `bytes_growth` still `count=25` —; the floor program's three
zeros, the held buffer's `4096`, and no line with fd 2 closed; `argv[0]` and
the environment moving nothing; `NPK_HEAP_STATS=0` and `=` printing, the three
misspellings not; the trapped exit under 64 MiB printing one line at exit 92;
the cap table, `/bin/true` from 2 048 to 3 072 KiB and every Nitpick program
92 at 10 240; `take_reuse` 13 and `take_grown` 12 on both legs; the three
escapes from a formatter's frame refused `NITPICK-BORROW-001` and the
consuming take compiling and exiting 0 on both legs; and the anchor again.

**Step 2 — the code, applied from this file.** `apply.py code`: the 37 blocks,
`group code: 37 block(s) applied`, exit 0, and the status list — this file
among the thirteen. `same.py step2` over the saved output: **`step2: 52
line(s) expected, 0 difference(s)`**, exit 0.

**Step 3 — the filtered run.** `--only probe06`, exit 0 in 79.0 s. The
printed commands' whole output, saved as `$W/step3.txt`:

```text
exit 0
2:this runner has been shown able to fail 8 ways (V-14), of the 9 V-14 names.
4:*** --only 'probe06': THIS RUN CONCLUDES NOTHING. A filtered run is for iterating; ***
7:[1/9] self-check -- 8 of V-14's 9 cases (case 6 is PEND), then 23 tree-check violations,
25:  ok    case 9  a program whose managed memory disagrees with its header
66:  ok    check_denominators       48 tagged number(s) over 214 file(s), against 24 measured denominator(s)
107:  ok    tests/probe/probe06_generic_vec.npk            exit 0, both legs, 0.4 s
108:  ok    tests/probe/probe06b_element_leak.npk          exit 0, both legs, 2.8 s, heap peak_live=70000024 on both legs, exit 92 under 65536 KiB on both legs
109:  ok    tests/probe/probe06c_element_drop.npk          exit 0, both legs, 3.9 s, heap peak_live=59 count=4000000 on both legs, exit 0 under 65536 KiB on both legs
115:GREEN under --only 'probe06' in 79.0 s -- 28 unit(s) ran; 5 pending.
2 unresolved
report: S-18f cited
report: V-17 cited
```

`same.py step3`: `step3: 13 line(s) expected, 0 difference(s)`, exit 0. Case
9's own line in the log reads *"the fault was caught, and the control beside
it came back green"*.

**Step 4 — the matrix.** `mut.py`, exit 0, in 27.99 s at a 125 184 KiB peak;
`same.py matrix`: **`matrix: 44 line(s) expected, 0 difference(s)`**, exit 0.
Every row `MATCH` and every named line `found`: K0's six files green at §6.3's
numbers; K1 … K8 red on their named lines; D1 and D2 fooled, D3 red and D3′
fooled as planned; the four ceilings recomputed by TM-185's rule and `SAME` as
the headers — 64 000, 81 000, 2 500 000 and 180. The tree after it was the
step-2 list unchanged: the mutants live under `$W/mut/`.

**Step 5 — the documents.** `apply.py docs` (26 blocks), `apply.py decisions`
(2) and `renumber.py 184 2026-09-26`, each exit 0; `same.py step5`: **`step5:
52 line(s) expected, 0 difference(s)`**, exit 0 — `DECISIONS.md` 7
placeholders and 7 dates, then 61 labels in 18 files, `PD-35 ... PD-39 -> TM
numbers 184 ... 188`. **The date is the 26th**: the work crossed midnight
between step 1 and step 2, so the new section is ratified on the day of the
work commit, as `renumber.py` asks, while this file's title says `RUNNING`
since the 25th.

**Step 6 — the stale-claim sweep.** §10.2's seven printed commands, run on the
tree after step 5, each count from `wc -l` on the command's own output:
**3, 6, 11, 4, 4, 6 and 38 lines** — planning's counts exactly. Every hit was
read, and each is what §10.2's table says it is: history — a settled
decision, a dated note, a heading followed by its dated update, a quotation
of the old words beside the new — or this subcycle's own change. **No live
stale claim.**

**Five more sweeps**, because a sweep list is wrong on its first telling
(`PLAYBOOK.md` §6) — the self-check's counts in other spellings, the lists of
marker keys, the statements of the marker grammar, every `NPK_HEAP_STATS` and
`peak_live`, and every `bytes_take` — with §10.2's exclusions:

```bash
cd "$REPO"
X=(-- . ':!meta/roadmap/0.1/0.1.[0-4]*.md' ':!meta/roadmap/done/*' ':!tests/probe/defect/*/TRANSCRIPT.txt')
git grep -n -i --untracked -E "seven ways|eight ways|seven faults|eight faults|7 of V-14|8 of V-14|seven cases|eight of|seven of|V14_CASES|PLANTED_CASES" "${X[@]}"
git grep -n -i --untracked -E "expect-golden.{0,40}stress|stress.{0,20}argv.{0,20}env|sweep-count.{0,30}(markers|keys)" "${X[@]}"
git grep -n -i --untracked -E "marker grammar|the markers|every marker|known key|(six|seven|eight|nine|ten) (markers|keys)" "${X[@]}"
git grep -n -i --untracked -E "NPK_HEAP_STATS|peak_live" "${X[@]}"
git grep -n -i --untracked -E "bytes_take" "${X[@]}"
```

**21, 2, 11, 87 and 27 lines**, each read. The only statements they found
that §10.2's seven did not are history or already true: `CLAUDE.md`'s *"eight
faults was printed on every run for three cycles"*, a past-tense note;
`tests/probe/defect/missing_failsafe/README.md`'s cycle-0.0.3 proposal that
*"`selfcheck.py`'s seven cases should gain an eighth"*, which case 8 made
history; TM-120's list of the markers at its date; `bytes.npk`'s *"`bytes_len`,
`bytes_view`, `bytes_take` and `bytes_clear` do not grow and do not
invalidate"* and *"AN OWNED `string` OF THE LIVE PREFIX … the one operation
here that allocates by design"*, both true since the fix; `probe16c`'s *"hand
out exactly `len` bytes"*, true of both implementations; and TM-150's
`peak_live` 120 in `SAFETY.md` S-18d and `src/core/vec.npk`, the measurement
0.1.3c commits a program for (§15). **No live stale claim.**

**Step 7 — the full run.** `free -g` 142 GiB available; exit 0; `121.93 s
wall, 125184 KiB peak RSS`. The printed commands' whole output, `$W/final.txt`:

```text
this runner has been shown able to fail 8 ways (V-14), of the 9 V-14 names.
[1/9] self-check -- 8 of V-14's 9 cases (case 6 is PEND), then 23 tree-check violations,
  ok    case 9  a program whose managed memory disagrees with its header
  ok    the tree checks                                23 planted violation(s) caught, 23 clean control(s) silent
      exemption verdicts: 7 of 7 re-derived, 0 moved
  ok    check_denominators       55 tagged number(s) over 214 file(s), against 24 measured denominator(s)
      82 parse cleanly, 24 parse and are refused by a later phase, 2 must not parse and do not, 0 failed
  ok    tests/probe/probe06_generic_vec.npk            exit 0, both legs, 0.4 s
  ok    tests/probe/probe06b_element_leak.npk          exit 0, both legs, 2.7 s, heap peak_live=70000024 on both legs, exit 92 under 65536 KiB on both legs
  ok    tests/probe/probe06c_element_drop.npk          exit 0, both legs, 3.9 s, heap peak_live=59 count=4000000 on both legs, exit 0 under 65536 KiB on both legs
  ok    tests/probe/probe12_set_overwrite_leak.npk     exit 0, both legs, 1.3 s, heap peak_live=70000059 on both legs, exit 92 under 65536 KiB on both legs
  ok    tests/probe/probe12b_set_overwrite_drop.npk    exit 0, both legs, 2.5 s, heap peak_live=94 count=2000002 on both legs, exit 0 under 65536 KiB on both legs
  ok    tests/unit/bytes_growth.npk                    exit 0, both legs, 0.5 s, heap peak_live=2048636 on both legs
  ok    tests/unit/bytes_view_lifetime.npk             exit 0, both legs, 0.4 s, heap peak_live=160 on both legs
GREEN -- 91 unit(s), 0 failures; 5 pending, 121.9 s. Read this file's header for what that does not mean.
322 declared rule(s)
0 unresolved
```

`same.py final`: **`final: 17 line(s) expected, 0 difference(s)`**, exit 0.
**The cost against §8's threshold: 121.9 s against step 0's 112.4 s, 9.5 s
more, under the 30 s set in advance** (planning measured 11.3). The defect
corpus is unchanged, `28 file(s) = 3 exempt (verdict re-derived above) + 25
asserted (14 run, 11 refusal, 0 unowned)`, and the header sweep `108 .npk in
the tree = 10 src/ + 98 tests/ + 0 elsewhere` — no file added.

**Found at execution, not planned: nothing.** Every expected block the plan
names — step 0, the baseline, step 2, step 3, the matrix, step 5 and the full
run — is equal to the run's output by `same.py`, and the seven sweep counts
are planning's. The one difference from the rehearsal is the calendar date of
the decisions, above.

**Step 8 — the references and the list.** `check_refs` clean before staging,
**77 md files, leak scan 223 of 223 tracked**, and after `git add -A` at the
same counts — no file added. `git status --short` saved as `$W/step8.txt`;
`same.py step8`: **`step8: 25 line(s) expected, 0 difference(s)`**, exit 0 —
the twenty-five paths exactly, this file among them.

**The gating run, on the staged tree** — every file above, this record to the
line before this paragraph: `free -g` 144 GiB available; exit 0; `124.33 s
wall, 125184 KiB peak RSS`; `GREEN -- 91 unit(s), 0 failures; 5 pending,
124.3 s.`; and its lines, extracted by step 7's printed commands into
`$W/gate.txt`, compared by `same.py final`: `final: 17 line(s) expected, 0
difference(s)`, exit 0 — the six units' evidence, `55 tagged number(s)`,
`322 declared rule(s)` and `0 unresolved` as at step 7.

**What it cost.** About twenty minutes of wall clock from the first command
(23:57) to the staged tree: the baseline run 112 s, `baseline.py` 23 s, the
filtered run 79 s, the matrix 28 s, the full run 122 s and the gating run
124 s, one at a time; nothing ran in parallel, and nothing died by signal.
